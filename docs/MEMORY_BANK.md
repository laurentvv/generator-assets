# 🧠 MEMORY BANK : Pipeline Vidéo IA & Modèles SOTA (2026)
### Station de travail : AMD Radeon RX 6950 XT (16 Go VRAM GDDR6) • Vulkan 1.4 • Windows 11

---

## 📌 1. Décisions d'Architecture Validées en Production

### 👑 1.1. Modèle Vidéo SOTA Numéro 1 : **LTX-2.5 Distilled (15B Audio + Vidéo)**
* **Validation Utilisateur** : *« il est mieux que le modèle d'avant !! »*
* **Composants Validés dans `C:\Modeles_LLM\`** :
  1. `LTX-2.5-Distilled-Q4_K_M.gguf` (15.08 Go) — Diffusion DiT
  2. `gemma4-12b-with-proj-ltx-2.5-Q5_K_M.gguf` (8.86 Go) — Encodeur Gemma 4 12B (repo gated `elix3r/gemma4-12b-with-proj-ltx-2.5-GGUF`)
  3. `ltx-2.5-video-vae-conv-bf16.safetensors` (1.45 Go) — Décodeur vidéo convolutionnel
  4. `ltx-2.5-audio-vae-bf16.safetensors` (364 Mo) — Décodeur audio stéréo natif
* **Profil Mémoire Hybride Vulkan / CPU** :
  * `--backend "diffusion=vulkan0,te=cpu,vae=cpu"`
  * **VRAM GPU utilisée** : **14.05 Go fixe** (marge de sécurité de 1.95 Go sous les 16 Go de la RX 6950 XT).
  * **RAM Système CPU** : Gemma 4 (~12.1 Go) + Buffer VAE Conv (~7.3 Go).
* **Profil d'Échantillonnage Officiel** :
  * Échantillonneur : `euler_ancestral` (`--sampling-method euler_a`).
  * Guidage : `--cfg-scale 1.0` (modèle distillé, **1 seule passe par étape**, vitesse x2).
  * Sigmas distillés 8-step : `--sigmas "1.0, 0.99375, 0.9875, 0.98125, 0.975, 0.909375, 0.725, 0.421875, 0.0"`.
* **Performances Chronométrées** :
  * Échantillonnage DiT (8 étapes complètes) : **1 min 24s (84.5 secondes)** sur GPU Vulkan (~10.5s/step) !
  * Décodage Audio Stéréo : **14.3 secondes** (audio PCM 48 kHz natif synchronisé).
  * Décodage Vidéo Conv 33 trames : ~101 secondes sur 12 cœurs CPU Alderlake.
  * Temps total génération complète (768×512 @ 24 FPS, 33 trames + son) : **4.2 minutes**.

---

### 🎬 1.2. Modèle de Référence Alternatif : **Wan 2.1 14B**
* **Composants** : `wan2.1-t2v-14b-Q4_K_M.gguf` (10.12 Go) + `umt5-xxl-encoder-Q4_K_M.gguf` + `wan_2.1_vae.safetensors`.
* **Profil Mémoire** : `--backend "diffusion=vulkan0,te=cpu"`.
* **Performances** : ~58s par étape (CFG 6.0 à 7.0 = 2 passes/step). 8 étapes = ~7.5 à 8 minutes par plan.
* **Son** : Vidéo muette en sortie native, sound design synthétisé via script procédural externe (`scripts/assemble_2plans.py`).

---

### ⚙️ 1.3. Validation Empirique des 4 Modèles SOTA (Grand Rendu Nocturne)
* **LTX-2.5 Distilled (15B Audio + Vidéo)** :
  * 8 étapes en 7.30 min (échantillonnage DiT ~10s/step). Audio stéréo synchronisé natif 48 kHz.
  * Modélisation héroïque 3D d'une netteté parfaite (écailles, crocs, langue, cornes, ailes translucides).
* **Wan 2.1 (14B Flagship T2V)** :
  * 8 étapes en 42.65 min (échantillonnage DiT ~312s/step, 100% VRAM fixe 9.8 Go, 0 swap).
  * Dragon d'or impérial majestueux complètement résolu (au lieu de la bouillie floue observée à 3 steps).
* **Wan 2.2 MoE (2x 14B Dual-DiT LowNoise + HighNoise)** :
  * 8 étapes MoE (4 High + 4 Low) en 51.58 min avec `--offload-to-cpu` et `--temporal-tiling`.
  * **Piqué cinématographique absolu** : micro-rides de peau, pupilles d'ambre et spécularité photoréaliste de niveau cinéma d'animation hollywoodien.
* **MiniMax-H3 (Titan 32B Hailuo AI Vidéo + Audio)** :
  * 12 étapes en 6.61 min (échantillonnage DiT record de **16.96s/step** sur Vulkan, Qwen3-VL 32B sur CPU RAM).
  * Wyvern titanesque rugissante avec crête enflammée, ailes massives et bande-son stéréo native.

### 🛡️ 1.4. Règle Critique Vulkan AMD Windows
* **Interdiction de `--diffusion-fa` (Flash Attention)** : Déclenche l'erreur `split_k_reduce` / `ErrorDeviceLost` sous pilote AMD 26.8.1.
* **Obligation du backend hybride** : `--backend "diffusion=vulkan0,te=cpu,vae=cpu"` pour les modèles à encodeur multimodal (LTX-2.5, MiniMax-H3) afin d'éviter les débordements de buffer Vulkan.

### ⚡ 1.5. Amélioration de Netteté Vidéo : Levier 1 (AMD FidelityFX CAS)
* **Problème résolu** : Le flou d'étirement causé par le filtre Lanczos seul lors du passage de 480p/512p à 1080p.
* **Solution validée** : Chaînage du filtre officiel **AMD Contrast Adaptive Sharpening** (`scale=1920:1080:flags=lanczos,cas=0.75`).
* **Bénéfice** : Micro-contraste et séparation des arêtes (écailles, crocs, pupilles) décuplés instantanément (0s de calcul supplémentaire via encodage matériel `h264_amf`).
* **Script dédié** : `scripts/test_levier1_cas.py` générant les masters CAS, les vidéos comparatives split-screen 50/50 et les planches de zoom 1:1.

#### 🚀 1.6. Amélioration de Netteté Vidéo : Levier 4 (Super-Résolution IA 4K Real-ESRGAN)
* **Principe** : Extraction trame par trame, passage dans le réseau de neurones `4x-UltraSharp.pth` via `sd-cli -M upscale --backend vulkan0` (~7.3s/trame sur RX 6950 XT), puis réassemblage matériel AMF en Ultra HD 4K (3840×2160 @ 50 Mbps) avec filtre AMD FidelityFX CAS 0.75.
* **Résultat visuel** : **Reconstruction de micro-détails réels** (lignes de grilles, arêtes, spécularité, textures 3D) impossibles à obtenir par interpolation classique.
* **Script généralisé réutilisable** : [`scripts/upscale_video_ai.py`](file:///C:/GIT/generator-assets/scripts/upscale_video_ai.py) prenant n'importe quel fichier WebM/MP4, conservant la piste audio native et produisant le Master 4K.

### 📺 1.7. Stratégie de Diffusion YouTube & Netteté Maximale
* **Contournement de la compression AVC1** : YouTube applique automatiquement un débit destructeur (~4-6 Mbps) sur les vidéos 1080p.
* **Mastering 4K Impératif** : L'upload en **4K UHD (3840×2160)** force YouTube à encoder en **VP09 / AV01 à 30-45 Mbps**, garantissant un piqué chirurgical même sur les écrans Full HD 1080p.
* **Paramètres de Conformation Master** : Bitrate AMF 50 Mbps, AMD FidelityFX CAS 0.75, profil High 4:2:0.

### ⏱️ 1.8. Standard de Production : Scènes de 5,5 secondes (165 trames à 30 FPS)
* **Cadence Cible** : 5,5 secondes = 165 trames exactes à 30 FPS.
* **Architecture Préconisée : Chaînage Multi-Plans I2V** :
  * Génération de 2 plans (~82 trames) ou 3 plans (~55 trames) successifs.
  * Réinjection de l'ultime trame du plan $N$ (`extraire_derniere_trame`) en amorce conditionnelle (`-i`) du plan $N+1$.
  * Maintient la VRAM < 11 Go, évite l'explosion attentionnelle $O(N^2)$ (qui sature les 16 Go à 161 trames en 1 seul bloc), et élimine l'adoucissement temporel des longues passes.

### 👑 1.9. Le Workflow Roi Validé : Image Maîtresse Wan 2.2 MoE ➔ Animation I2V (Wan 2.2 MoE / Wan 2.1)
* **Principe** : Génération d'une seule image fixe de référence en 85s via Wan 2.2 MoE (`--video-frames 1`), validation visuelle instantanée à 100%, puis animation fluide Image-to-Video (`-i`).
* **Support I2V Wan 2.2 MoE** :
  * Modèles dédiés : `Wan2.2-I2V-A14B-HighNoise-Q4_K_M.gguf` (~8.99 Go) + `Wan2.2-I2V-A14B-LowNoise-Q4_K_M.gguf` (~8.99 Go).
  * Conditionneurs : `umt5-xxl` (CPU RAM) + `wan_2.1_vae` + `clip_vision_h.safetensors` (ViT).
  * Gestion VRAM : Grâce à `--offload-to-cpu`, chaque expert de 9 Go est permuté en mémoire de travail séquentiellement, maintenant la VRAM crête sous 13.5 Go / 16 Go.
* **Bénéfice** : Zéro hallucination, zéro dérive de décor, 100% de conformité géométrique et vitesse d'itération foudroyante.

---

### 🎵 1.10. Génération Musicale IA : MiniMax-Music3 GGUF via audio.cpp (Vulkan) — moteur alternatif

* **Validation Utilisateur** : *« le générateur de musique est validé [...] c'est suffisant »* (3 boucles tech 20-22 s livrées, cand_1 95 BPM promue ; QA Music Flamingo `--analyse` et recette ducking voix non testées à ce jour — optionnelles).
* **Stack validée** : `audiocpp_cli.exe` (audio.cpp v0.7.3, release Windows x64 **Vulkan**, `C:\audio-cpp\`) + paquet `audio-cpp/MiniMax-Music3-GGUF` (mix Q4_0/Q8_0 : LM 6 Go + RVQ 0,66 + flow 1,3 + encoder 0,1 + vocoder 0,2 Go + `config/` + `tokenizer/`) dans `C:\Modeles_LLM\MiniMax-Music3-GGUF\`. Zéro PyTorch, même philosophie que sd-cli/llama.cpp.
* **Backend** : **Vulkan retenu** — RTF 55,8 (10 s de musique en 9 min 17 s) vs CPU RTF 75,2 (12 min 31 s, i7-13700KF 20 threads). La RX 6950 XT (RDNA2, pas de matrix cores, `int dot: 0`) reste ~1,3× plus rapide que le CPU pour ce modèle. Budget : ~21 min par boucle de 20 s (générée sur 23 s).
* **Sortie réelle** : WAV **stéréo 44,1 kHz** (pas 32 kHz comme annoncé par la fiche HF) → rééchantillonnage rationnel 160:147 vers 48 kHz via `scipy.resample_poly` dans `core/music_ai.py`.
* **CLI de génération** : `audiocpp_cli --task gen --family minimax_music3 --model <dossier> --backend vulkan --threads 16 --metrics --text "<desc EN>" --request-option lyrics=[Instrumental] --request-option duration_sec=23 --request-option num_inference_steps=30 --out out.wav`. `lyrics` est **requis** ; `[Instrumental]` donne des lits sans voix. `--metrics` imprime RTF/durée. Options session préfixées famille (`--session-option minimax_music3.<option>=<valeur>`, ex. `mem_saver`, `rvq_depth_decoder_gguf`).
* **Bouclage « percussif » validé** (technique du doc « Générateurs Musique en Boucle ») : BPM par autocorrélation de l'enveloppe d'onsets (test synthétique : 121 détecté pour 120 réel), coupe alignée au nombre entier de mesures, snap passages par zéro, micro-fondu equal-power 20 ms → couture mesurée Δ0,4 dB. Mode ambiant : crossfade 1 s. **V2 essentielle** : le modèle compose une structure de morceau (intro en fondu, break, outro) même en 23 s → ne PAS couper depuis t=0 ; rechercher le meilleur point de boucle parmi les fenêtres ≥ durée cible d'un nombre entier de mesures, en appariant l'énergie tête/queue sur fenêtres 50-500 ms (cand_1 : 20,3 s / Δ0,1 dB ; cand_2 : 21,6 s / Δ0,3 dB).
* **Standard « lit derrière voix off »** : highpass 80 Hz + creux −3 dB @ 2,8 kHz puis **bed normalisé −30 LUFS** (ffmpeg loudnorm 2 passes, linéaire) ; ducking par recette `sidechaincompress`+`amix` générée dans `recette_mixage_voix.txt`. ffmpeg **9.0.1** (`C:\ffmpeg\dist\bin\ffmpeg.exe`, prioritaire sur l'ancien build Amuse 7.1.1).
* **Licences** : MiniMax-Music3 communauté (MIT-like, commercial OK < 20 M$ ; **signaler la musique IA dans la description YouTube**). Music Flamingo (QA optionnelle `--analyse`, via `llama-cli --mmproj --audio`) = **non commercial**, désactivé par défaut.
* **Workflow** : `main.py -w music_bg "<desc>" --duration 20 --candidats 10` → N candidats générés, validés (couture/clipping/LUFS), TOUS finalisés en bed + MP3 (`candidats/`), meilleur promu à la racine de `output/music_bg/`.
* ⚠️ **Crash silencieux exit 127 = exception C fatale** : `soundfile.write(..., format="OGG")` fait un **stack overflow C** (libsndfile Vorbis) au-delà de quelques secondes d'audio → processus tué instantanément, sortie Python bufferisée perdue, Git Bash affiche « exit 127 » sans aucun log. Diagnostic : relancer avec `python -c "import faulthandler; faulthandler.enable(); ..."`. Règle : **OGG toujours via ffmpeg** (`convertir_ogg()` de `core/music_ai.py`), soundfile réservé au WAV. Ce bug a détruit le premier batch complet en phase 4.
* ⚠️ **Resets du pilote GPU AMD** (LiveKernelEvent 141, dumps `LiveKernelReports\WATCHDOG`) possibles pendant les longues générations Vulkan audiocpp → un candidat peut mourir en cours de route. Règle : batch résilient (`scripts/generer_boucles_music_bg_batch.py`) — un candidat par processus, reprise sur fichiers existants, 2 essais Vulkan puis CPU, graines explicites (le défaut du runtime est déterministe).

### 🎵 1.11. Génération Musicale IA : ACE-Step 1.5 Turbo GGUF — moteur PAR DÉFAUT depuis le 2026-09-05

* **Validation Utilisateur** : comparatif A/B sur le même prompt tech (3 candidats par moteur) → *« c encore mieux que l'autre !! »*. ACE-Step promu défaut (`--moteur acestep` implicite) ; Music3 reste via `--moteur music3`.
* **Stack** : `audiocpp_cli.exe` (audio.cpp **v0.8.2-audio8-perf-hotfix** depuis le 2026-09-25 — crash ace_step de v0.8.2 pure corrigé sur le hotfix, A/B même jour vs v0.8.1 +11 % dans la marge de bruit, sfx stable_audio OK ; windows épinglé hotfix dans `engines_manifest.json`) + paquet monolithique `audio-cpp/audio.cpp-gguf → ACE-Step1.5-GGUF/turbo/ace-step-1.5-turbo-bf16.gguf` (**9,4 Gio**, tout embarqué : DiT turbo + LM planner 1.7B + text encoder Qwen3 + VAE, `embedded_sidecars=true`) dans `C:\Modeles_LLM\ACE-Step1.5-GGUF\`. Licence **MIT** (aucune mention obligatoire). Zéro PyTorch.
* ⚠️ **v0.8.2 REJETÉE le 2026-09-24 puis RÉHABILITÉE via le hotfix (2026-09-25)** : v0.8.2 pure crashait au chargement ace_step (`STATUS_ILLEGAL_INSTRUCTION` / 0xC000001D, vulkan ET cpu, CLI et `core.music_ai`) → rollback v0.8.1 ce jour-là. Le lendemain, **v0.8.2-audio8-perf-hotfix fonctionne** (smoke 12 s turbo OK, RTF 3,53-3,82) — le commit réparateur n'est pas identifié, il est entre v0.8.2 et le hotfix. A/B même jour hotfix vs v0.8.1 : moyennes 3,68 vs 3,32 (+11 %, marge de bruit du poste — v0.8.1 faisait 3,25-3,85 intra-jour) → hotfix conservé. **Leçon : toujours vérifier/restaurer `version.json` en même temps que les binaires lors d'un rollback** (le mismatch a fait croire à la veille que v0.8.2 était encore installée). Détail : `C:\audio-cpp\README.md`.
* **CLI** : `audiocpp_cli --task gen --family ace_step --model <chemin/vers/le.gguf> --backend vulkan --task-route text2music --text "<desc>" --duration-seconds 28 --num-inference-steps 8 [--seed N] [--lyrics "..."] [--request-option bpm=126] --out out.wav`. **`--model` doit pointer le .gguf LUI-MÊME** : le dossier donne une erreur trompeuse (`source 'safetensors'... missing lm_chat_template`). `--lyrics` omis = instrumental natif. Turbo distillé : 8 pas suffisent. BPM/tonalité/signature imposables (`--request-option bpm= / keyscale= / timesignature=`).
* **Perf Vulkan (RX 6950 XT)** : ~42 s par génération de 28 s (RTF ~1,5) — **~36× plus rapide que Music3** (RTF 55,8). Sortie native **48 kHz stéréo** (aucun rééchantillonnage). ⚠️ **Référence RTF à jour (smoke post-maj v0.8.0, 2026-09-16)** : ~52-54 s par 28 s (**RTF ≈ 1,84-1,93**) — la baseline historique 1,613 (14/09) et le ~1,5 (05/09) ne sont **plus reproductibles** : dérive machine ≈ +15 % sans cause identifiable (charge vérifiée OK, aucun processus, pilotes inchangés). **Leçon : toute alerte « régression RTF » doit être tranchée par un A/B même jour (v_prev rollback → mesure → re-maj → mesure), jamais par comparaison à une baseline d'un autre jour** — l'alerte +16 % sur v0.8.0 s'est effondrée à la parité (v0.7.4 = 1,836 vs v0.8.0 = 1,85-1,93 le même jour). Impact projet : ~10 s de plus par boucle de 28 s, absorbé par le workflow batch.
* ⚠️ **q8_0 non fonctionnel** pour cette famille (`docs/gguf.md` audio.cpp : « planner sampling can fail » — « found no valid token ») → **bf16 obligatoire** (d'où 9,4 Gio au lieu de 6,2).
* ⚠️ **Fondu de sortie structurel** : le modèle termine chaque morceau par un fade de ~4-6 s (jusqu'à −62 dB). Première itération : coutures Δ62 dB. Correctifs (dans `core/music_ai.py`, profitent aux deux moteurs) : recherche de boucle **restreinte à la zone d'énergie stable** (`_zone_stable`) + marge de génération **+8 s** pour acestep (+3 s music3) dans le workflow. Résultat : coutures 0,4-3,0 dB.
* ⚠️ **Bridage CDN Hugging Face** : curl mono-connexion tombe de 18 Mo/s à ~0,6 Mo/s après quelques Gio (3 h restantes pour 9,4 Gio). Contournement : téléchargeur parallèle par segments HTTP Range, 10 connexions (`scratch/telecharger_acestep_parallele.py`) → **~112 Mo/s**, reprise sur segments, préfixe curl réutilisé. À réutiliser pour tout gros fichier HF.
* **Variantes XL (DiT 4B, testées le 2026-09-06)** : `--variante xl-turbo` — paquets **absents de HF** (seuls turbo/base y sont), hébergés sur le **miroir ModelScope** `HereIsMark/audio.cpp-gguf` (miroir fidèle : turbo identique octet pour octet). `xl-turbo-bf16.gguf` = 14,23 Gio dans `xl-turbo/`. Exige `--load-option ace_step.dit_model_path=acestep-v15-xl-turbo` (paquets spécifiques à une variante — câblé dans `generer_musique_acestep(variante=...)`). Perf Vulkan : **~92 s par candidat de 28 s (RTF ~3,3)** vs 42 s pour le turbo 2B → **~2,2× plus lent**. Tient en VRAM 16 Go avec `mem_saver` (aucun OOM). Run comparatif même prompt : coutures 2,0/11,4/4,3 dB (cand_1 promue), 80-120 BPM. `xl-sft` (CFG, 25 pas par défaut dans le workflow) non testé — installable : `uv run python scripts/download_acestep15_gguf.py xl-sft`. Installateur variantes : `download_acestep15_gguf.py [turbo|xl-turbo|xl-sft|tout]`.
* **Chanson complète avec paroles (validée le 2026-09-06)** : « La Symphonie du Silence » puis « Le Neuvième Fils » (univers Vent-Gris, paroles utilisateur) — validation utilisateur : *« c'est incroyable »* / *« chanson très bien également »*. 240-260 s, paroles FR via `--lyrics` + `--language fr` (param `langue` de `generer_musique_acestep`), balises de structure `[Intro]/[Verse]/[Chorus]/[Bridge]/[Outro]` respectées, xl-turbo, **~850-925 s de génération (RTF ~3,5-3,9)**. Le retry Vulkan automatique a sauvé le 2e morceau (1re tentative morte par reset pilote AMD — l'écueil §1.10 se confirme sur les longues durées). Nettoyage des paroles indispensable : retirer toute citation/annotation (sinon chantée). Script générique : `scripts/generer_chanson_acestep.py paroles.txt --duree 240`. Routes d'édition (repaint/cover/lego/extract) toujours non testées.
* **✅ Retrait de voix HTDemucs VALIDÉ (workflow `retrait_voix`, 2026-09-06)** — verdict utilisateur : *« retrait de la voix : OK validé »*. `main.py -w retrait_voix -i <morceau>` : rééchantillonnage auto 44,1 kHz (HTDemucs le EXIGE — « sample rate mismatch » sinon) → séparation 4 stems (écriture via **`--out-dir`**, `--out` refusé en multi-sortie) → instrumental = drums+bass+other (`amix normalize=0`) + stems conservés (vocals.wav isolé). Modèle `htdemucs-f16.gguf` (84 Mo !) dans `C:\Modeles_LLM\HTDemucs-GGUF`, Vulkan, ~1 min pour 4 min de musique. Code : `core/separation.py` + `workflows/retrait_voix.py`, sorties `output/retrait_voix/<nom>/`. `llb_instrumental.mp3` conservé dans `output/music_chanson/` sur demande utilisateur (base pour tests futurs).
* **❌ Route cover : REJETÉE DÉFINITIVEMENT (2026-09-06)** — avec le chant source : voix fantômes, « pas bon » ; sur instrumental sans chant (style LLB corrigé via Wikipédia — German gothic rock/dark wave/hard rock) : *« le clone : horrible »*. **❌ Essence SA3 rejetée aussi** (qualité small insuffisante ; rendu piste complète « pas bon »). **Le « clonage de musique » est clos** : besoin couvert par `musique_adn` (BPM+tonalité) et `retrait_voix` (stems).
* **✅ SA3 Medium ESSENCE init_audio → WORKFLOW `musique_essence` INTÉGRÉ le 2026-09-09 (validation utilisateur : « c bien » sur les instrumentaux)** — suite du test texte (pop) : mode `init_audio` avec référence LLB (`llb_extrait_30s.wav`), prompt sobre identique, graine 42, 30 s, échelles 0,7 et 0,5 testées. **RTF 0,80** (24 s/génération). Verdict relatif : **0,5 > 0,7** (« le plus proche » de la référence) — le conditionnement audio contourne le biais pop du texte, SA3 Medium fait mieux que small (qualité rejetée en son temps). Fichiers : `output/test_sa3_medium/sa3_medium_essence_0.{5,7}.mp3`. **Batch d'itération (2026-09-09, 8 candidats : échelles 0,40-0,60, graines 7/1337/2026, prompt minimal)** : 1er verdict — `ess_g7_s50` (graine 7, échelle 0,5) = « ça va mais toujours pas l'essence » → **la capture d'essence à 0,5 est sensible à la graine** (42 l'attrape, 7 non) ; verdict 2 — `ess_g42_s40` (0,40, graine 42) = **NOUVEAU CHAMPION proximité** : « le plus proche, voix + rythme presque musical, toujours une touche de pop » ; écarts timbraux vs original notés par l'utilisateur : batterie originale forte/sourde/volumineuse non rendue, guitare originale moins saturée que le rendu modèle (la « voix » à 0,40 = probable bave du chant LLB, zone copie proche). **Trajectoire : plus bas = plus proche.** Vague 2 (2026-09-09) : `w2_g42_s35`, `w2_g42_s30` (bornes copie), `w2_g7_s40` (loterie graine au nouveau sweet spot), `w2_ref60_g42_s40`/`w2_ref60_g42_s45` (ancre référence 60 s, plus forte). **Verdicts batch (2026-09-09) : PLATEAU 0,30-0,45 graine 42** — s45/ref60, s40/ref60, s40/ref30, s35, s30 = tous « batterie bien, guitare pas mal, voix proche de l'original » (idem ×5) → l'essence est STABLE sous 0,45 (pas de loterie contrairement à 0,5), la réf 60 s ne différencie pas dans cette zone. La voix = chant LLB qui bave (zone quasi-copie) — ressemblance jugée positivement. **Piste ouverte : essence + `retrait_voix` (HTDemucs) post-génération = instrumental avec l'essence LLB SANS la voix clonée** (2 instrumentaux générés : `output/retrait_voix/ess_g42_s40/instrumental.mp3`, `output/retrait_voix/w2_ref60_g42_s45/instrumental.mp3`). **✅ WORKFLOW INTÉGRÉ le 2026-09-09 (règle test validé ⇒ workflow)** : `main.py -w musique_essence "<style EN>" -i <référence> [--duration 30] [--scale 0.45] [--seed 42] [--keep-vocals]` — pipeline SA3 Medium init_audio (RTF 0,8-0,9) → retrait_voix HTDemucs → **instrumental avec l'essence de la référence, sans la voix clonée** (aucun risque contenu dérivatif). Code : `core/music_essence.py` + `workflows/musique_essence.py` ; sorties `output/musique_essence/<nom>/` (brut + instrumental + stems) ; test end-to-end OK (échelle 0,45, réf 60 s). ⚠️ Leçon convention : flags CLI en ANGLAIS (`--scale`, pas `--echelle` — identifiants EN, prose FR). **Stable-Audio-3-Small (2,2G) supersédé par Medium → candidat suppression (accord explicite requis).**
* **❌ SA3 Medium gothic rock TEXTE (2026-09-08, TESTÉ NON VALIDÉ — verdict utilisateur : « toujours pop »)** : `stable_audio_3_medium_f16` (5,43 Gio, `C:\Modeles_LLM\Stable-Audio-3-Medium-GGUF\`) — le « Medium » défaut de spec, signalé par la veille `hf-modeles-gguf` (instruction permanente). Recette de référence : tempo/tonalité **dans le prompt** (SA3 n'a pas d'options planner bpm/keyscale — ace_step only), graine 42, 30 s. **RTF 0,48 Vulkan** (30 s en 14,5 s — small = 0,25, ace_step turbo = 1,6, Music3 = 55,8), 44,1 kHz stéréo, brut −11,5 LUFS (+0,3 dBTP → limiteur avant tout usage). Extrait : `output/test_sa3_medium/sa3_medium_gothic_30s.mp3`. **Bilan registre sombre : le conditionnement TEXTE de SA3 Medium sort aussi du pop — comme ACE-Step (plafond §1.11) et Music3.** Piste SA3 non testée : `init_audio` (essence depuis référence audio, le mode fort de SA3 — échelle utile 0,7 sur small) sur **Medium**, qui contourne le biais pop du texte. Aucun workflow (règle). Modèle retéléchargeable (`audio-cpp/audio.cpp-gguf`).
* **✅ SA3 Small SFX (foley) → WORKFLOW `sfx` MOTEUR IA PAR DÉFAUT (VALIDÉ utilisateur le 2026-09-09 : 4 SFX « ok » + pluie v3 validée après 3 itérations)** — `stable-audio-3-small-sfx-f16.gguf` (2,20 Gio, `C:\Modeles_LLM\Stable-Audio-3-Small-SFX-GGUF\`, paquet officiel `audio-cpp/audio.cpp-gguf` ; finetune foley/SFX de SA3 small = base `stabilityai/stable-audio-3-small-sfx`). Signalé par la veille `hf-modeles-gguf` via le repo `thepatch/stable-audio-3-small-sfx-GGUF` — **⚠️ précision : ce repo cible le moteur DISTINCT `sa3.cpp` (betweentwomidnights, port C++/GGML de Stable Audio 3, CPU/CUDA/Vulkan/Metal, philosophie zéro PyTorch)** — pas audio.cpp ; le test local passe par le paquet officiel audio.cpp mono-fichier, même famille `stable_audio`. Verdicts d'écoute (graine 42, `output/test_sa3_small_sfx/`) : **épée 2 s / pas gravier 4 s / porte 3 s / whoosh 2 s = « ok »** ; **pluie/orage 8 s rejetée 3×** (« faible volume » puis « son trop faible » puis « pluie assourdie pas terrible ») → **trois causes mesurées successivement** : ① crête seule insuffisante (corps à −35,2 LUFS sous un pic isolé, TP 19 LU au-dessus, LRA 3,4 — la normalisation de crête laissait le corps à −27,7) ; ② **texture spectralement déséquilibrée par le prompt** (« …distant thunder rumble » = grondement 50-250 Hz à +18 dB, crépitement HF à −17..−28 dB → rendu assourdi quelle que soit la dynamique) ; ③ l'acompressor seuil fixe de la 2e chaîne écrasait aussi la modulation du crépitement. **Correctifs intégrés à `core/sfx_ia.py` (itération 3)** : ❶ **le choix du prompt = 1er levier qualité — nommer le CONTENU** (« heavy rain falling on glass, dense patter with natural splashes » → HF/LF −2 dB) plutôt que l'ambiance (→ −39 dB), écart mesuré sur 4 variantes ; ❷ **rognage auto des silences/fondus d'entrée-sortie SA3** (−45 dBFS, garde-fou 50 %) ; ❸ traitement nappe I < −20 LUFS = **gain linéaire + alimiter plafond −1,5 dBFS** (`level=disabled` obligatoire — ne touche que les crêtes, texture intacte) au lieu du compresseur ; sinon crête 0,9 (échantillons transitoires validés inchangés). **v3 `sfx_pluie_8s_v3.mp3` : 6,5 s utiles, −17,2 LUFS, HF/LF −3,9 dB** (vs −39 brut), zone des « ok » (épée −15,1, whoosh −18,2) — **VALIDÉE par l'utilisateur (« ok on valide »), dossier clos le 2026-09-09** ; helper `mesurer_lufs(filtre_amont=…)` étendu dans `core/music_ai.py` ; **gothic rock 30 s rejeté** (« gothic non » — plafond SA3 en conditionnement texte confirmé même sur le finetune SFX, et « son limite compressé » : la bande-son full music n'est pas le terrain de ce modèle). **RTF 0,19-0,67 Vulkan** (30 s = 5,6 s wall — le plus rapide du poste ; clips courts = surcharge fixe ~1,3 s), 44,1 kHz stéréo. **Workflow intégré le jour même (règle test validé ⇒ workflow)** : `main.py -w sfx "<prompt EN libre>" --duration N --seed 42 [--sfx-engine procedural]` — défaut `ia`, procédural numpy conservé en repli ; code `core/sfx_ia.py` + `workflows/sfx.py` ; sorties WAV PCM16 + OGG (OGG via ffmpeg, règle §1.10 — `exporter_sfx_godot` corrigée au passage, elle utilisait encore soundfile/OGG). Piste ouverte : moteur `sa3.cpp` à surveiller (veille) — perf annoncée 1,7 s pour 12 s sur GPU 8 Go.
* **🧪 Campagne « anti-pop gothic rock » (2026-09-07, testé NON VALIDÉ — 9 candidats, `output/test_acestep_sft/`)** : objectif sortir du pop (« GIMS bullshit ») sur la recette LLB. **Résultat clé : les variantes turbo/xl-turbo sont distillées guidance et IGNORENT `--guidance-scale`** (doc audio.cpp) → sur ces variantes, ancres/négatifs/descriptions luttaient sans levier. **Classement** : 🥇 **cand_B = xl-sft 4B + CFG 7.0 + 25 pas** (« passe le mieux ») — SEUL candidat à respecter tempo imposé (85/83) ET mode mineur réel (E mineur, là où tout le reste glisse en relative majeure = signature pop) ; 🥈 cand_I = MiniMax-Music3 (F mineur ✓, 72 BPM, **37 min de génération** pour 28 s, brut −28 LUFS) ; base 2B + CFG (« plus grave mais mou » ; rendu −14,5/−16 LUFS vs témoin pop −10,5 : le « mou » a une composante mixage réelle) ; D/E/F (lm_cfg_scale 5 / négatif dur / combiné) : **déstabilisent le planner** — BPM imposé ignoré (109/168/173 BPM, leçon punk 163 confirmée) ; A (xl-turbo témoin) et C (base+CFG) = « POP 100 % ». **⚠️ Plafond structurel confirmé : le réalisme instrumental (basse/batterie/multi-guitares crédibles) dépasse la capacité des DiT 2B/4B — le guidage (CFG) ne peut pas l'inventer.** Verdict utilisateur B : « très loin du résultat souhaité ». **Conséquences** : instruction permanente AGENTS.md = tester tout nouveau modèle musical signalé par la veille `hf-modeles-gguf` (pipeline `text-to-audio` ajoutée le 2026-09-07) avec la recette de référence (83 BPM/C# minor/graine 42) ; variantes **base** (HF ; **supprimée le 2026-09-19** — audit des modèles du disque, testée une fois en campagne cand_C « POP 100 % », jamais câblée dans le code, retéléchargeable `audio-cpp/audio.cpp-gguf`, `download_acestep15_gguf.py base` la réinstalle) et **xl-sft** installées dans `C:\Modeles_LLM\ACE-Step1.5-GGUF\` ; `download_acestep15_gguf.py` délègue ≥ 1 Gio au téléchargeur parallèle (**166 Mo/s sur ModelScope**, HTTP Range 206 — vs 2 Mo/s curl ; xl-sft 14,23 Gio en 3,5 min) et connaît `base`. Détails CFG dans `C:\audio-cpp\README.md`.
* **🔭 Piste YuE2-3B GGUF (2026-09-10, runtime débloqué le 2026-09-13 → §1.22)** : candidat direct anti-pop (lyrics-to-song complet chant+instru, chanson jusqu'à 3 min 27 en démo audio.cpp) ; GGUF publié (`audio-cpp/Yue2-3B-GGUF`), support famille `yue2` sur la **branche `dev`** d'audio.cpp (toujours absent des releases au 13/09) → **test réalisé le 2026-09-13 via scratch basculé sur dev, clip court OK / écueil Vulkan clip long — détail complet en §1.22 ; verdict rendu le 13/09 (« encore du POP ») et RE-CONFIRMÉ à la ré-écoute du 14/09 (« non c'est pop ») → rejeté cible gothique, cible CLOSE**. Démos locales `output/test_yue2/` ; ⚠️ **licence poids CC BY-NC 4.0** (vs ACE-Step MIT) — limite chaîne YouTube monétisée. **Écoute utilisateur des démos officielles (2026-09-10) : « pop moderne » — sans valeur prédictive pour la cible gothique : 4/5 démos ont des prompts pop/jazz-funk par construction** (indie pop, piano pop ×2, jazz funk cover + exemple upstream pop) — le modèle a obéi aux prompts, ça ne préjuge pas d'un prompt gothic rock ; le test de référence (83 BPM/C# minor/graine 42) reste seul juge.
* **📋 Bilan complet des essais « musique depuis la référence LLB » (2026-09-06, session close sur demande — tout est conservé pour reprise)** :
  ① **Dubbing parlé** (`voix_off` clonée sur l'instrumental + ducking) : REJETÉ (*« ça fait parler, c'est pas terrible »*). Recettes mixage utiles retenues : ducking dur (ratio 12, musique −6 dB) = musique inaudible sous la voix (« c'est inversé ! ») ; **le bon équilibre = v3 : musique −2 dB + sidechaincompress ratio 3/attack 60/release 700** (voix +4 dB ≈ −14 LUFS) ; ⚠️ piège ffmpeg : `apad=whole_dur` sur la clé sidechain sinon le mix se coupe à la fin de la voix. Fichiers : `llb_dub_poc_v1/v2/v3.mp3`.
  ② **SVC Seed-VC** (timbre du chanteur → voix utilisateur, route `v1_svc` F0) : TESTÉ NON VALIDÉ (POC `llb_chant_clone_v1.mp3` vers 1). API : `--audio` + `--voice-ref` (+ `--task-route v1_svc`), `--target-voice` refusé. Par conception : **paroles et langue ORIGINALES conservées** (l'utilisateur « ne comprend rien » — la source chante en anglais). Paquet `seed-vc-mlx-q8_0.gguf` 2,91 Gio installé. Workflow `chant_clone` créé par erreur (malentendu « ok pour tester » ≠ validé) — **annulé avant commit** ; à recréer uniquement après validation explicite.
  ③ **Chanson FR dans le groove LLB via `musique_adn`** (paroles FR Vent-Gris `paroles_llb_fr.txt`, 83 BPM imposés, xl-turbo) : **REJETÉ ×2 — « toujours pop, c fou, c gentillet »** : v1 `llb_fr_adn.mp3` (D# major détecté sur piste COMPLÈTE — l'extrait 30 s donne C# minor : chroma mixte du morceau, d'où l'importance du forçage) ET v2 `llb_fr_adn_minor.mp3` (C# minor forcé) — **le planner sort du pop-rock même en mineur avec style sobre** (leçon Ⅲ confirmée et étendue au registre rock). **Leçon ④ : les registres sombres VALIDÉS d'ACE-Step = dark folk néoclassique à ancre artiste** (Wardruna — les 2 chansons « incroyables ») ; le gothic rock échappe au planner. **v3 `llb_fr_adn_bauhaus.mp3` (ancres Bauhaus/Sisters of Mercy + négatif anti-pop « pop, catchy, upbeat, cheerful, bright, polished, mainstream, radio-friendly ») : écoutée le 2026-09-08 → VERDICT « NUL » — REJETÉE.** La piste en suspens est CLOSE : les ancres artistes gothique + négatif dur ne sortent pas ACE-Step du pop sur ce registre. **Bilan définitif anti-pop sur ACE-Step : toutes les branches textuelles (adn, minor, bauhaus, campagne 9 candidats) épuisées et rejetées — la seule percée locale = essence SA3 Medium init_audio échelle 0,5 (2026-09-08).** Autres pistes non testées : modèle **base** (CFG), itérations à graines.
  ④ **Essais supprimés** (rejetés sans valeur de reprise) : essences SA3 ×4, covers ×4, probes ADN, tests workflows ; **conservés** : 2 chansons validées, `llb_xl_adn`, source LLB + extraits, `llb_instrumental.mp3`, stems retrait_voix, dubbing v1-v3, SVC v1, adn FR ×3, paroles FR.
* **Workflows intégrés le 2026-09-06 (règle AGENTS.md : test validé ⇒ workflow)** : ① `main.py -w chanson "<paroles ou .txt>" [--style-musique "<EN>"] [--duration 180]` — recette chansons exacte (xl-turbo par défaut, langue fr, balises structure, style Vent-Gris par défaut). ② `main.py -w musique_adn "<style EN sobre>" -i <référence> [--duration 60] [--tonalite] [--negatif] [--lyrics paroles.txt]` — détection BPM+tonalité auto imposée au planner (test : 83 BPM + C# minor retrouvés sur llb_extrait_30s) ; corrige au passage un piège du script : le suffixe « completely instrumental » n'est plus ajouté quand des paroles sont fournies. ⚠️ Piège argparse réglé : `--variante` default None + repli par workflow (xl-turbo chanson/adn, turbo music_bg) sinon le défaut du flag écrasait la recette validée.
* **Routes audio-conditionnées (cover/repaint/lego/extract) — ⚠️ Vulkan bloqué, CPU requis (diagnostiqué le 2026-09-06)** : ces routes chargent le **VAE encoder** dont le graphe exige un buffer unique de ~4,5-4,8 Gio. Or le pilote AMD Windows plafonne la taille d'un buffer Vulkan à 4 Gio (`maxBufferSize`, VK_KHR_maintenance4 / Vulkan 1.3 — limite **par buffer**, pas la VRAM totale) → « Requested buffer size exceeds device buffer size limit » immédiat, la VRAM reste quasi vide dans le moniteur. La taille du buffer est fixe (30 s de source a demandé PLUS que 60 s : 4,84 vs 4,52 Gio) → raccourcir l'audio ne sert à rien. Le XL OOM même avant l'encoder (14 Gio + graphs > 16 Go). **Solution validée : `--backend cpu --threads 20`** — cover 30 s en 77 s (RTF 2,58) sur turbo 2B. Première utilisation : cover « Love Like Blood » (groupe allemand de rock gothique) → dark folk Vent-Gris.
* **Pipeline « ADN depuis référence » (2026-09-06, nuit — carte honnête des essais)** : `scripts/generer_depuis_reference.py <audio> --style "<EN>" [--tonalite] [--negatif] [--avec-paroles]` — BPM + tonalité auto (ou forcés) imposés au planner. **Seule version validée par l'utilisateur : `llb_xl_adn.mp3`** (45 s, C# mineur détecté sur extrait 30 s, description SOBRE « German gothic rock 1990, dark wave, hypnotic tribal groove, deep pulsing bass, chiming chorus guitars », xl-turbo). **Tout ce qui a été « amélioré » ensuite a été rejeté** : tonalité full-track D# majeur → « gentil/pop » ; basse poussée (« dominant bass leading ») → 73 % basse, « POP pire qu'avant » ; agressivité punk → 163 BPM ; probe mineur + négatif dur + mastering compressé −12 LUFS → « loupé aussi ». **Leçons** : ① le mode MAJEUR sonne pop quoi qu'on fasse — toujours vérifier/imposer mineur pour du rock sombre ; ② les superlatifs du style (« dominant », « hard-hitting ») sont pris trop au pied de la lettre → descriptions sobres ; ③ la sensibilité compositionnelle du planner penche pop ; pistes non testées : modèle **base** (CFG, contrôle fin), **repaint** sur l'original lui-même, itérations à graines autour de la recette gagnante `llb_xl_adn`. SA3 `init_audio` : échelle utile ≈ 0,7 (0,2-0,45 = copie quasi littérale avec chant fantôme, ≥0,85 = ancre perdue, 0,7 = pulse conservé mais qualité « small » jugée insuffisante).
* **Comparatif mesuré (même prompt, 3 candidats)** : Music3 → coutures ≤ 0,3 dB, ~25 min/candidat, 32→48 kHz ; ACE-Step turbo 2B → coutures ≤ 3 dB (0,4 promu), ~42 s/candidat, 48 kHz natif, meilleur à l'écoute (jugement utilisateur) ; ACE-Step xl-turbo 4B → coutures ≤ 4,3 dB (2,0 promu), ~92 s/candidat, 48 kHz natif (écoute comparative à valider par l'utilisateur).
* **Édition** : routes `--task-route complete|lego|extract|cover|repaint` (+ `--audio`, `--repaint-start/end`) non testées à ce jour — stems/repaint documentés dans `docs/models/ace_step.md` du dépôt audio.cpp.

### 🎵 1.12. MusicGen Melody Large (Meta, 2024) — testé le 2026-09-06, NON RETENU

* **Motif du test** : demande d'évaluation de `facebook/musicgen-melody-large` (2,62 Md params, avril 2024, text2music + conditionnement mélodie par chroma 12 bins).
* **Faisabilité philosophie dépôt (C++ Vulkan + GGUF, zéro PyTorch) : ❌ impossible**. audio.cpp v0.7.2 n'a **aucune famille musicgen** (model_specs + CHANGELOG + dépôts GGUF HF `audio-cpp/audio.cpp-gguf` et miroir ModelScope vérifiés — le seul tensor « musicgen » est un encodeur de style interne à ControlFoley). Aucun GGUF valide sur HF : `yyang12/MusicgenV1-f16-gguf` = archi llama/chat (conversion sans rapport), `mradermacher/music_generation_model-GGUF` = merge llama MIDI-texte. **Seule voie locale : transformers (PyTorch CPU)** — test fait en venv isolé jetable `C:\temp\mgt` (torch 2.14+cpu, transformers 5.16.1), modèle safetensors f32 9,8 Gio dans `C:\Modeles_LLM\musicgen-melody-large` (téléchargé via hf_transfer multi-connexions).
* **Perf mesurées (i7-13700KF, 20 threads, CPU, f32)** : text2music **RTF 11,60** (10 s en 115 s) ; avec chroma **RTF 11,89**. Modèle chargé en <1 s (mmap). Sortie **32 kHz mono** PCM16, très chaude (−8,4 LUFS, **+1,5 dBTP** → limiteur obligatoire avant tout usage). Comparaison : ACE-Step turbo RTF 1,5 Vulkan 48 kHz stéréo → **~8× plus lent, qualité inférieure attendue (modèle 2024)**.
* **⚠️ Régression silencieuse transformers v5 : le conditionnement mélodie est INOPÉRANT**. Triple preuve : ① kwarg `melody` (API v4) **ignoré sans erreur** (l'argument v5 est `audio`) ; ② A/B même graine + 2 mélodies différentes (LLB vs symphonie) → **sorties bit-identiques** (diff 0,0) ; ③ chroma réelle vs chroma nulle injectées manuellement dans `generate()` → identiques aussi (le code de `modeling_musicgen_melody` référence `input_features` mais la boucle `generate()` v5 ne l'utilise pas). Métrique cohérente : cosinus chroma sortie vs référence 0,140 pour un plancher de bruit à 0,118. **MusicgenMelody en v5 = text2music pur** ; tester la mélodie sérieusement exigerait transformers 4.x (non fait — voir verdict).
* **⚠️ Bug transformers v5 n°2** : `MusicgenMelodyFeatureExtractor` appelle `torchaudio.functional.resample` sur une **liste** → `AttributeError` dès que l'audio n'est pas à 32 kHz. Contournement : rééchantillonner soi-même (`rolloff=0.945, lowpass_filter_width=24`) et passer `sampling_rate=32000`.
* **Licence ⚠️ : CC-BY-NC 4.0 (non commercial)** — incompatible avec une chaîne YouTube monétisée, contrairement à ACE-Step (MIT) et MiniMax-Music3. Seconde raison bloquante, indépendante de la technique.
* **Verdict : NON RETENU, aucune intégration.** Plus lent, mono 32 kHz, licence NC, pas de route GGUF, et son seul atout différenciant (calage sur mélodie) est cassé dans transformers v5 — besoin déjà couvert localement par SA3 `init_audio` (essence de référence, RTF 0,25, Vulkan) et ACE-Step route cover (CPU, RTF 2,58). À retester uniquement si ① audio.cpp ajoute une famille musicgen-GGUF, ou ② transformers corrige la régression melody en v5.
* **Nettoyage fait le 2026-09-06 sur décision utilisateur** (« on oublie ») : modèle 9,8 Gio + venv + artéfacts d'écoute supprimés (~13,3 Gio libérés). Le modèle est retéléchargeable à tout moment (`snapshot_download('facebook/musicgen-melody-large')` avec hf_transfer) si un retest devient pertinent (voir conditions ci-dessus).
* **Leçon transverse** : kwargs inconnus **ignorés silencieusement** par les processors transformers v5 (« will be ignored » en warning) — toujours vérifier par A/B (même graine, entrée changée) qu'un conditionnement atteint vraiment le modèle.

### 🎙️ 1.13. Voix off TTS + clonage vocal français — 3 moteurs GGUF testés le 2026-09-06 (chaîne YouTube)

* **Besoin** : cloner une voix française (référence 11,8 s de l'utilisateur, `output/comparatif_tts/ref_voix_laurent.wav`) puis lui faire lire des textes **avec expression**. Stack 100 % locale conforme philosophie : `audiocpp_cli` (v0.7.3, Vulkan) + paquets GGUF q8_0 monolithiques dans `C:\Modeles_LLM\`.
* **Paquets installés** : `Qwen3-TTS-12Hz-1.7B-Base-GGUF` (2,51 Gio), `VoxCPM2-GGUF` (2,75 Gio), `Fish-Audio-S2-Pro-GGUF` (5,88 Gio) + **bonus** `Qwen3-ASR-0.6B-GGUF` (1,07 Gio) pour transcrire la référence (voir écueil ③).
* **Commandes validées** :
  ```powershell
  # Zéro-shot (fish / voxcpm2 uniquement) :
  audiocpp_cli --task tts --family fish_audio --model <fish q8_0.gguf> --backend vulkan --metrics --text "<FR>" --out out.wav
  audiocpp_cli --task tts --family voxcpm2  --model <voxcpm2 q8_0.gguf> --backend vulkan --metrics --language French --text "<FR>" --out out.wav
  # Clonage (les 3) :
  #   qwen3 : --voice-ref ref.wav --reference-text "<transcript de ref>" [--instruct "<style/émotion>"] --language French
  #   fish  : --voice-ref ref.wav --reference-text "<transcript>" (+ balises expression DANS le texte : [whisper] [excited] [pause]…)
  #   voxcpm2 : --voice-ref ref.wav (transcript NON requis)
  # ASR (transcription d'une référence) :
  audiocpp_cli --task asr --family qwen3_asr --model <qwen3-asr-0.6b-q8_0.gguf> --backend vulkan --language fr --audio ref.wav
  ```
* **Perf Vulkan (RX 6950 XT)** : RTF fish 1,46 / voxcpm2 1,68 sur ~7 s de parole (chargement inclus). Sorties **mono** : fish 44,1 kHz • voxcpm2 48 kHz • qwen3 24 kHz (limite qualité HF — à réserver intermédiaire, rééchantillonner).
* **Expression** : fish = **balises inline libres dans le texte** (`[whisper]`, `[excited]`, `[pause]`… 15 000 tags, la plus riche) ; qwen3 = `--instruct "<instruction style/émotion>"` ; voxcpm2 = non testé. Voix design (voix décrite plutôt que clonée) dispo : `--task vdes` (paquet qwen3 VoiceDesign séparé, non installé).
* **⚠️ Écueils** : ① qwen3-TTS **Base** = pas de zéro-shot (« requires voice clone reference audio ») ET **transcript obligatoire** (« ICL mode requires reference text ») ; ② fish = zéro-shot OK mais **transcript obligatoire pour le clonage** (« inline reference audio requires reference_text ») ; ③ pas de transcript sous la main → transcription ASR locale (qwen3-asr 0.6B) : pipeline complet autonome validé (m4a → wav mono ffmpeg → ASR → clone). Voix de référence : enregistrement smartphone m4a 48 k stéréo converti `ffmpeg -ac 1 pcm_s16le` — suffisant.
* **Licences** : qwen3-tts/qwen3-asr **Apache-2.0** ✓ production chaîne OK • voxcpm2 **Apache-2.0** ✓ • fish s2-pro **Research License = commercial payant** ⚠️ (référence qualité uniquement, pas de production chaîne sans licence).
* **À écouter** (`output/comparatif_tts/ECOUTE_*.mp3`) : `ref_voix_laurent` (original) vs `qwen_fr_clone_laurent` (instruct présentateur), `voxcpm2_fr_clone_laurent` (transcript-free), `fish_fr_clone_laurent` (balises whisper/excited) + `*_sans_ref` pour le timbre natif fish/voxcpm2. Phrase test identique pour tous : « Trois heures du matin, le serveur principal s'effondre… ». **Validation utilisateur en attente** — voxcpm2 rend la phrase en 4,6 s vs ~6 s ailleurs (débit rapide ou troncature ? à l'écoute).
* **⚠️ Niveau de la référence** : l'enregistrement smartphone mesurait **−39,3 LUFS** (crêtes −16,5 dBTP → seulement 15 dB de marge). Référence normalisée en **−18 LUFS / −1,5 dBTP** (`ffmpeg -af "loudnorm=I=-18:TP=-1.5:LRA=7"` — LRA 2,5 LU donc gain quasi linéaire, sans dénaturer la voix) → `ref_voix_laurent_norm.wav`, clones relancés en `*_v2.wav`. **Règle future : viser −18/−20 LUFS à la capture (micro près de la bouche), et vérifier le niveau AVANT tout clonage** (`ffmpeg -af volumedetect`). Sorties v2 mesurées ~−19 à −20 dB moyen.
* **Validation utilisateur (2026-09-06)** : *« franchement les 3 sont bien »* — qwen3-tts, VoxCPM2 et Fish S2-Pro jugés tous bons à l'écoute sur le clonage FR avec expression. **Choix production chaîne = licence** : qwen3-tts et VoxCPM2 (Apache-2.0) libres d'usage ; Fish réservé à la référence qualité (commercial payant). Détail différenciant si besoin trancher : voxcpm2 = 48 kHz natif (meilleure bande passante), qwen3 = `--instruct` (direction d'expression la plus souple), fish = balises inline les plus riches.
* **Workflow intégré le 2026-09-06** : `main.py -w voix_off "<texte ou fichier.txt>" --voix-ref <wav/mp3/m4a> [--instruct "…"] [--moteur qwen3|voxcpm2|fish] [--lufs-voix -16]` — pipeline complet : contrôle du niveau de la référence (seuil −26 dB moyen → normalisation auto −18 LUFS), transcription ASR auto (qwen3-asr) si le moteur l'exige, génération, finalisation −16 LUFS + MP3. Sorties `output/voix_off/<nom>/`. Testé fin à fin sur qwen3 (**RTF 0,64** Vulkan, chargements compris) et fish. Code : `core/voix_off.py` + `workflows/voix_off.py`. Note : la finalisation loudnorm linéaire (TP −3) peut plafonner à ~−19 LUFS sur les voix très crêtées — sans conséquence derrière un bed à −30 LUFS.
* **Non testés** : stabilité sur textes longs (chunking `--text-chunk-size` fish 200 cars), voix design `--task vdes`, chatterbox (anglais seulement), omnivoice (600+ langues, licence à vérifier), qualité 1.7B CustomVoice qwen3.

### 🧊 1.14. Image → 3D : TRELLIS.2-4B via trellis.cpp (GGUF, Vulkan) — VALIDÉ utilisateur le 2026-09-06

* **Besoin** : passer d'une image 2D générée (sprites/icons du dépôt) à un **vrai maillage 3D volumique** pour Godot — l'extrusion 2,5D (`mesh3d --shape cutout`) fait du bas-relief, pas du volume fermé (cas d'école : le casque, §Capabilities README). Enquête du jour : TRELLIS.2-4B / Hunyuan3D-2.1 / stable-fast-3d → **seul TRELLIS.2 a un runtime C++/GGML** ; Hunyuan3D-2.1 = PyTorch+CUDA (10 Go shape / 21 Go texture, rasterizers custom) ; SF3D = nvdiffrast CUDA + gated → écartés, accès HF non demandé.
* **Outil** : [`pwilkin/trellis.cpp`](https://github.com/pwilkin/trellis.cpp) **v0.6.0** (19/08/2026, MIT, projet très actif depuis 07/2026) — port ggml complet : preprocess+cutout BiRefNet → DINOv3 → flow sparse-structure → shape SLAT (FlexiDualGrid, marching cubes, rebouchage) → texture SLAT + PBR → décimation QEM GPU + atlas xatlas → **GLB PBR prêt Godot**. Concurrent moins mature écarté : `localai-org/trellis2cpp` (13★, pas de binaire Windows).
* **Installé** : `C:\trellis\` (archive `trellis-vulkan-windows-x64.zip`, 96 Mo : trellis-cli.exe + trellis-server.exe HTTP + ggml-vulkan.dll) ; GGUF f16 `ilintar/trellis2-gguf` (10 fichiers, **16,4 Go**) dans `C:\Modeles_LLM\trellis2-gguf\` (variantes q4/q8 existent sur HF, non testées). README outil : `C:\trellis\README.md`.
* **Upgrade v0.8.1 le 2026-09-26 (accord utilisateur, nuit)** : A/B propre post-reboot (512, seed 42, casque.png, jambes séquentielles, script `scripts/proto_ab_trellis_v081.sh`, sortie `output/trellis_ab_v081/`) — **v0.8.1 = 630 s vs v0.6.0 = 583 s (+8 %, marge de bruit)** : la régression +51 % de v0.8.0 (rejetée le 25/09, 1193,8 vs 789,1 s) n'est PAS reproduite. Rendus Blender comparés équivalents (`rendu_v060.png`/`rendu_v081.png`, mêmes silhouette/textures, PBR v0.8.1 très légèrement moins saturé) ; GLB structurellement identiques (121 k vs 123 k sommets, 1 matériau, 2 textures). Pixal3D + réduction mémoire 1024p désormais dans le binaire production (à explorer) ; rétopo quad toujours absente du prebuilt Windows (builds sources uniquement). Rollback : `C:\trellis\backups\backup_20260926_v060\`.
* **Commande validée** :
  ```bash
  cd /c/trellis && ./trellis-cli.exe -i <image.png> -o <out.glb> --models C:/Modeles_LLM/trellis2-gguf --res 512   # ou 1024 (défaut)
  ```
* **Perf Vulkan (RX 6950 XT, f16)** — tout passe du premier coup, aucun crash watchdog : device vu `fp16: 1 | matrix cores: none` (16 368 Mo).
  * **res 512 : 10 min 44 s** → GLB 5,3 Mo, 144 200 faces, atlas 1024² (mesh brut 1,06 M sommets ; flow ss ~11 s/pas, shape ~27 s/pas).
  * **res 1024 (cascade LR→HR) : 55 min 10 s** → GLB 12 Mo, 293 356 faces, atlas 2048² (mesh brut 6,25 M sommets → remesh DC ~20 M faces → QEM 300 k ; shape HR ~152 s/pas = ~30 min à lui seul). **Budget réaliste : 512 pour itérer, 1024 pour le master.**
* **⚠️ Écueils** : ① pas de « matrix cores » Vulkan sur RDNA2 → ~4-6× plus lent que les benchmarks Strix Halo du projet (6 min @1024) ; ② `_base.png` exporté à côté du GLB = aperçu de l'**atlas**, pas l'image source ; ③ cutout par défaut = BiRefNet (`--bg-removal auto`), le mode threshold découpe les hautes lumières → trous.
* **Licence** : trellis.cpp **MIT** ; TRELLIS.2-4B **MIT** ; DINOv3 Apache-2.0 ; BiRefNet MIT — **production jeu + chaîne OK, zéro contrainte**.
* **À inspecter** (`output/trellis_smoke/`) : planches de contrôle Blender 4 vues + texture `casque_512_planche.png` / `casque_1024_planche.png`, GLB `casque_512.glb` / `casque_1024.glb` (script de rendu réutilisable : `output/trellis_smoke/rendu_controle.py`).
* **Validation utilisateur (2026-09-06)** : *« énorme ça marche »* — planches 512 et 1024 validées à l'inspection.
* **Workflow intégré le 2026-09-06** : `main.py -w mesh_ia "<prompt>" | -i <image.png> [--res 512|1024|1536] [--faces-cible N] [--seed N] [-o nom]` — prompt → image Flux (workflow `generate`, détourée : l'alpha est conservé par trellis) → GLB PBR + 4 rendus Blender + planche 2×3 dans `output/mesh_ia/<nom>/`. Code : `core/mesh_ia.py` (trellis + rendus + planche) + `workflows/mesh_ia.py`. Veille étendue : releases `pwilkin/trellis.cpp` (via `C:\trellis\version.json`) + nouveaux GGUF sur `ilintar/trellis2-gguf`. Défaut `--res 512` (itération), 1024 = master. **Réduction optionnelle `--faces-cible N`** (défaut 0 = master complet, demandée par l'utilisateur le 2026-09-06) : décimation Blender headless (collapse délimité UV/SHARP, PBR conservé) → `<nom>_<res>_jeu.glb` ; mesuré 144 200 → 29 999 faces (5,3 → 1,5 Mo). Repères : 30 k = item héro, 10 k = prop décor, 3 k = clutter répété, ≥8 k si silhouette très courbe. Test bout-en-bout workflow validé (potion 512 : 8 min 53 s, GLB 7,4 Mo).
* **Non testés** : variantes q4/q8 des GGUF (division ~2-4 de la taille/VRAM, qualité à mesurer), `--res 1536`, mode `--no-texture`, `trellis-server.exe` (HTTP POST /generate), stabilité sur objets complexes multi-objets.

### 🧑‍🎨 1.15. Skins MPFB : recette validée = base + Vent-Gris + features PEINTES (jamais de portrait collé) — consolidé le 2026-09-08

* **🎯 RÈGLE PROJET (utilisateur, 2026-09-08) : JAMAIS coller le portrait 2D sur le skin.** L'identité vient du personnage MPFB (morphologie canon + assets) ; le skin = texture MakeHuman de base + colorimétrie Vent-Gris + **features signature via couche MakeUp (ink layer MPFB)**. La projection de portrait (YuNet+affine) est **refusée** — abandonnée au profit du MakeUp nodal officiel.
* **⚠️ Coordonnées UV hm08 — VÉRITÉ ABSOLUE ÉTABLIE PAR INSPECTION DU MAILLAGE ET RENDUS (2026-09-08)** :
  - Axe de symétrie du visage : **$V = 0.4832 \rightarrow Y = 1058$ px** (sur texture 2048²).
  - Orientation du visage sur la texture : **axe vertical tête (front $\rightarrow$ menton) le long de $+X$ !** Axe horizontal (oreille gauche $\rightarrow$ oreille droite) le long de $+Y$ !
  - Repères exacts validés sur le maillage :
    * Front : $X \sim 1540, Y = 1058$
    * Bord palpébral inférieur (base des cils) : $X \sim 1720-1725$, $Y \in [950..1012]$ (gauche) et $[1104..1166]$ (droite)
    * Cœur du creux orbitaire / cernes : $X \sim 1745-1755$, $Y \in [965..1010]$ (gauche) et $[1106..1151]$ (droite)
    * Sillon lacrymal (tear-trough) : début à $(1728, 1012)$, descente vers $(1750, 985)$
    * Pointe du nez : $(1788, 1058)$
    * Pommettes / flush de fatigue : $(1865, 940)$ et $(1865, 1176)$
    * Temples creuses : $(1680, 860)$ et $(1680, 1256)$
    * Lèvres : $(1938, 1058)$
  - ⚠️ **Écueil corrigé (2026-09-08)** : Une ancienne note erronée prétendait que le visage était en $X \in [256..960] / Y \in [384..1152]$ — c'était en réalité **le thorax / torse** ! Peindre là-bas laissait le visage totalement vierge de maquillage. Le visage MakeHuman hm08 vit bien sur l'îlot droit ($X \in [1450..2000], Y \in [800..1300]$).
* **💄 MakeUp ink layer Elian validé (2026-09-08, zéro bug, photo-fidèle)** :
  - Générateur : `scripts/build_makeup_elian.py` — extrait YuNet de `assets/portraits/elian_portrait.png` ($\Delta L = 70.6$, ratio de contraste cernes/peau $[0.535, 0.529, 0.588]$) $\rightarrow$ transfert vers le gamut Vent-Gris $\rightarrow$ peinture multi-calques (pénombre diffuse, croissant orbitaire raccord au bord palpébral, sillon lacrymal profond, creux palpébral supérieur, blush pommettes, teinte douce lèvres) avec flous gaussiens étagés (4 à 14 px) sans aucun coup de pinceau dur.
  - Sorties : `godot_assets/skins/elian_enfant/makeup/elian_fatigue_ventgris.{png,json}` + copie active MPFB dans `%APPDATA%\Blender Foundation\Blender\5.2\mpfb\data\data\ink_layers\`.
  - Yeux cyan Elian : texture dédiée `elian_cyan_eye.png` (teinte cyan/turquoise $H=85-95$, limbe net, lueur interne) dans `godot_assets/skins/elian_enfant/` + MPFB eyes.
* **🔧 Leçons et correctifs de rendu Blender (2026-09-08, validés en production)** :
  ① **Bug du menton tronqué en rendu tête** : Quand les vêtements (`Human.donitz_monk_robe`, etc.) sont masqués (`hide_render = True`), les modificateurs MASK (`Delete.donitz_monk_robe`) sur le corps restent actifs et coupent le cou et le bas de la mâchoire ! $\rightarrow$ Toujours désactiver les modificateurs de masque `"delete"` quand les vêtements sont cachés.
  ② **Bug des lumières Area dans le vide** : Par défaut dans Blender, un Area light pointe vers $-Z$ (vers le sol) ! Sans rotation ou contrainte, une lumière à 1,8 m éclaire le sol et laisse le personnage dans le noir complet. $\rightarrow$ Toujours ajouter une contrainte `TRACK_TO` (`TRACK_NEGATIVE_Z`, `UP_Y`) ciblant le personnage.
  ③ **Cadrage vertical plein pied (896×1536)** : En mode `sensor_fit = 'AUTO'` avec une résolution portrait ($Y > X$), Blender cale la dimension capteur de 36 mm sur l'axe vertical. Le demi-angle vertical est $\theta = \arctan(18 / 55) \approx 18.1^\circ$. La distance exacte pour une hauteur $H \approx 0.96$ m avec marge 28 % est $dist = (H \times 1.28) / (2 \tan \theta) \approx 1.88$ m.
  ④ **Débruitage Cycles** : Activer `scn.cycles.use_denoising = True` garantit des rendus nets dès 48 échantillons sans grain résiduel. Rendus de production validés : `exports/elian_tete_face.png`, `exports/elian_tete_tiers.png`, `exports/elian_perso_face.png`, `exports/elian_perso_tiers.png`.
  ⑤ **Ordre d'assemblage MPFB critique : Rig Mixamo AVANT les assets .mhclo (vêtements, chaussures, cheveux)** : `HumanService.add_builtin_rig(human, "mixamo")` DOIT impérativement être appelé AVANT `HumanService.add_mhclo_asset(...)`. Si le rig est créé après, MPFB génère les vêtements et cheveux sans groupes de sommets (0 vertex groups) et sans modificateur Armature, laissant la tenue figée en l'air en pose en A pendant que le corps s'anime dans Godot. En créant le rig en premier, MPFB détecte l'armature existante sur le corps, injecte automatiquement le modificateur `Armature` (`Human.rig`) et transfère par coordonnées barycentriques les poids de déformation des 52 os Mixamo sur chaque vêtement et cheveu (validé avec 49 animations dans `elian_mixamo_viewer.tscn`).
* **🚀 Workflow CLI 100% Automatique intégré (2026-09-08)** : `main.py -w character3d --portrait <img.png> [--character nom] [--age 0.12] [--gender 0.0] [--eye-color cyan] [--samples 48]`. Encapsule 100% de la chaîne de bout en bout : détection YuNet ONNX, échantillonnage et transfert de gamut 3D avec compensation SSS, peinture vectorielle 5 calques UV hm08 (2048²), manifeste JSON MPFB2, personnalisation des yeux, **assemblage automatique du corps 3D MPFB2 dans Blender (MakeSkin + MakeUp + rig Mixamo préalable + vêtements/cheveux pondérés automatiquement + export .blend et .glb)** et 4 rendus studio Cycles avec contraintes TRACK_TO et correction de masques. Code : `core/mpfb_ops.py` (`creer_corps_personnage_mpfb`, `rendre_personnage_blender`) + `workflows/character_makeup.py` (`character3d`, `character_makeup`).

### 🐉 1.16. MiniMax-H3 Ref2VA (vidéo de référence → vidéo+audio) : VALIDÉ utilisateur le 2026-09-09 → workflow `h3_ref2va` — ⚠️ CASSÉ sur master-864+ (51 segments + OOM submit, tests du 14-15/09) : **production H3 = build parallèle `C:\SD-6b3edaa\`** (re-validé le 15/09 matin) en attendant fix amont (#1976)

* **Contexte** : analyse de `hradec/ComfyUI-HR-Endless-Sampler` (génération de vidéos longues H3 par chunks avec continuation Ref2VA — la queue du chunk N devient la référence `<Video 1>`/`<Audio 1>` du chunk N+1, nœud ComfyUI/PyTorch inutilisable ici). But du test = valider que le mécanisme Ref2VA fonctionne en CLI sd-cli Vulkan (1 seul chunk, 22 frames).
* **Modèle requis SPÉCIFIQUE** : `minimax_h3_ref2va_pruned-Q4_K_M.gguf` (10,64 Gio, source `leejet/MiniMax-H3-GGUF` — le repo `MiniMax-AI/MiniMax-H3-GGUF` du guide est gated/401) — **distinct** du `minimax_h3_fl2va_pruned` (T2VA/I2VA/FL2VA). DiT ref2va ≠ DiT fl2va.
* **Recette gagnante (essai 4, sortie `output/test_h3_ref2va/ref2va_test_22f.webm`)** :
  ```bash
  sd-cli.exe -M vid_gen \
    --diffusion-model C:/Modeles_LLM/minimax_h3_ref2va_pruned-Q4_K_M.gguf \
    --vae C:/Modeles_LLM/minimax_h3_video_vae_fp16.safetensors \
    --audio-vae C:/Modeles_LLM/minimax_h3_audio_vae_fp32.safetensors \
    --llm C:/Modeles_LLM/qwen3vl_32b_minimax_h3-Q2_K_M.gguf \
    -p "Use the dragon from <Video 1> and the roar from <Audio 1> as the opening state. ..." \
    --ref-video <dossier frames 24 fps> --ref-video-audio <wav appairé> \
    --cfg-scale 1.0 -W 864 -H 480 --diffusion-fa --offload-to-cpu --rng cpu \
    --fps 24 --video-frames 22 --seed 42 --max-vram 10 \
    --backend "diffusion=vulkan0,te=cpu,vae=cpu" -t 16
  ```
* **⚠️ Placement mémoire OBLIGATOIRE sur 16 Go VRAM / 31,8 Go RAM (3 crashes avant la solution)** :
  ① `--backend te=cpu` : le Qwen3-VL 32B Q2 (13,1 Go) **déborde la VRAM à lui seul** (graphe + fragments → `ErrorOutOfDeviceMemory` puis exit 127 silencieux) → tout sur CPU.
  ② `--backend vae=cpu` : le VAE vidéo réserve un compute buffer de ~5 Go en VRAM avant le Qwen → conflit ; sur CPU le buffer passe à 9,8 Go **en RAM** (swap massif, ~31 s/tile).
  ③ `--max-vram 10` : sans plafond, sd-cli charge les 11 Go de poids DiT + 4 Go de compute d'un bloc (15,4/16 Go) → **device lost au premier submit** (driver AMD). Avec le plafond, graph-cut en 2 segments (8,5 Go + 1,7 Go), stable.
* **Timings mesurés (RX 6950 XT, réf 12 frames, 22 frames générées, 20 steps Euler)** : encode VAE réf **869 s** • encode audio VAE 0,45 s • Qwen3-VL condition **98 s** • sampling **2 983 s (149 s/step, GPU)** • decode audio 6 s • decode vidéo **251 s** • **total ~70 min pour 0,875 s de vidéo → RTF ≈ 4 800×**. Référence communauté : ~16,9 s/step (tout résident) → facteur ~9 dû au re-staging des poids à chaque step (graph-cut).
* **Résultat** : webm VP8 864×480 + **audio PCM 32 kHz stéréo généré** (max −12 dB). Frames inspectées : dragon cohérent avec la référence LTX (face caméra, ailes déployées), souffle de feu orange vers caméra en formation au milieu de séquence, décor raccord (pierre sombre, contre-jour chaud) → continuité personnage+décor plausible. **Verdict œil/oreille utilisateur EN ATTENTE.**
* **Conclusion opérationnelle** : mécanisme Ref2VA **fonctionne en CLI Vulkan** (condition préalable au workflow « endless » type ComfyUI-HR-Endless-Sampler). Mais RTF rédhibitoire en l'état pour la chaîne — leviers identifiés : ① RAM 64 Go (tuerait le swap du VAE CPU ~31 s/tile → probablement ×5-10), ② continuation à résolution réduite (option `video_continuation_res` du nœud ComfyUI — divise l'encode réf par 4-8), ③ chunks plus longs (39-56 frames) pour amortir l'encode, ④ grille frames `17k+5` (22/39/56…), 24 fps imposé, Ref2VA **incompatible avec `--init-img`/`--end-img`** (pas de keyframe de raccord en CLI — la référence porte seule la continuité).
* **Statut : VALIDÉ utilisateur le 2026-09-09 (« résultat très bon ») → encapsulé en workflow `main.py -w h3_ref2va`** (`workflows/h3_ref2va.py` + `core/diffusion.py::generer_video_ref2va_h3`) : extraction automatique de la queue de référence (N dernières frames + WAV appairé via ffmpeg), pré-contrôle `check_charge_systeme` intégré (refus si machine occupée), grille 17k+5 et alignement 32 px automatiques, `--dry-run` pour inspecter la commande. Paramètres : `-i <vidéo|dossier frames>`, `--ref-frames` (12), `--frames` (22), `--max-vram` (10), `--steps` (20). Un seul chunk — la boucle endless multi-chunks n'est PAS validée.
* **🐉 Turbo LoRA 8 steps (idée n° 1 de `docs/recherche_comfyui_2026-09-09.md`) : VALIDÉ utilisateur le 2026-09-09 (« très bonne qualité, dragon très proche, son très bien ») → flag `--turbo` du workflow `h3_ref2va`** (LoRA + steps 8 automatiques, `DEFAULT_H3_REF2VA_TURBO_LORA` dans `core/config.py`, support LoRA générique `lora=` dans `generer_video_ref2va_h3`). LoRA `minimax_h3_ref2v_turbo_8step_v1.0_768p_bf16.safetensors` (1,29 Gio, HF `lightx2v/Minimax-h3-Turbo`, installé dans `C:\Modeles_LLM\loras` — prendre les variantes NON `_comfyui_`). Recette = recette validée ci-dessus (seed 42, cfg 1.0, 864×480, réf `ref_frames12`+`ref_audio_05.wav`) avec 3 changements : `<lora:minimax_h3_ref2v_turbo_8step_v1.0_768p_bf16:1.0>` en fin de prompt + `--lora-model-dir C:/Modeles_LLM/loras --lora-apply-mode auto` + `--steps 8`. **Timings : sampling 1 078 s (8 × ~134 s/step — le LoRA ne coûte rien par step) vs 2 983 s → −64 % ; total 2 275 s ≈ 38 min vs ~70 min → −46 % (RTF ≈ 2 600× vs 4 800×)**. Chargement LoRA en `at_runtime` OK sur modèle quantisé (apply 1,15 s, aucun surcoût VRAM constaté, graph-cut inchangé 2 segments). **Résultat** (`output/test_h3_ref2va/ref2va_turbo8_22f.webm`) : format identique (VP8 864×480 + PCM 32 kHz stéréo) ; audio plus présent (max −4,7 dB / mean −19,9 dB vs −12 / −26,7 dB, verdict utilisateur « son très bien ») ; détail des textures égal ou meilleur ; **raccord à la référence nettement SUPÉRIEUR à la baseline** (frame 1 quasi identique à la dernière frame de réf — comparer `compare_raccord.png`) ; trajectoire différente (zoom avant sur la tête vs plan large) — divergence normale de distillation à même seed, non gênante (verdict utilisateur). Vigilance : LoRA entraîné 768p testé en 480p sans artefact visible ; CFG 1.0 conservé (fiche GitHub sans reco CFG). Gain projeté boucle endless 18 chunks : ~21 h → ~12 h.

* **📝 Grammaire prompt H3 officielle (distillée le 2026-09-09 des guides du Prompt-Writer duckyshell — `guides/VIDEO_PROMPT_WRITING_GUIDE_base_en.md` + `_ref_en.md`, grammaire MiniMax de référence)** : à utiliser pour les PROCHAINS prompts vidéo H3 (nos prompts actuels en une ligne sont très en dessous de ce que le modèle attend) :
  - **Modes simples (T2VA/I2VA/FL2VA/L2VA)** = 3 champs : `integrated_multimodal_description:` (corps chronologique plan par plan), `overall_soundscape:` (1-4 phrases : ambiances, bruits physiques, respirations — JAMAIS dialogues ni musique), `non_diegetic_music:` (1-3 phrases : instrumentation/tempo/dynamique, pas de mots d'humeur abstraits ; `N/A` si rien).
  - **Mode full-reference (notre Ref2VA)** = 6 sections dans l'ordre : `subject_definitions:` (définit `<Subject N>` = contenu visuel réutilisable extrait des références, ex. « `<Subject 1> is the dragon in <Video 1>`, black scales… » ; `<Video N>` = relation vidéo entière ; `<Audio N>` = rôle de l'audio), `summary:` ([types de tâche] — **notre boucle endless = `[video continuation + audio reference]`**), `retention_analysis:` (marqueurs figés : `fully_preserved`/`partially_preserved`/`attribute_transfer`/`weak_reference` ; audio : `fully_copy`/`partially_copy`/`reference`/`weak_reference`), `detailed_description:` (350-500 mots EN, style en 1-2 phrases AVANT `[Shot 1]`), + les 2 champs son.
  - Syntaxe transversale : `[Shot 1]` SANS timestamp ; `[Shot 2] At 00:03.500, the camera cuts to…` (instants de cut strictement croissants) ; caméra = mouvement + amplitude + vitesse en anglais naturel DANS le plan (« The camera pushes in with small amplitude at slow speed toward… ») — vocabulaire : `Push In/Pull Out`, `Zoom In/Out`, `Pan/Truck Left-Right`, `Tilt Up/Down`, `Pedestal Up/Down`, `Arc Shot`, `Tracking Shot`, `Static Shot`, `POV`, `Roll` ; locuteurs stables `(S1)/(S2)`, dialogues verbatim `<d>[English] …</d>`, voix off = `says in an off-screen voiceover` + « while his lips remain completely closed » ; texte visible entre guillemets anglais ; `<scenetrans>`/`<cutoff>` pour un son traversant un cut/tronqué par la fin.
  - Numérotation : `<Video N>` et `<Audio N>` sont numérotés INDÉPENDAMMENT (une vidéo de réf ne crée `<Audio N>` que si sa piste est réellement référencée).
* **🔧 Comportement interne sd-cli sur la référence Ref2VA (lu dans les sources, scratch bisect master ~#1940, 2026-09-09) + intégration des leviers « endless »** :
  - Les trames de `--ref-video` sont redimensionnées vers une taille **nominale 768 px de base** (ratio conservé, plafond 768×1344, aligné 32 ; taille source CONSERVÉE si plus petite) — PAS vers la résolution de sortie (la recette validée passe des trames 4K pour une sortie 864×480). Conséquence : pré-downscaler les trames ne réduit le latent réf QUE si le résultat passe sous la nominale (ex. 4K × 0,15 → 576×320).
  - Le dossier de réf est **tronqué à la grille 17k+5** (12 trames → 5) et seules les **5 PREMIÈRES trames** du dossier sont encodées (l'ordre du dossier compte). Pour un raccord maximal : fournir exactement 5 trames finissant au raccord (encodage VAE réf ÷ ~2,4). Non validé.
  - La « recette validée » = 12 trames + WAV **0,5 s** (`ref_audio_05.wav` = 0,5 s, pas 5 s) — aligné avec l'auto-extraction du workflow. La « fenêtre audio qui remonte » (leçon Motion-Context : fenêtre finissant au raccord, découpée dans la timeline complète) est un levier NON validé.
  - Les 3 leviers sont implémentés en options du prototype `scripts/proto_endless_h3.py` (`--ref-frames 12`, `--ref-audio-sec 0.5`, `--ref-scale 1.0` — défauts = recette validée) : à sonder via la boucle, et ne remonter dans le workflow `h3_ref2va` QUE si l'A/B est validé utilisateur. Feature requests sd-cli publiées le 2026-09-09 pour aller plus loin : [#1951](https://github.com/leejet/stable-diffusion.cpp/issues/1951) (résolution réf + carry-over latent), [#1952](https://github.com/leejet/stable-diffusion.cpp/issues/1952) (images sémantiques dans le prompt Qwen TE), [#1953](https://github.com/leejet/stable-diffusion.cpp/issues/1953) (VACE Wan 2.2).

## 🎬 2. Masters et Fichiers de Production Validés





| Nom du Fichier | Spécifications | Modèle Utilisé | Statut & Rendu |
| :--- | :--- | :--- | :--- |
| `output/ansible_nexus/ansible_nexus_wan22_4k_master.png` | 3840×2160 Ultra HD 4K, CAS 0.75 | **Wan 2.2 MoE + 4x-UltraSharp** | 👑 **Image Maîtresse Validée (Ansible Nexus)** |
| `output/overnight/esrgan_4k/wan22_moe_dragon_4k_ultrasharp.mp4` | 3840×2160 Ultra HD 4K @ 16 FPS, 50 Mbps | **Wan 2.2 MoE + 4x-UltraSharp** | 👑 **Master Cinéma 4K Absolu** |
| `output/overnight/esrgan_4k/01_ltx25_dragon_4k_ultrasharp.mp4` | 3840×2160 Ultra HD 4K @ 24 FPS, Audio AAC, 50 Mbps | **LTX-2.5 + 4x-UltraSharp** | 👑 **Master 4K + Audio Natif** |
| `output/overnight/cas_sharp/03_wan22_1080p_sharp_cas.mp4` | 1920×1080 @ 16 FPS, AMD CAS 0.75 | **Wan 2.2 MoE** | 🟢 Validé CAS Net (2.2 Mo) |
| `output/overnight/cas_sharp/01_ltx25_1080p_sharp_cas.mp4` | 1920×1080 @ 24 FPS, Audio AAC, CAS 0.75 | **LTX-2.5** | 🟢 Validé CAS Net + Son (3.4 Mo) |
| `output/overnight/01_ltx25_8steps_dragon_1080p.mp4` | 1920×1080 @ 24 FPS, Audio AAC | **LTX-2.5 (8 steps)** | 🟢 Validé Héroïque + Son (7.3 min) |
| `output/overnight/02_wan21_14steps_dragon_1080p.mp4` | 1920×1080 @ 16 FPS, Muet | **Wan 2.1 14B (8 steps)** | 🟢 Validé Dragon Impérial (42.6 min) |
| `output/overnight/03_wan22_moe_18steps_dragon_1080p.mp4` | 1920×1080 @ 16 FPS, Muet | **Wan 2.2 MoE (8 steps MoE)** | 🟢 Validé Piqué Photoréaliste (51.5 min) |
| `output/overnight/04_minimax_h3_20steps_dragon_1080p.mp4` | 1920×1080 @ 24 FPS, Audio AAC | **MiniMax-H3 (12 steps)** | 🟢 Validé Wyvern Titanesque + Son (6.6 min) |
| `output/comparatif/ltx25_dragon_8steps_1080p.mp4` | 1920×1080 @ 60 FPS, Audio AAC 48 kHz | **LTX-2.5 (8 steps)** | **Validé Supérieur** ⭐⭐⭐⭐⭐ |
| `output/comparatif/ltx25_dragon_3steps_1080p.mp4` | 1920×1080 @ 60 FPS, Audio AAC 48 kHz | **LTX-2.5 (3 steps)** | **Validé Turbo (37s)** ⚡ |
| `output/film_10s/film_2plans_dragon_slowmo_5s.mp4` | 1920×1080 Full HD @ 60 FPS, 5.0s, Audio Stéréo AMF | **Wan 2.1 14B (Plans 1 + 2, 14 steps)** | Validé Cinéma 🎬 |

---

## 🛠️ 3. Scripts de Production Déployés

1. [`scripts/generate_wan22_single_image.py`](file:///C:/GIT/generator-assets/scripts/generate_wan22_single_image.py) : **Génération d'image de référence Wan 2.2 MoE (85s) + 4K AI Upscale**.
2. [`scripts/download_wan22_i2v_models.py`](file:///C:/GIT/generator-assets/scripts/download_wan22_i2v_models.py) : **Téléchargement automatisé des modèles Wan 2.2 I2V MoE 28B** (`HighNoise` et `LowNoise` Q4_K_M).
3. [`scripts/animate_ansible_nexus_wan22_i2v.py`](file:///C:/GIT/generator-assets/scripts/animate_ansible_nexus_wan22_i2v.py) : **Animation cinématique I2V Wan 2.2 MoE vers Master 4K 5.5s**.
4. [`scripts/upscale_video_ai.py`](file:///C:/GIT/generator-assets/scripts/upscale_video_ai.py) : **Super-Résolution IA 4K universelle** (Real-ESRGAN Vulkan0 + AMD CAS + AMF Hardware 50 Mbps).
5. [`scripts/generate_ansible_nexus.py`](file:///C:/GIT/generator-assets/scripts/generate_ansible_nexus.py) : Pipeline de production complet pour les architectures IT complexes (génération Wan/LTX + 4K AI + extraction de trames + zoom 100%).
6. [`scripts/chain_video.py`](file:///C:/GIT/generator-assets/scripts/chain_video.py) : Chaînage continu multi-plans I2V avec extraction de trame de transition.
7. [`scripts/conform_youtube_hd.py`](file:///C:/GIT/generator-assets/scripts/conform_youtube_hd.py) : Conformation matérielle AMF Lanczos + AMD CAS 0.75.
8. [`scripts/generate_ltx25_8steps.py`](file:///C:/GIT/generator-assets/scripts/generate_ltx25_8steps.py) : Rendu maître LTX-2.5 avec sigmas distillés et audio stéréo.
9. [`scripts/run_overnight_all_sota.py`](file:///C:/GIT/generator-assets/scripts/run_overnight_all_sota.py) : Orchestrateur de grand benchmark nocturne SOTA.

### 🎬 1.17. Intro Vent-Gris de nuit : LTX-2.5 I2V chaîné (recette de survie) + Wan 2.2 vid_gen CASSÉ sur 6b3edaa puis RE-FONCTIONNEL sur master-864 (2026-09-13, `--vae-on-cpu` ≤ ~20 trames)

* **Contexte** : plan d'établissement 10 s @ 24 fps 16:9 du Château du Vent-Gris (jeu « L'HÉRITIER DU VIDE »), I2V depuis l'image de référence utilisateur (`Forteresse_de_pierre_en_hiver01.png` portrait → version paysage Gemini fournie par l'utilisateur = amorce directe, zéro dérive visuelle).
* **⛔ Wan 2.2 vid_gen (T2V/I2V, MoE ou LowNoise seul) INUTILISABLE sur build 6b3edaa + RX 6950 XT cette nuit** — 6 configurations testées, toutes mortes par `device fault: No fault detected` / `0xC0000409` : ① sans `--diffusion-fa` le graphe 81 trames réserve ~160 Go d'hôte ; ② avec FA, les **params wan_vae (4,2 Go, 471 tensors) se stagient sur Vulkan0 en ignorant `vae=cpu`, `--vae-on-cpu` et `--max-vram`** → OOM avec les poids DiT résidents ; ③ le MoE meurt en plus au basculement HighNoise→LowNoise ; ④ ni 49 trames, ni `--stream-layers`, ni le retrait des tilings ne sauvent. Le sampling Wan peut tourner (8/8 à 98-170 s/it observés) mais un module finit toujours par faire tomber le device. **✅ RETESTÉ sur master-864 (nuit 2026-09-13, cf. §1.19) : Wan 2.2 T2V RE-FONCTIONNE** — recette validée : LowNoise Q4_K_M seul, 832×480, **17 trames, 8 steps, `--diffusion-fa --temporal-tiling --vae-on-cpu`** → webm OK (sampling 264 s/it ~35 min + décode VAE CPU ~5 min ; sortie `output/test_m864/wan_t2v_m864_17f_vaeoncpu.webm`). **⚠️ Qualité visuelle NON jugée** : le smoke était en 8 steps (recette qualité = 20 steps, ~1 h 35) → ghosting/dédoublement attendu vu sur les trames (`output/test_m864/frames/`) — juger uniquement sur un render 20 steps si le besoin Wan revient (verdict utilisateur 2026-09-13 matin : « pas important »). Limites 16 Go : workspace DiT 17 trames = 941 Mo OK / **33 trames = 2,2 Go refus PROPRE** (nouveau gestionnaire, plus de crash) ; décode VAE GPU = 19,4 Go requis → `--vae-on-cpu` obligatoire ; **`--offload-to-cpu` = ErrorDeviceLost au 1er submit (bug streamé à ouvrir en issue amont**, idem avec `GGML_VK_DISABLE_HOST_VISIBLE_VIDMEM=1`). **Détail complet dans `C:\SD\README.md` (ligne Wan)** ; LTX-2.5 et H3 n'ont jamais eu le problème.
* **✅ Recette LTX-2.5 I2V (testée techniquement de bout en bout, validation utilisateur du rendu en attente)** : le chemin vid_gen de LTX tolère `-i <init image>` — même budget pixels que le canevas validé (832×480 ≈ 394k px ≈ 768×512), `--video-frames 65` sain (92 s/it). Commande : LTX DiT + video VAE + audio VAE + gemma4 `--llm`, sigmas Lightricks 8 steps, `euler_a`, `cfg 1.0`, `--diffusion-fa`, `--backend diffusion=vulkan0,te=cpu,vae=cpu`, seed explicite, `-n` négatif OK. **Natif 24 fps** (pas d'interpolation) + **piste audio générée** (ne PAS mixer entre plans chaînés : raccords sonores audibles — prendre un lit `sfx` dédié).
* **Chaînage 4 × 65 trames = 10,83 s → 10,0 s** (§1.8) : raccords invisibles vérifiés trame à trame (t=2,71 / 5,42 / 8,13 s), dolly continu, style painterly préservé. Timings mesurés : plan 1 = **32,7 min** (chargements à froid), plans suivants = **17,3 min** chacun (65 trames, 8 steps) ; conform 1080p AMF ≈ 1 s/plan.
* **Script** : `scripts/lancement_nuit_intro_vent_gris.py` (paramétrable, reprenant, logs par phase, rapport JSON — recette d'orchestration nocturne réutilisable ; prompts/frames en tête de fichier).
* **🔧 `scripts/upscale_video_ai.py` réparé au passage** : ffmpeg Amuse 7.1.1 disparu (migration MSYS2 09-09) → `C:\ffmpeg\dist\bin\ffmpeg.exe` prioritaire ; et **`-vsync` est retiré en ffmpeg 9.0** → `-fps_mode passthrough` (sinon échec silencieux de l'extraction de trames, stderr avalé par `check=True` + DEVNULL).
* **Livrables nuit** : `output/intro_vent_gris/` — `intro_vent_gris_10s_1080p.mp4` (1920×1080/24, 10,000 s, master canonique du jeu), `intro_vent_gris_10s_4k_logiciel.mp4` (4K lanczos+CAS AMF 50 Mbps, 5 s de production), plans bruts webm avec audio, `ambiance_tempete_vent_gris.wav/.ogg` (sfx SA3, contenu nommé « gusting wind… waves crashing », 0,29 min), MP4 d'écoute muxé.
* **⚠️ Leçon upscale IA 4K : sourcer depuis les plans 480p, JAMAIS depuis un master 1080p** — lancé sur le master 1080p, 4x-UltraSharp produit du 7680×4320/trame (5,4× le budget §1.6 calibré 480p) → **43 s/trame mesurées = ~3 h pour 240 trames** (job tué). La voie correcte : extraire les trames des `planN_brut.webm` 480p (~7,3 s/trame ≈ 30 min) puis conform 4K par plan avant concat. En attendant, le 4K logiciel lanczos+CAS+AMF est quasi instantané et suffisant pour un master de jeu.
* **🔍 Complément saccades (2026-09-10 après-midi, itérations v2-v6 avec l'utilisateur)** : les « saccades/reculs » du master chaîné venaient des COUPES de contenu (chaque plan réinterprète nuages/vagues/fenêtres) — ni fondus (v2 : ghosting « encore pire »), ni coupe des têtes-à-queue (v3), ni stabilisation traceur (v4 : le NCC injecte son bruit en translation — ne JAMAIS mettre de mesure brute dans un warp correctif) ne suffisent. **La solution = plan unique + ralenti** : v6 = 1×65 trames LTX ralenties ×3,69 (minterpolate 24 fps) + zoom pur conçu 1,10→1,32 (v5 validée) → 9,7 s sans AUCUNE coupe. **Plafond GPU LTX-2.5 ≈ 81 trames à 832×480** (module interne `ltxav` ~29 Mo/trame en VRAM, impossible à mettre sur CPU) : 121/241 trames = device lost. **Pilote AMD dégradé après device-lost en cascade** (65f marchait la nuit, échouait l'après-midi, marche à nouveau post-reboot) → sur séries de crash Vulkan, redémarrer la machine AVANT de diagnostiquer la recette. Script : `scripts/finir_monoplan_intro.py` (reprenant) ; livrables `output/intro_vent_gris/*v6*`.
* **✅ Carton de titre de fin VALIDÉ (2026-09-10 soir, verdict utilisateur « C'est parfait »)** : `intro_vent_gris_final_titre.mp4` = v6 48 fps (9,65 s) + 6 s de carton (dernière trame figée, raccord vérifié sans saut) + ambiance SA3 étendue jusqu'au bout (fondu sortie 2,5 s) + fondu au noir 1,5 s. **Recette « haute couture » Pillow** (exigence explicite : « le titre doit être TRÈS beau ») — multi-couches par glyphe : dégradé ivoire→or ancien via masque, biseau ciselé (calques décalés ±2 px), halo chaud flou, ombre portée bleu-noir, vignette de contraste radiale derrière le bloc ; **police Felix Titling** (`FELIXTI.TTF`, capitales inscriptionnelles style Trajan ; Centaur testé en QA, plus fin) ; animation = fondu smootherstep + vague de stagger 35 ms/glyphe + interlettrage 0,12→0,22 em + ornement filet-or-losange déployé depuis le centre (largeur calée sur la ligne 2, mesurée 1206 vs 1208 px). **QA visuelle itérée 3 tours** (analyses d'image + mesures pixel) : scores finaux 9/8,5/9/8,5/9 « qualité théâtrale » ; leçons : bloc à 45 % de hauteur (ciel calme, pas la maçonnerie), rythme vertical 0,42/0,52 em de part et d'autre de l'ornement. **Zoom du carton = poursuite de la rampe du plan** (valeurs absolues 1,32→1,36, warp relatif z(t)/z(0) — même math que zoom_pur, aucune vibration). 288 trames 1080p composées en ~50 s (plus rapide que le temps réel). Code générique dans `core/cinema.py` (`extraire_derniere_trame`, `rendre_titre_beau`, `construire_carton_titre`, `etendre_ambiance`, `assembler_finale`), intégré au workflow `monoplan_ia` (`--carton-titre "L1|L2" --carton-duree --carton-zoom-fin`, smoke testé) ; script de production `scripts/finir_carton_titre_vent_gris.py`. drawtext FFmpeg écarté (accents/apostrophe/interlettrage ingérables), Remotion écarté (framework React, hors philosophie du poste). Essais ratés de la nuit supprimés (~490 Mo, sources conservées : amorce, `monoplan_65f.webm`, masters v6).
* **✅ Chemin 4K UHD natif intégré au workflow `monoplan_ia` (`--4k` / `--upscale-ia`) — 2026-09-12 (demande ai-doc2video : Hero Hooks « 4K UHD Broadcast » 3840×2160 @ 30 fps, 45-50 Mbps)** : le goulot identifié à l'inspection 4K = chaque pixel 480p étiré ~34× en surface (480p → interp 1080p sans IA → zoom qui rogne → Lanczos 4K final) → détails durs (baies de serveurs, arêtes métalliques, néons) baveux. **Test utilisateur préalable VALIDÉ** : `4x-UltraSharp.pth` via `sd-cli -M upscale --backend vulkan0` sur une trame brute 832×480 = 6,87 s/trame (45 tuiles), 3328×1920, pixellisation disparue. **Chaîne intégrée** (ordre crucial : upscale IA des 65 trames BRUTES ~7-9 min, AVANT ralenti/zoom — l'inverse coûterait 3× plus de GPU sur 165+ trames et serait plus mou, cf. leçon « sourcer depuis les plans 480p » ci-dessus) : ① master brut UltraSharp 3328×1920 (`_brut_4k_ai.mp4`, chaîne `scripts/upscale_video_ai.py` réutilisée telle quelle, `cas_strength=0.0`) → ② ralenti minterpolate en 3328×1920 @ **30 fps** (cadence Hero Hooks) → ③ zoom pur en 3328×1920 (**CAS désactivé à cette étape** — un seul sharpening, à la conform, sinon double accentuation) → ④ `conformer_master_4k` : lanczos 3840×2160 + **CAS 0.75** + **h264_amf 45M** (+faststart). **Run de validation réel** (brut `hero_ltx_halloween`, 5,5 s cible) : upscale 522 s (moy. 8,0 s/trame — vs 6,87 s isolé utilisateur, écart = extraction concurrente), ralenti ×2,03 ~3 min, zoom ~1,5 min, conform <1 min ⇒ **~18 min bout en bout** ; master mesuré 3840×2160 / 30 fps / h264 / 47,9 Mbps / 5,33 s, **audit bande noire 0 px ✅**, A/B trame t=4,8 s vs master 1080p étiré : LEDs de baies lisibles en 4K, matière baveuse en 1080p. **Code** : `core/cinema.py` — `ralentir_interp` généralisée (`taille=None` = résolution source conservée ; alias `ralentir_interp_1080p` conservé pour les scripts de production), `zoom_pur(taille=, fps=)`, `master_brut_4k` (sonde la résolution source, cible 4×, résout le modèle via `resoudre_upscaler("4x-UltraSharp")`), `conformer_master_4k`, constantes `TAILLE_UPSCALE_4X/TAILLE_MASTER_4K/FPS_SORTIE_4K/BITRATE_MASTER_4K/CAS_MASTER_4K` ; `_BlocTitre` **multi-résolution** (param `largeur`, `facteur = largeur/1920` : vignette, interlettrage/em, traits d'ornement ×2 en 4K — facteur 1.0 = rendu 1080p validé inchangé ; `construire_carton_titre` lit maintenant les dimensions de la trame figée avant de construire le bloc) → le carton de titre marche en 4K (still QA `output/test_4k_monoplan/titre_beau_4k.png` magnifique ; ⚠️ la révélation complète dure 2,2 s → un carton de test de 2 s ne montre jamais le titre fini, garder le défaut 6 s). Reprise inchangée (chaque étape = un fichier : `_brut_4k_ai`, `_4k_ralenti`, `_4k_zoom`, `_4k`). Le master 4K force le profil VP09/AV01 haut débit de YouTube même en lecture 1080p (§4.10 README).

* **🚨 LTX-2.5 CASSÉ DEPUIS master-864 AUSSI (découvert le 2026-09-14 au test de l'idée « planche de poses → danse i2v ») — dernière release LTX fonctionnelle vérifiée = master-841/6b3edaa** — T2V 33 trames (benchmark script, sans init) échoue sur m864 avec LE MÊME refus calculé que m866 (`ltxav workspace capacity check`, need 910,16/budget 398,16 Mo — chiffres identiques entre builds = comportement du plan mémoire, pas de l'état machine ; VRAM 1,3/16 Go vérifiée) ; I2V 65 trames 832×480 échoue aussi sur m864 (`ErrorOutOfDeviceMemory` au submit ~115 s, graphe NON segmenté) et le portrait 480×832 (même budget pixels) meurt pareil (50 segments, submit OOM étape 5/8). **Tout le LTX validé §1.17 l'avait été sur 6b3edaa ; master-864 n'avait été validée QUE sur Flux + Wan T2V** (journal 13/09) — la casse LTX s'est produite dans la fenêtre de refonte mémoire **841→864** (#1940/#1956/#1957/#1958), pas 864→866. **⚠️ H3 sur m864 : CONFIRMÉ CASSÉ le 2026-09-14 nuit (test `scripts/statut_h3_m864.py` post-reboot utilisateur)** — signature identique à m866 : `minimax_h3 using 51 segments` (référence validée 6b3edaa = **2 segments**) puis `vk::Queue::submit: ErrorOutOfDeviceMemory` à 263 s de sampling ; log `output/test_h3_m864/h3_m864_20260914_235828.log`. La fenêtre de régression H3 = **841→864** (refonte mémoire), comme LTX.
* **✅ RE-VALIDATION 6b3edaa + installation parallèle de production (2026-09-15 matin)** — `C:\SD-6b3edaa\` créé depuis `C:\SD\backups\backup_2026-09-13_m864/` (binaires 6b3edaa, `--version` vérifié) ; le test H3 turbo EXACT (même prompt/seed/LoRA/assets que la recette §1.16, via le binaire parallèle) **PASSE intégralement** : graphe **2 segments × 8 steps** (`segment 1/2: blocks.0..37` + `segment 2/2`), webm 864×480 22 trames + PCM 32 kHz stéréo en 2279 s (timing historique) — `output/test_h3_6b3edaa/h3_turbo_6b3edaa.webm`. **A/B sur machine identique à 24 h d'écart : 2 segments OK (6b3edaa) vs 51 segments OOM (m864/m866) → cause 100 % build, dans la refonte mémoire 841→864.** ⚠️ Le workflow `h3_ref2va` appelle `C:\SD\sd-cli.exe` en dur — pour produire sur le build parallèle, utiliser la commande brute (ci-dessus, `output/test_h3_6b3edaa/h3_turbo_6b3edaa.log`) ou ajouter une option de binaire au workflow. Le plafond « 81 trames LTX » ci-dessus = mesures 6b3edaa, à reprendre après fix amont. En attendant : **seule vidéo fiable sur m864 = Wan T2V/I2V ≤ ~20 trames `--vae-on-cpu`** (recette validée ci-dessus).

* **⛔ master-866-42d6c0a : RÉGRESSION Wan 2.2 T2V — rejetée, rollback master-864 (2026-09-14)** — smoke Flux OK mais la recette exacte §1.17 ÉCHOUE sur 866 : poids DiT pourtant résidents identiques (9211,74 Mo/1095 tensors), le graphe est découpé en **42 segments** (864 : 1 segment, compute 941 Mo) et la génération meurt à l'**étape 5/8** sur `segment 24/42 (wan.blocks.22) failed during weight preparation` (need 664,34 Mo / available 631,30 Mo — `model_manager.cpp:1767`). Suspect n° 1 : #1967 « preserve explicit backend assignments during auto-fit » (seul commit mémoire de la gamme 864→866). **Rester sur master-864** ; toute release postérieure à 866 passera le double test (Flux + Wan 17 trames). Logs `output/test_m866/`, journal 2026-09-14. La nouveauté S2V de 866 (lipsync portrait+WAV via `--audio-encoder`/`--audio`) attendra un build corrigé — sur 24 Go elle tourne en Q4_K_M à 13,4 Go VRAM, sur nos 16 Go ce sera juste. → **Suite le soir même : contournement `--auto-fit off` validé (voir ci-dessous).**

* **⛔ master-866-42d6c0a : régression Vidéo COMPLÈTE confirmée — rejetée définitivement, master-864 épinglée (2026-09-14)** — matrice de validation complète du soir : **image OK** (Flux schnell ✅, SDXL ✅, upscale 4× ✅) mais **Wan 2.2 T2V ❌** (42 segments, échec weight prep segment 24/42 à l'étape 5/8 — need 664/available 631 Mo ; **contourné par `--auto-fit off` ✅**, webm 2399 s, divergence numérique 14,8/255 attendue), **LTX-2.5 ❌ avec ET sans `--auto-fit off`** (`ltxav workspace capacity check` : need 910/available 319 Mo, mort à 20 s), **MiniMax H3 Ref2VA turbo ❌** (`using 51 segments` vs graph-cut 2 segments en 864, puis `vk::Queue::submit: ErrorOutOfDeviceMemory` à 359 s). **Suspect : #1967 « preserve explicit backend assignments during auto-fit »** (44dd137) — le nouveau découpage en segments et les budgets de workspace cassent les 3 moteurs vidéo sur 16 Go. **Rester sur master-864 ; toute release postérieure à 866 passera la matrice complète (Flux + Wan 17 trames + LTX 33 + H3 turbo) avant adoption.** **Issue amont postée le 14/09 soir (accord utilisateur) : [leejet/stable-diffusion.cpp#1976](https://github.com/leejet/stable-diffusion.cpp/issues/1976)** (4 datapoints, texte EN propre). Logs `output/test_m866/`, journal 2026-09-14. La nouveauté S2V de 866 (lipsync portrait+WAV) attendra un build corrigé.

* **✅ master-908-88411ef installée et gardée en pin (2026-09-24, accord utilisateur implicite « reboot ok » pour la matrice convenue) — H3 TOUJOURS cassé malgré le fix #1900 attendu (54 segments), les 3 autres jambes stables ou meilleures** — 9 commits depuis 899 dont **#1900 « fix: add graph cuts for MiniMax-H3 text conditioning »** (1re intervention amont sur le plan de graphe H3 depuis la régression #1976), #2034 conditioning cache configurable tous modèles, #2038 perf Wan VAE conv 2D, #2029 fix alpha upscale ESRGAN RGBA, Qwen Image 2.1 (#2032/#2035). Matrice post-reboot (bureau 0,4 Go, conditions identiques à 899 la veille) : **Flux 1024² 4 steps ✅ 27,00 s** (= 899) • **LTX 33 trames ✅ 54,21 s** (=, +0,9 %, 1 segment, aucun refus capacity check, même WARN pinned non fatal) • **Wan 17 trames ✅ 1999,5 s** (−1,2 % vs 899, 1 segment résident 9211,74 Mo, décode VAE CPU 209,5 s) • **H3 turbo ❌ — `minimax_h3 using 54 segments` (sain : 2) + `ErrorOutOfDeviceMemory` au submit à 257,37 s** : #1900 a bien MODIFIÉ le découpage H3 (51→54 segments) mais ne restaure pas le plan sain 2 segments, la régression #1976 reste ouverte (issue toujours sans réponse amont). **Décision : 908 gardée** (aucune jambe régressée ; gains tangibles : conditioning cache #2034, fix upscale RGBA #2029 ; rollback `C:\SD\backups\backup_20260924_master908/`). **Production LTX/H3 INCHANGÉE via `C:\SD-6b3edaa`**. Logs `output/test_m908/` (Flux/LTX/Wan/H3 + webm). **Datapoint 54-segments posté en commentaire de #1976 le jour même (accord utilisateur) : [issuecomment-5813245104](https://github.com/leejet/stable-diffusion.cpp/issues/1976#issuecomment-5813245104)** — preuve fournie à l'amont que #1900 modifie le découpage H3 (51→54) sans restaurer le plan sain 2 segments ; source `output/test_m908/issue1976_comment_908.md`.
* **✅ master-899-28b454b installée et gardée en pin (2026-09-23, accord utilisateur « Oui go ») — matrice complète : H3 TOUJOURS cassé (signature identique), LTX passe sur machine propre, Wan −4 %** — 9 commits dont **#2020 « fix: handle GPU memory reports and LLM encoding failures »** (le fix de l'issue #1989 attendu depuis le relevé du 22/09). Résultats du 23/09 : ① **pré-reboot** (bureau 0,8 Go VRAM) : Flux ✅, **LTX 33 trames ❌ à 21,5 s** — `ltxav workspace capacity check` need 908,75 Mo / **available 787,26 Mo = il ne manquait que 121 Mo** ; ② **post-reboot** (bureau 0,4 Go) : **LTX 33 trames ✅** (53,75 s, webm OK, 1 WARN non fatal pinned memory), **Wan 17 trames ✅** (2023 s vs 2116 s sur 864 = −4,4 %), **H3 turbo ❌** — `minimax_h3 using 51 segments` (sain : 2) + `ErrorOutOfDeviceMemory` au submit à 276,68 s, **signature identique à 864/866 : le fix #2020 n'améliore que le reporting mémoire (fini les « available 0.00 MB »), le plan de graphe H3 est inchangé**. ③ **Décision : 899 gardée** (≥ 864 sur toutes les jambes mesurées, aucun chemin production régressé ; rollback `C:\SD\backups\backup_20260923_master899/`). ④ **Conséquences opérationnelles** : **production LTX/H3 inchangée via `C:\SD-6b3edaa`** — le passage LTX sur 899 dépend d'une marge VRAM de ~120 Mo (fragile : variable selon le bureau) et l'I2V 65 trames d'`monoplan_ia` n'a pas été retesté sur 899 (seul le benchmark T2V 33 trames a passé la matrice) ; **la régression H3 (issue #1976) reste ouverte de 864 à 899**. ⑤ **Leçon confirmée** : le verdict LTX dépend de l'état VRAM de la machine — **toute matrice sd-cli doit tourner sur machine fraîchement rebootée** (règle §1.17 déjà écrite pour les crashs Vulkan, valable aussi pour les capacity checks) ; les « chiffres identiques entre builds » du 14/09 étaient bien un artefact d'état machine pour LTX (marge), mais un vrai comportement de build pour H3 (51 segments déterministes). Logs `output/test_m899/`.

### 🖼️ 1.18. Boucle de fond de menu (cinemagraph IA) + incident bande noire — 2026-09-10 soir

* **Recette boucle infinie VALIDÉE EN JEU** (`menu_vent_gris_boucle_fix.mp4/.ogv`, 8,33 s, silencieux — la musique du menu boucle à part dans Godot) : master propre (`menu_vent_gris_10s_1080p.mp4`, ralenti ×3,75 du brut LTX 65 trames) → **xfade queue→tête, fondu 1,5 s**. **Pièges xfade** : fps forcé `fps=24` sur les DEUX branches avant xfade (sinon frame variable rejeté) ; `trim=X` fixe le DÉBUT pas la fin → longueur finale = `trim=end=…` ; `offset = durée_post − fondu` (9,83 s → offset 6,83) ; trim final de propreté. Script : `scripts/faire_boucle_menu_vent_gris.py` (reprenant, sorties suffixées `_fix`, pièces à conviction conservées).
* **⛔ INCIDENT bande noire montante (diagnostiqué en jeu, trame par trame)** : l'ancienne chaîne passait par une stabilisation maison (`stabiliser_camera_fixe`, ORB 6 ancrages + warp conçu) dont le warp échantillonnait HORS du cadre source en fin de dérive LTX (push-in 7,6 % + translation, ~236 px mesurés) — `cv2.warpAffine` remplit ces pixels en **NOIR constant** (borderMode par défaut), et le recadrage de sécurité ζ était plafonné trop bas (1,18 vs 1,78 nécessaire) → bande noire grandissante (35→145 px), que le crossfade figeait ensuite dans la boucle. **Leçons** : ① tout warp qui peut sortir du cadre ⇒ `borderMode=BORDER_REPLICATE` systématique ; ② un crop de sécurité doit COUVRIR la dérive mesurée, jamais être plafonné en silence (avertir + refuse) ; ③ **contenu quasi fixe = PAS de stabilisation** : le fondu croisé absorbe la dérive douce, l'étage est retiré de la chaîne active (fonction conservée corrigée, preuve d'audit `stabilise_fix` : 0 px noir mais bords répliqués visibles en fin de dérive → inutilisable sur ce clip, à réserver à des dérives < plafond de recadrage) ; ④ LTX-2.5 impose toujours son push-in même avec prompt « locked camera » (cfg 1.0 = négatif ignoré).
* **🔍 `scripts/audit_bande_noire.py` — CRITÈRE DE SORTIE obligatoire de toute vidéo du pipeline** : mesure la hauteur du voile noir bas (lignes de luminosité moyenne < 8/255 en remontant depuis le bas) à 0,5/2/3,5/5/6,5/8 s — exigé **0 px** — et, pour les boucles (`--raccord`), diff L moyenne trame 0 vs dernière trame **≤ 6**. `--zero vid1 vid2 --raccord boucle.mp4` ; exit 1 si non conforme. Le pipeline `faire_boucle_menu_vent_gris.py` l'appelle en dernière étape et échoue si non conforme.


### 🔧 1.19. Test du correctif candidat PR #1958 (issue #1946) : crash éliminé, régression PAS corrigée sur 16 Go — 2026-09-10 soir

* **Contexte** : leejet a répondu sur l'issue #1946 (2026-09-10 17:08 UTC) qu'il ne reproduit pas le bug et demande de tester son **PR #1958** `58d3f6e` (« fix: guard GPU memory capacity and propagate encoding failures », 5 fichiers, branche `fix/gpu-memory-failure-handling`, basée sur le refactor #1957).
* **Méthode** : build local MSYS2/UCRT64 gcc 16.2 (même recette bisect §`C:\SD\README.md` : `-DGGML_VULKAN=ON` Ninja Release, **pin ggml `e20c3a14` identique au bisect** — conditions comparables), clone `scratch/bisect_1946/sd-cpp-src/` checkouté sur le head PR, repro Flux schnell Q6_K 512×512 4 steps euler `--vae-tiling --backend vulkan` (`test_bisect.sh`). Charge vérifiée avant chaque run (`check_charge_systeme.py`).
* **✅ Partie « propagate failures » du PR : fonctionne** — plus de `terminate()` / `vk::Queue::submit: ErrorOutOfDeviceMemory` : l'échec est intercepté et remonté proprement jusqu'au CLI avec diagnostic précis.
* **❌ La génération échoue toujours, de façon DÉTERMINISTE** : au segment 30/59 (`flux.single_blocks.9`) en plein sampling, `model_manager.cpp:1762` signale « cannot make enough memory available on Vulkan0: **need 954.20 MB device / 457.22 MB budget, available 906.93 MB device / unlimited budget** » → manque de ~47 Mo, chiffres **strictement identiques** pré-reboot (0,8/16 Go VRAM avant lancement) et post-reboot (0,5/16 Go) → PAS un problème de fragmentation : le gestionnaire ne peut plus évincer assez de poids DiT résidents pour préparer le segment suivant.
* **❌ `--offload-to-cpu` : 2e bug distinct** — tous les poids partent bien en RAM (18 858 Mo RAM / 0 VRAM), chargement complet, puis mort silencieuse **exit 127 non interceptée** sur `vk::Device::createComputePipeline: ErrorOutOfDeviceMemory` puis `vk::Queue::submit` avec **~15,5 Go de VRAM libre** → OOM incohérent sur la création de pipeline compute, probablement encore lié au refactor #1940 sur ce chemin.
* **Statut (10/09)** : PR #1958 « testé, non suffisant » — ni workflow ni changement d'installation ; **rollback master-841 maintenu**, hook SessionStart issue #1946 conservé. Données transmises à leejet via commentaire (brouillon : `scratch/bisect_1946/commentaire_issue_1958_BROUILLON.md`, logs `scratch/bisect_1946/pr1958_*`).
* **✅ RÉSOLU sur le head `1bf76e6` (2026-09-13, re-test) : les DEUX chemins qui échouaient passent** — leejet a répondu sur l'issue (13/09 09:43 UTC) et poussé 3 commits : **blocs de résidence réduits** (`da4e841` 256 Mio → `5c7fc62` **1 Gio**) + gestion erreurs cleanup backend + 2 merges master. Re-test mêmes conditions (repro `test_bisect.sh`, ggml `e20c3a14` inchangé) : ① **chemin plain = corrigé** (complète en 13,1 s, exit 0, image valide — vs échec déterministe segment 30/59 sur `58d3f6e`) ; ② **`--offload-to-cpu` = corrigé aussi** (commande suggérée par leejet : exit 0, image en 17,4 s — vs mort silencieuse exit 127 sur `58d3f6e`). Contrôle : même repro sur le binaire installé `6b3edaa` = image visuellement identique (diff moyenne 1,6/255 — dérive numérique attendue des refactors master fusionnés). Observation bonus : VRAM pressée par un process zombie (~2-3 Go) → le VIEUX `6b3edaa` OOM à `flux prepare` (332 Mo) là où le PR venait de réussir → le nouveau gestionnaire de résidence est aussi plus robuste sous pression VRAM. Build scratch : `scratch/bisect_1946/sd-cpp-src` (branche `pr1958`, ~10 min). Logs/PNG : `scratch/bisect_1946/pr1958_head2_test.{png,log}`, `pr1958_offload_head2.{png,log}`, `baseline_6b3edaa_fox.png` ; commentaire EN **POSTÉ le 13/09 avec les 2 images du repro + baseline inline** (PNGs+logs commités dans `scratch/bisect_1946/` pour URLs raw) : [issue #1946, commentaire du 13/09](https://github.com/leejet/stable-diffusion.cpp/issues/1946#issuecomment-5653035420) ; source : `commentaire_pr1958_head2_FINAL.md`. **SUITE (13/09 13:35 UTC) : PR #1958 MERGÉ** (`9a97738` — leejet après notre commentaire avec images : « Thanks for testing. Since all the issues have been fixed, I'll merge the corresponding PR »). **⚠️ master-860-44dd137 (12:33 UTC) est ANTÉRIEURE au merge et NE CONTIENT PAS le fix** (API compare : 1 commit d'écart) → à sauter malgré la règle « postérieure à 859 » ; **→ FAIT le 2026-09-13 soir : master-864-ca37fad installée (accord « Go »)** — asset officiel 39,1 Mo, backup `backups/backup_2026-09-13_m864/`, smoke Flux repro = **PASSÉ sur l'officiel** (exit 0, 23,7 s). **Hook issue #1946 retiré** (fonction `bloc_issue_sdcli` + `output/veille/issue_sdcli_1946.json` + mentions AGENTS.md/README C:\SD) ; manifest `engines_manifest.json` épinglé master-864-ca37fad. Wan 2.2 T2V sur master-864 : refus PROPRE à 33 trames (workspace 2,2 Go > 1,0 Go dispo), **sampling 8/8 OK à 17 trames résident (264 s/it, ~35 min — 1re fois depuis master-841)**, décode VAE impossible en résident (19,4 Go requis) → `--vae-on-cpu` en test ; chemin `--offload-to-cpu` = ErrorDeviceLost au 1er submit (nouveau bug Wan streamé, issue à ouvrir — logs `output/test_m864/`). Autres utilisateurs touchés par la même fenêtre (Minimax H3 crash post-841) débloqués par ce merge.
* **Leçon build/test candidat-fix** : la chaîne « fetch PR head → vérifier pin ggml → build incrémental Ninja → `test_bisect.sh` → logs archivés » est reproductible en ~15 min (build ~10 min dont 2 en reprise, run ~4 min) ; relancer la même commande exacte du rapport initial est ce qui rend le résultat directement exploitable par le mainteneur.

### 🎙️ 1.20. qwentts.cpp — moteur TTS dédié Qwen3-TTS (clone géré `C:\IA\qwentts.cpp`) — casting du jeu JUGÉ le 2026-09-14 : 5 voix validées, 1 avec réserve, 2 rejetées (accents EN), 4 speakers non écoutés

* **Quoi** : port C++17/GGML de **Qwen3-TTS 12 Hz** par ServeurpersoCom (`github.com/ServeurpersoCom/qwentts.cpp`) — le MÊME modèle que la famille `qwen3_tts` validée via audio.cpp (§1.13), mais en moteur dédié avec des capacités en plus : **speakers nommés CustomVoice** (le point « qualité 1.7B CustomVoice : non testé » de §1.13), **voice design** par instruction texte, clonage zero-shot (x-vector seul ou in-context avec transcript), **streaming** frame-by-frame, et **`tts-server`** (serveur HTTP compatible OpenAI avec registre de voix clonées). 24 kHz mono, 11 langues (dont dialectes mandarins), quantisation Q8_0/Q4_K_M du talker (0.6B/1.7B), RVQ codec en F32.
* **Installation locale** : clone `C:\IA\qwentts.cpp` (**commit `90efdc6` depuis le 2026-09-16** — maj « ggml fork update » (sous-module ggml → 6431c6a2 : 2 fix Vulkan in-place/batch stride + fix conv_1d batché), build+smoke 6,07 s OK via `manage_qwentts.py --update` ; précédents : `71ad93d` 14/09, `779c7cb` 12/09), build Release **Vulkan** (`GGML_VULKAN=ON`) → `build\Release\qwen-tts.exe` + `quantize.exe` (le build actuel ne contient ni `qwen-codec` ni `tts-server` — à produire au besoin via buildvulkan). Modèles déjà en place dans `C:\IA\qwentts.cpp\models\` : `qwen-talker-1.7b-{base,customvoice,voicedesign}-Q8_0.gguf` + `qwen-tokenizer-12hz-Q8_0.gguf` ; la base existe aussi en doublon dans `C:\Modeles_LLM\Qwen3-TTS-12Hz-1.7B-Base-GGUF\` (chemin audio.cpp §1.13). GGUF amont : `huggingface.co/Serveurperso/Qwen3-TTS-GGUF`. Usage CLI (3 modes, texte sur stdin, WAV 24 kHz mono) documenté dans l'AGENTS.md d'`ai-doc2video` (commit `2f8c8ab`).
* **🔄 Gestion RÉCUPÉRÉE du projet `C:\GIT\ai-doc2video` (2026-09-12 soir, décision utilisateur — va-et-vient du jour)** : maj + modèles pilotés depuis CE dépôt via `uv run python scripts/manage_qwentts.py --check|--models|--update|--backup|--rollback` (maj git sécurisée, sauvegardes `C:\IA\qwentts_backups` (5 conservées), build Vulkan, smoke test, rollback auto ; catalogue modèles + vérification intégrité GGUF/taille/SHA256 vs HF `Serveurperso/Qwen3-TTS-GGUF` avec `--models --verify-hash`). Historique du jour : gestion branchée ici le matin (veille commits amont), transférée à `ai-doc2video` l'après-midi (qui a appliqué la maj `779c7cb` + recompile 16:39 via `factory.py qwentts-update`), puis **récupérée par ce dépôt le soir même** — script recopié/adapté (`scripts/manage_qwentts.py`), source de veille `qwentts.cpp` réintégrée à `veille_versions.py` (commits amont vs HEAD local), commandes `factory.py qwentts-*` supprimées d'`ai-doc2video` (qui n'utilise plus le moteur qu'en lecture via `generate_voices.py`). Le clone n'est jamais modifié à la main ; notes conservées ici car notre stack voix off (§1.13, workflow `voix_off`) peut l'interroger comme moteur alternatif. ⚠️ **Chemin réel du binaire : `C:\IA\qwentts.cpp\build\Release\qwen-tts.exe`** (pas de dossier `bin\` — ancienne erreur doc corrigée chez ai-doc2video).
* **Premiers tests techniques RÉUSSIS (2026-09-12 soir, casting du jeu « L'Héritier du Vide »)** : 9/9 speakers nommés en FR (serena, vivian, uncle_fu, ryan, aiden, ono_anna, sohee, eric, dylan — phrase du jeu sur stdin, `--lang French`, customvoice Q8_0, ~5-7 s par synthèse sur Vulkan) + **3/3 voix designed** (`--instruct` en anglais sur le talker voicedesign : « elderly male, deep gravelly », « young female, bright clear », « middle-aged male, stern commanding ») → **la piste « plein de voix FR » du jeu** : 9 voix immédiates + voix illimitées par instruction. Fichiers : `output/casting_voix_jeu/` (qwentts_<speaker>.wav, design_<perso>.wav).
* **✅ Verdict casting (2026-09-14, écoute des 12 fichiers — verdict utilisateur verbatim)** : **voix designed** : `design_ancien_gardien` (« elderly male, deep gravelly ») **bien mais trop lent** — retenu avec réserve (piste : accélérer au montage `atempo`, ou assumer le débit lent comme trait du personnage) ; `design_heraut_jeune` (« young female, bright clear ») « drôle de voix mais pourquoi pas, aigu marrant » — **retenu** (ton comique/jeune héraut) ; `design_capitaine_sterne` (« middle-aged male, stern commanding ») « pas top, anglais qui parle français » — **rejeté**. **Speakers nommés** : dylan, eric, ono_anna **bien** (validés) ; sohee « bien mais un peu anglais » (retenu, accent léger à surveiller sur textes longs) ; aiden « fort accent anglais » (rejeté pour le FR) ; **non écoutés : serena, vivian, uncle_fu, ryan** (verdict manquant). **Bilan piste « plein de voix FR » : VALIDÉE dans son principe** (plusieurs voix FR exploitables dès maintenant + design à l'instruction marche — 1 réussite franche sur 3, l'accent EN sur les instructs anglophones reste le risque principal). **Workflow de production de dialogues à créer au moment du besoin réel** (batch de répliques du jeu), pas avant — le casting s'affinera alors (voix restantes + réglages).
* **Prochaines étapes candidates** (ni lancées ni validées — aucun workflow tant que non validé, règle générale) : smoke test 1 synthèse speaker nommé ; A/B avec le chemin audio.cpp §1.13 ; test clonage FR avec la référence §1.13 (`output/comparatif_tts/ref_voix_laurent.wav`, normalisée −18 LUFS) ; évaluer streaming/serveur pour la production voix off de la chaîne.
* **Signal qualité (2026-09-12)** : Qwen3-TTS-GGUF classé **🥇 1er en TTFA (time-to-first-audio)** de l'[Open TTS Leaderboard](https://huggingface.co/spaces/hf-audio/open_tts_leaderboard) HF au 2026-09-09 (benchmark streaming, mono-requête) — le plus rapide à démarrer la production audio ; réutilisation license : upstream + codec Apache-2.0, qwentts.cpp MIT. Écosystème du même auteur à surveiller : **yue2.cpp** (créé le 2026-09-12, moteur dédié musique Yue2 — instruction permanente musique s'appliquera dès qu'un GGUF testable sort), omnivoice.cpp (TTS 600+ langues), acestep.cpp (ACE-Step 1.5 standalone, ⭐414).



### 🔊 1.21. Kokoro-82M TTS (GGUF org audio-cpp, famille `kokoro_tts`) — verdicts confirmés le 2026-09-14 : EN validé (dépannage), FR écarté — intégré à la release v0.7.4

* **Quoi** : TTS ultra-léger **82 M params** (hexgrad/Kokoro-82M v1.0) — **multilingue 9 langues dont `fr-fr`**, 54 voix preset **intégrées dans le GGUF** (voix + config + vocabulaire + tables g2p ja/zh ; phonémisation via eSpeak-ng). GGUF publiés par l'org dans `audio.cpp-gguf/Kokoro-82M-GGUF/` le 2026-09-12 (`kokoro-82m-q8_0.gguf` 189,6 Mo, `kokoro-82m-bf16.gguf` 212 Mo — téléchargés et validés GGUF v3 dans `C:\Modeles_LLM\Kokoro-82M-GGUF\`).
* **Moteur : master uniquement** — support `kokoro_tts` fusionné le 2026-09-11 (PR #496 « multilingual GGUF synthesis » + refactoring #515) sur master d'audio.cpp (commit `5bea9c7`), **PAS dans la release installée v0.7.3**. Phonémisation : build avec `AUDIOCPP_STATIC_ESPEAK=ON` → `espeak-ng-data.bin` (18,4 Mo) généré automatiquement à côté du binaire.
* **Test (2026-09-12 soir, machine libre vérifiée)** : build master **en scratch** `C:\IA\audio_cpp_master_test` (2,8 Go — **SUPPRIMABLE**, ce n'est PAS une maj de `C:\audio-cpp` qui reste en v0.7.3 ; cmake+MSVC+Vulkan SDK 1.4.304.1 puis **1.4.357.0** (`build-357`, maj SDK du 2026-09-12 soir), `GGML_VULKAN=ON`). CLI : `--task tts --family kokoro_tts --model <gguf> --backend vulkan --language fr-fr --voice-id ff_siwis --text "…" --out out.wav --metrics`.
* **Perfs mesurées (RX 6950 XT, 24 kHz mono)** : 12 s d'audio en **3,09 s** au 1er run (RTF 0,258 — compilation des pipelines Vulkan incluse, cf. ~4 s one-shot doc PR) puis **RTF ~0,11 en régime établi (~9× temps réel**, A/B propre machine libre : 0,109 avec SDK 1.4.357.0 vs 0,108 avec 1.4.304.1 — parité**) — fr-fr q8_0, fr-fr bf16 et en-us q8_0 tous conformes ; niveaux sains (mean −23/−26 dB). **~50× plus rapide que le RTF des pipelines TTS lourds** du poste ; candidat crédible voix off/ previews à très faible coût.
* **Fichiers d'écoute** : `output/test_kokoro/` — même phrase voix off sysadmin en FR (`ff_siwis` q8_0 vs bf16) + EN (`af_heart` q8_0). Verdict utilisateur en attente → statut « testé, non validé », **aucun workflow tant que non validé** (règle générale).
* **Verdict utilisateur (2026-09-12 soir)** : « Kokoro c'est pas mal niveau anglais, je préfère Qwen TTS mais FR » → **écarté pour le FR du jeu** ; gardé en candidat voix **EN** (épisodes/usages anglophones éventuels). **Re-confirmé le 2026-09-14** à la ré-écoute : `af_heart` EN « **OK** » (validé usage EN), `ff_siwis` FR « **moyen** » (FR confirmé écarté). Statut final : **validé usage EN limité (dépannage/voix de fond), pas de workflow tant qu'aucun besoin EN réel** — qwentts (§1.20) reste le TTS principal. Condition « release > v0.7.3 intégrant kokoro_tts » REMPLIE depuis le 2026-09-14 (v0.7.4 installée) — un workflow ne demanderait plus que la recette CLI si le besoin apparaît.
* **⚠️ Limitation découverte : cross-language VERROUILLÉ par le moteur** — audio.cpp impose voix ↔ langue natale (`kokoro_tts/frontend.cpp:93-97`, « voice X requires lang_code=a »), aucun contournement : **le FR se limite à ff_siwis (1 seule voix)**. L'espoir « 54 voix parlant français » est donc nul côté Kokoro ; c'est qwentts.cpp (9 speakers + voice design illimité, §1.20) qui répond au besoin de casting FR du jeu.
* **Si validé** : attendre une release audio.cpp > v0.7.3 intégrant `kokoro_tts` (le build scratch n'est pas pérenne) → maj via `C:\audio-cpp\update.ps1` puis workflow à créer. Limites connues : qualités vocales « preset » (pas de clonage), eSpeak obligatoire au runtime (embarqué au build statique), japonais nécessite MeCab/UniDic en plus.
* **⚠️ Écueil release (2026-09-17) : le zip release v0.8.0 ne peut PAS lancer `kokoro_tts` tel quel** — `audiocpp_cli.exe` release échoue « Could not load eSpeak-ng; install the shared library or provide its path » : le zip n'embarque NI la DLL espeak-ng partagée NI le pack statique (`espeak-ng-data.bin` ; copier ce dernier à côté du binaire release ne sert à rien, il n'est lu que par un build `AUDIOCPP_STATIC_ESPEAK=ON`). Solution actuelle : passer par le binaire scratch `C:\IA\audio_cpp_master_test\build-357\bin\Release\audiocpp_cli.exe` (espeak embarqué). Solution pérenne à envisager si usage kokoro fréquent : installer espeak-ng partagé (MSYS2 `mingw-w64-ucrt-x86_64-espeak-ng` ou DLL officielle) avec son dossier `espeak-ng-data`.

### 🎵 1.22. YuE2-3B — lyrics-to-song GGUF (chant + instru complets) — testé le 2026-09-13, verdict utilisateur : « encore du POP » → rejeté cible gothique — support branche `dev` audio.cpp uniquement

* **Quoi** : **YuE2-3B** (base `m-a-p/YuE2-3B`, **licence CC BY-NC 4.0** ⚠️ — frein chaîne YouTube monétisée, vs ACE-Step MIT) — **lyrics-to-song** : génère chanson COMPLÈTE (voix chantée + accompagnement) depuis paroles balisées (`[Verse]/[Chorus]…`) + prompt style. Pipeline : planification ABC symbolique (cot) → tokens sémantiques (AR) → synthèse acoustique NAR (flow ODE midpoint) → VAE Oobleck. Langue : **en uniquement**. C'était la « piste suivante anti-pop » de §1.11 — le runtime existe désormais.
* **Moteur : branche `dev` d'audio.cpp UNIQUEMENT** (famille `yue2`, **PAS dans v0.7.4** ni aucune release au 2026-09-13). Test via le scratch `C:\IA\audio_cpp_master_test` **basculé `main`→`dev`** (commit `cda0e3a` du 13/09 — la CI release dev du jour est en échec côté amont mais le build local passe) : rebuild incrémental `build-357` Vulkan OK (~8 min). ⚠️ le scratch n'est plus sur master (kokoro §1.21) — rebasculer selon le besoin.
* **Modèle** : `audio-cpp/Yue2-3B-GGUF` (org, créé le 10/09, README détaillé avec perfs officielles) → `C:\Modeles_LLM\Yue2-3B-GGUF\` : `yue2-3b-q8_0.gguf` **4,3 Go** + `yue2-vae-f16.gguf` 265 Mo + 4 sidecars (`sidecars/` : model-config, generation-config, tiktoken Qwen, vae-config) — défauts auto-détectés, `--model` pointe le DOSSIER (contrairement à ace_step qui veut le .gguf lui-même, §1.11). bf16 (~7,3 Go) et q4_0 dispo côté HF.
* **Test (2026-09-13, machine libre vérifiée, recette de référence gothic rock adaptée)** : `--task gen --family yue2 --backend vulkan --threads 8 --lyrics "[Verse]…[Chorus]…" --request-option 'style=English, German gothic rock 1990, dark wave, hypnotic tribal groove, deep pulsing bass, chiming chorus guitars, 83 BPM, C# minor' --request-option cot=off --request-option seed=42`. **Pas d'options bpm/keyscale** (ace_step only) → imposés via le texte du style ; graine 42 conservée. Paroles EN courtes à structure balisée (le modèle chante, pas d'instrumental pur).
* **Résultats (32 steps, défaut)** : clip 2 lignes → **47,2 s d'audio valides** (48 kHz, RMS −16,6 dB, crête −1,1 dB — sain, pas de silence) en ~9 min wall. Décomposition : AR préfill 4120 tokens = 28 s froid (compil shaders) / **0,95 s chaud** ; **NAR = goulot absolu** : 64 passes DiT (32 steps ODE × 2) sur 1180 frames = **452 s** ; VAE décode 1,5 s. **RTF ~9,6 Vulkan** — vs RTF 0,2 officiel RTX 5090 CUDA (~50× : code dev non optimisé RDNA2).
* **✅ Levier vitesse mesuré : `num_inference_steps=8` (réglage des démos officielles) = RTF /3,5** — même prompt/graine : l'AR a généré une **chanson complète de 2:53** (4401 tokens, clip beaucoup plus long — cf. quirk ci-dessous), NAR 16 passes (8×2) sur 4325 frames = **363 s**, wall total ~8,2 min → **RTF ~2,8 wall** (NAR seul 2,1). Niveaux : mean −16,0 dB, crête −0,0 dB (chaud — limiteur conseillé en production). **C'EST le réglage par défaut à retenir** ; ⚠️ le coût NAR croît ~quadratiquement avec les frames (clip 5655 frames à 32 steps arrêté à ~50 min, non fini — les grands clips DOIVENT passer en 8 steps, voire moins).
* **⚠️ Quirk : la graine ne rend PAS l'AR reproductible** — mêmes paroles/style/graine 42 : run 32 steps → 1246 tokens AR (47 s de clip), run 8 steps → 4401 tokens (2:53) : **le seed ne commande que le bruit NAR** (`nar_runtime.cpp` : `std::mt19937(seed)` sur le noise) ; l'échantillonnage sémantique AR n'est pas ancré → la durée/composition varie d'un run à l'autre à réglages égaux.
* **⚠️ Écueil bloquant (certains clips longs)** : crash **`vk::Queue::submit: ErrorDeviceLost`** au 1er submit du graphe NAR sur le prompt 6 lignes (**4021 frames**) — reproductible ×3 ; **PAS corrigé par** `--session-option yue2.nar_graph_arena_mb=10240` ni `GGML_VK_DISABLE_COOPMAT=1` ; les clips 2 lignes (1180 frames), 4 lignes (**5655 frames**, submit OK) **et 4401 tokens (4325 frames)** passent → **bug NON monotone en taille, dépend du contenu du prompt** (forme du graphe NAR générée) — pas un plafond de buffers. Issue amont #535 similaire côté Intel Arc (assert alignement, AR decode). Rapport upstream à envisager avec ces éléments (sur accord utilisateur — action externe).
* **Fichiers d'écoute** : `output/test_yue2/` — **`gothic_short_seed42_8steps.wav` (2:53, chanson complète — le livrable principal)** + `gothic_short_seed42.wav` (47 s, 32 steps, même graine) — à comparer aux 5 démos officielles HF déjà dans le dossier (rappel §1.11 : leur verdict « pop moderne » n'a pas de valeur prédictive gothic, prompts pop par construction).
* **❌ Verdict utilisateur (2026-09-13, écoute `gothic_short_seed42_8steps.wav`) : « mieux mais encore du POP dedans c'est fou »** — **rejeté pour la cible gothic rock anti-pop**. **Le plafond pop est désormais confirmé sur 5 moteurs** : ACE-Step (toutes branches textuelles, §1.11), MiniMax-Music3, SA3 Medium texte, SA3 small SFX, **YuE2-3B lyrics-to-song** — le « candidat direct anti-pop » tant attendu dérive lui aussi. Même la structure paroles `[Verse]/[Chorus]` + style genre explicite ne l'en sort pas. Le YuE2 reste noté comme **moteur lyrics-to-song fonctionnel** (chant intelligible + arrangement, RTF 2,8 en 8 steps) pour un éventuel besoin d'un genre qu'il maîtrise (pop/indie moderne) — **pas de workflow, cible gothique CLOSE sur ce moteur aussi**. Dernier levier non testé si besoin un jour : `cot=full` (plan ABC) ; couplage SheetSage2→ABC→`cot=melody` = territoire cover, rejeté définitivement (§1.11).
* **Statut : testé, verdict utilisateur négatif sur la cible gothique — aucun workflow.** Si un besoin Yue2 réapparaît (autre genre) : attendre une release audio.cpp intégrant `yue2` + optimisations Vulkan RDNA2 (RTF 2,8 en 8 steps reste 14× le 5090 CUDA), arbitrer la licence NC pour l'usage prévu. Modèle retéléchargeable (`audio-cpp/Yue2-3B-GGUF`).

### 🧱 1.23. Blendkit (addon Blender) — pipeline headless API REST VALIDÉ par l'utilisateur le 2026-09-15 (« magnifique ») : workflow `asset_blendkit` créé (prop Godot .glb + plaque décor chaîne Cycles)

* **Quoi** : **Blendkit** (« Blendkit Online Asset Library » v3.21.2, addon de la session Blender — dossier `%APPDATA%\Blender Foundation\Blender\5.2\extensions\user_default\blenderkit` ; session migrée 5.2.1 → 5.2.2 LTS le 2026-09-15 soir, addons intacts, workflow validé sur 5.2.1 le matin même) = bibliothèque d'assets en ligne (modèles 3D, matériaux PBR, HDRIs, brushs) avec filtre licence **CC0**. Cas d'usage visés : props GLB pour le jeu Godot (complément de `mesh_ia`/TRELLIS.2 pour les objets standards) + décors/plaques de base rendues Blender pour la chaîne YouTube (**arbitrage utilisateur 2026-09-15 : l'origine humaine des assets n'est PAS un critère bloquant** pour la chaîne).
* **Recette API (validée par le test, SANS le daemon local)** :
  1. **Recherche** (anonyme, pas de clé requise) : `GET https://www.blenderkit.com/api/v1/search/?query=<mots>+asset_type:model,author+license:cc_zero+order:-score&page_size=N&dict_parameters=1` — le domaine `blendkit.com` (sans r) est un alias officiel du même service ; `isFree` se filtre côté client. Réponse : `results[]` avec `displayName`, `license`, `assetBaseId`, `files[]` (`fileType` = blend/gltf/gltf_godot/resolution_XK/thumbnail, chacun avec son `id`).
  2. **Clé API** : celle du compte connecté dans le GUI, stockée dans `bpy.context.preferences.addons['bl_ext.user_default.blenderkit'].preferences.api_key` — à lire **dans un Blender headless** (`--background --python-expr`), jamais en dehors (userpref.blend est binaire) et **jamais logguée en clair**.
  3. **URL signée** : `GET /api/v1/downloads/<file_id>/?scene_uuid=<uuid4>` + header `Authorization: Bearer <clé>` → JSON `{"fileType", "uuid", "filePath"}` ; `filePath` = URL signée à télécharger **sans auth**. ⚠️ `scene_uuid` doit être un **vrai UUID** en **query string** (pas dans un corps JSON — le serveur ignore le corps d'un GET) sinon 403 avec un message explicite.
  4. **Téléchargement** : le CDN (`assets.blenderkit.com`) renvoie **403 au User-Agent Python** → passer un UA navigateur (`Mozilla/5.0`).
  5. **Import/usage** : `bpy.data.libraries.load(blend, link=False)` append les objets (matériaux/textures suivent, textures packées) ; rendu de contrôle **Workbench** (aucune lumière requise, `color_type='TEXTURE'`) ; export `bpy.ops.export_scene.gltf(export_format='GLB')`.
* **⚠️ Impasse documentée : le daemon local de l'addon (Blendkit-Client, binaire Go sur `127.0.0.1:<port>`) est inutilisable en headless** — en background `global_vars.CLIENT_ACCESSIBLE=False`, le handshake addon↔daemon n'a pas lieu, et un daemon orphelin peut traîner avec une config serveur vide (`/wrappers/get_download_url` → 500 « unsupported protocol scheme »). La voie **API REST directe** ci-dessus est LA solution robuste (elle reproduit ce que fait le GUI, sans lui).
* **⚠️ Piège Blender headless** : `bpy.ops.wm.read_factory_settings(use_empty=True)` **réinitialise les préférences addon en mémoire** → lire la clé API AVANT, la garder dans une variable.
* **Test du 2026-09-15** (`scripts/proto_blendkit_test.py`, sorties `output/test_blendkit/`) : « Wooden barrel » CC0 (28 692 faces, 2 maillages + support, 3 matériaux) → GLB 2.0 valide (58 Mo — le fichier `blend` de base embarque du 4K ; des variantes `resolution_0_5K`→`8K` existent dans `files[]` pour alléger, 2K = cible prop jeu) + rendu Workbench cadré correct (fit FOV sur l'englobant). Décors complets (asset_type:scene) = **tous `free=False`** (plan payant requis malgré licence cc_zero) → pour la chaîne, passer par des modèles d'intérieur gratuits (la recherche de décors utilisables est EN COURS à la date de cette note).
* **Process de découverte (méthodologie réutilisable par un agent IA)** : ① inventaire headless des addons (`addon_utils.modules()` + `check()` = source de vérité sur ce qui est ACTIVÉ, vs simple listing de dossiers) ; ② **le code source de l'addon EST la doc de son API** — grep `bl_idname`/`class.*Operator`/`def search(`/`query_to_url`/`paths.*API` pour extraire endpoints et contrats ; ③ sondage curl **anonyme** pour séparer public/protégé (200 vs 401/403) ; ④ jeton factice pour lire le schéma d'erreur sans exposer la vraie clé ; ⑤ itération **erreur-guidée** — chaque 403 nommait le paramètre manquant suivant (`Invalid token` → `scene_uuid not set` → `not a valid UUID`) ; ⑥ secrets lus dans l'application, jamais hors de son processus.
* **Test « plaque chaîne » du 2026-09-15** (`scripts/proto_blendkit_plate.py`, sorties `output/test_blendkit/plaque_1080p.png` + `probe_*.png`) : scène CC0 gratuite « Colorful tunnel » (219 objets, 124,8 Mo, 3 caméras, 0 lumière — tout en émissifs matériaux) → rendu 5760×3240 splendide conforme à la vignette officielle (tunnel néon violet/rose/orange). Enseignements : ① **EEVEE sature cette scène** (monde à strength 7,7 + surfaces blanches → tout cramé même AgX/exposure −1) → **Cycles GPU HIP sur la 6950 XT = le bon moteur**, exposure −1 + AgX et le look officiel est reproduit ; ② l'enum RNA des moteurs ne liste pas tout en headless (`engine.enum_items` = ['BLENDER_EEVEE'] seulement) → **tester l'AFFECTATION DIRECTE** `scene.render.engine = 'CYCLES'` en try/except, elle fonctionne ; ③ les fichiers scène imposent leur `resolution_percentage` (300 % ici → 1080p demandé = 6K rendu) → **toujours le réinitialiser** dans le pipeline ; ④ les décors complets (asset_type:scene) gratuits EXISTENT (contrairement aux scènes « interior room », toutes payantes) — requêtes gagnantes : `studio`, `environment` (« Colorful tunnel », « Low Poly City Pack », « Night Environment », « Majestic Mountain at Sunset ») ; ⑤ le CUEW warning (« CUDA init failed ») est bénin, HIP prend le relais.
* **✅ VALIDATION + WORKFLOW (2026-09-15, même journée)** : verdict utilisateur **« magnifique »** sur les deux modes → **workflow `main.py -w asset_blendkit` créé** (`core/blendkit.py` + `workflows/asset_blendkit.py` + worker `scripts/blendkit_blender_job.py`) : `--query`/`--index`/`--list-assets`, licence **cc_zero forcée par défaut**, mode **`prop`** (GLB Godot + aperçu Workbench, `--resolution 2K` par défaut = **GLB 9,4 Mo vs 58 Mo en max, ÷6**) et mode **`plate`** (Cycles GPU HIP, AgX + exposure −1, `--width/--height/--percentage/--camera/--engine/--samples`, percentage réinitialisé à 100 = résolution stricte maîtrisée). Cache `output/blendkit/<asset>/source.blend` réutilisé entre runs (`--no-cache` pour forcer). Écueil corrigé au premier run : **chemins du job TOUJOURS en absolu** (read_factory_settings/open_mainfile réinitialisent le CWD de Blender). Arborescence du compte : scènes « interior room » payantes mais requêtes `studio`/`environment` regorgent de scènes gratuites (tunnel néon, low poly city, night environment).


### 🎸 1.24. sa3.cpp (port C++/GGML natif de Stable Audio 3) — installé et testé le 2026-09-15 : gothic rock 30 s en 3,6 s (RTF 0,12) — verdict utilisateur « non concluant » → NON VALIDÉ, cible gothique close

* **Quoi** : **sa3.cpp** (`C:\IA\sa3.cpp`, betweentwomidnights — auteur de gary4local ; commit C-ABI #43 inclus) = port C++/GGML **natif** de Stable Audio 3, Vulkan, zéro PyTorch — INDÉPENDANT d'audio.cpp (famille `stable_audio` §sfx), avec en plus : modèles multiples (medium 1,5B / small-music 0,5B / small-sfx 0,5B / SAOS / foundation-1), quantizations Q4→Q8, `--duration` exact, seed réellement reproductible, continuation/splice avec `--init`, LoRA natifs, serveur HTTP optionnel.
* **Install (2026-09-15)** : clone + sous-modules (ggml fork épinglé betweentwomidnights) ; **écueil build.cmd : il cherche le générateur « Visual Studio 17 2022 » absent du poste** (VS « 18 » 2026 Community, MSVC 14.44/14.51) → recette : `cmake -S . -B build-vulkan -G "Visual Studio 18 2026" -A x64 -DCMAKE_BUILD_TYPE=Release -DSA3_VULKAN=ON` puis `cmake --build build-vulkan --config Release` (binaires `build-vulkan\bin\Release\sa3-generate.exe`). Modèles via `models.cmd --variant small-music` → 5 fichiers HF `thepatch/*` (dit 0,5B F16 919 Mo + same-s F32 + conditioner + t5gemma-b encoder 0,3B + vocab, **1,93 Go au total**) dans `models/` (⚠️ les GGUF `C:\Modeles_LLM\Stable-Audio-3-*` = fichiers uniques format audio.cpp, NON réutilisables : sa3.cpp attend un jeu tok/t5/cond/dit/same). Téléchargement ~65 Mo/s sans bridage CDN.
* **Test référence gothic (recette instruction permanente)** : `SA3_MODELS_DIR=...\models sa3-generate --model small-music --duration 30 --seed 42 --prompt "German gothic rock 1990, dark wave, hypnotic tribal groove, deep pulsing bass, chiming chorus guitars, 83 BPM, C# minor" --out …` — pas d'options bpm/keyscale dédiées → imposés dans le texte du prompt. Résultat : **30,00 s audio (PCM 16 bits 44,1 kHz stéréo) en 3,6 s wall** (8 steps défaut, graphe Vulkan détecté proprement, 6,13 s de schedule headroom tronqué canvas 390→324 frames). Fichier : `output/test_sa3cpp/gothic_rock_30s_seed42_smallmusic.wav`. RTF 0,12 = **~80× plus rapide que YuE2 §1.22 en 8 steps** (0,5B vs 3B, 8 passes vs 16 NAR).
* **⚠️ Contexte du verdict** : le plafond « pop » était déjà documenté sur SA3 Medium texte et SA3 small SFX **via audio.cpp** (§1.22, 5 moteurs) — sa3.cpp est un autre runtime du même modèle de base, avec des leviers que audio.cpp n'expose pas (`--cfg-rescale`, `--apg-scale`, `--dist-shift`, negative prompt, `--steps`) ; **non exercés** : le verdict est tombé au premier essai, sans nécessité de pousser plus loin.
* **❌ Verdict utilisateur (2026-09-15, écoute `gothic_rock_30s_seed42_smallmusic.wav`) : « non concluant » — testé, NON VALIDÉ, aucun workflow.** Conforme au plafond pop SA3 de §1.22 : **la famille SA3 est désormais testée sur DEUX runtimes (audio.cpp, sa3.cpp) avec le même résultat sur la cible gothique — cible gothique CLOSE sur SA3 aussi** (6ᵉ moteur au plafond pop). **Install conservée** (`C:\IA\sa3.cpp` + models 1,93 Go, retéléchargeables HF) : le runtime reste le plus rapide du poste (RTF 0,12, seed reproductible, `--duration` exact) et peut resservir pour SFX/ambiances ou un autre genre ; leviers non testés si besoin un jour : `--cfg-rescale`/`--apg-scale`/negative prompt, medium 1,5B, quantization Q4_K_M. Désinstallation = supprimer `C:\IA\sa3.cpp` (clone + build + models).

### 🤖 1.25. Voix de robot EN — workflow `voix_robot` VALIDÉ utilisateur le 2026-09-17 (« c'est bien » sur l'essai 17) : Kokoro `af_heart` + pitch +30 % / ring mod 120 Hz / atempo 0,65 / gain −4 dB

* **Besoin** : une « petite voix de robot » en anglais (pas une grosse voix grave — l'utilisateur a écarté d'emblée le registre C-3PO/grave). Phrase de test commune aux 17 essais : « Greetings, human. I am your robot assistant. All systems are fully operational. Beep. Boop. »
* **Campagne d'essais (2026-09-17, `output/test_voix_robot/`)** : 2 moteurs TTS × effets FFmpeg distincts — ① Kokoro-82M `af_heart` (F) et `am_michael` (M) via audio.cpp (scratch build-357, écueil espeak §1.21) ; ② qwentts.cpp `ryan` (CustomVoice) + **VoiceDesign natif** (instruct robot : monotone/métallique puis « small cute hovering drone ») ; effets : ring mod 50-120 Hz (`aeval`), pitch −20 %/+30 % (`asetrate`), flanger, bitcrush 6 kHz+trémolo, vibrato+phaser, mix sec/ring-mod 50 %. Verdicts successifs : préférence pour les ring mod (« pas mal ») en « voix moins forte » → essai 13 (pitch +30 %, ring 120 Hz, −4 dB) « un peu trop vite » → **essai 17 = recette finale « c'est bien »** : base Kokoro à débit NORMAL (pas de `--speaking-rate` — le +15 % pressait trop le débit), `asetrate=31200,aresample=24000,atempo=0.65,aeval='val(0)*sin(2*PI*120*t)',volume=-4dB` (7,56 s, mean −32,6 dB, SANS loudnorm — la normalisation LUFS n'est pas dans la recette validée).
* **Leviers appris** : ① le pitch up (+25/30 %) est ce qui fait « petit » ; le ring mod dose le caractère métallique (120 Hz ≈ buzz fin, 50-70 Hz ≈ gros robot radio) ; ② le mix sec+ring mod (asplit/amix normalize=0) adoucit sans perdre le timbre ; ③ `atempo` après `asetrate` règle le débit indépendamment du pitch (0,65 = « tranquille » validé) ; ④ le débit du TTS source compte autant que l'effet (base +15 % = trop pressé une fois pitchée) ; ⑤ VoiceDesign qwentts sait faire un robot natif mais le verdict est tombé sur la chaîne Kokoro+FFmpeg.
* **✅ Workflow** : `main.py -w voix_robot "<texte EN>" [-o nom] [--robot-voice af_heart] [--robot-pitch 1.30] [--robot-ringmod 120] [--robot-tempo 0.65] [--robot-gain -4]` — code `core/voix_robot.py` + `workflows/voix_robot.py`, sorties `output/voix_robot/<nom>/` (WAV + MP3 + brut). Résolution moteur kokoro intégrée (scratch build-357 si présent, `AUDIOCPP_KOKORO_CLI` en surcharge) tant qu'une release audio.cpp n'embarque pas espeak-ng. Test end-to-end OK : sortie identique à l'essai 17 (7,56 s / −32,6 dB).
* **Statut : VALIDÉ (usage robot EN).** Les 6 autres familles d'essais (ring mod purs, qwentts natif, bitcrush, vibrato…) restent écoutables dans `output/test_voix_robot/` comme point de départ d'itérations (paramètres `--robot-*` du workflow).

### 🧰 1.26. Chasse aux workflows blender-skills — 8 prototypes Blender headless testés le 2026-09-17 : TOUS FONCTIONNELS, en attente de verdict utilisateur (aucun workflow créé)

* **Contexte** : adaptation du pack blender-skills (arjun988, MIT, cloné `C:\GIT\blender-skills` — 94 skills MCP) à notre stack headless ; seule la partie moteur-agnostique est pertinente (budgets/nommage/LOD/checklists → consignés dans le skill `references/pipeline_3d_blender.md`, commit 740ff6e). Cette campagne teste les CAPACITÉS exécutables : prototypes `scripts/proto_blender_skills/` (7 scripts bpy + driver `run_all.py`, relance sélective par nom), sorties `output/test_blender_skills/`.
* **8/8 OK, vérifiés visuellement** (casque TRELLIS.2 144 200 tris + baril blendkit + personnage riggé) :
  ① **audit scène JSON** (tris/matériaux/UV/dims/rig par objet — casque 144 200 tris / 0,8 m ; marc_novice 38 684 tris, 8 objets) ; ② **turntable animé** 48 frames EEVEE → MP4 H.264 2 s (394 Ko) ; ③ **chaîne LOD** 144 200 → 72 100 → 36 050 → 18 025 tris (COLLAPSE delimit SHARP/UV, échelle/UV préservées, planche 4 vues : silhouette tenue à LOD3) ; ④ **proxy collision Godot** convex hull 2 324 faces nommé `SM_x-convcolonly` (import Godot → StaticBody3D auto) + aperçu alpha ; ⑤ **renommage moteur** `SM_/MAT_` (JSON avant/après) ; ⑥ **scatter geometry nodes** Poisson (barils blendkit complets multi-meshes fusionnés, Distance Min 0,9, rotations/échelles aléatoires seedées) ; ⑦ **beauty render Cycles HIP** 6950 XT 96 samples débruité ; ⑧ driver + planches de contact automatiques.
* **Écueils Blender 5.2 appris** (valables pour tout futur workflow bpy) : ① geometry nodes : `bpy.data.node_groups.new(...)` (plus `node_trees`) ; mode POISSON de DistributePointsOnFaces n'expose PAS le socket `Density` (seulement `Density Max`) ; sortie ObjectInfo = `Geometry` ; ② `bmesh.ops.convex_hull` : `geom_interior` ne contient que sommets/arêtes — pour garder l'enveloppe seule, ne conserver que les `BMFace` de `hull["geom"]` et supprimer le reste ; ③ EEVEE Next : transparence matériau via `surface_render_method='BLENDED'` (`blend_method`/`shadow_method` supprimés) ; ④ `primitive_*_add(size=…)` → `radius=` ; ⑤ `Vector - tuple` invalide (mathutils strict). Addons locaux constatés : BlenderMCP + blendkit présents dans le Blender du poste.
* **✅ VERDICT UTILISATEUR (2026-09-17 soir) : « rien d'extra, on va oublier ça » → REJETÉ, campagne CLOSE.** Aucun workflow ne sera créé à partir de ces prototypes (règle respectée : jamais de workflow sans validation). Les scripts restent archivés dans `scripts/proto_blender_skills/` (rejouables à la demande pour un besoin ponctuel, ex. MP4 de présentation d'un GLB), les rendus dans `output/test_blender_skills/` (gitignore). **Apport durable retenu** : les écueils bpy 5.2 de cette page (chemins relatifs avalés → toujours absolu, refonte compositing, GLB riggé → transformer les racines), les prérequis Blender/MPFB/BlendKit et les standards game-ready documentés dans le skill (`references/pipeline_3d_blender.md`). Le pack blender-skills n'a pas de pépite exécutable au-delà — sujet clos.
* **Campagne étendue le même jour (« le but est de tout tester », vagues 2-3) — 21 tests au total, TOUS fonctionnels, vérifiés à la planche** (`output/test_blender_skills/planche_styles.png`, `spritesheet3d_casque/atlas_4x2.png`, `bake_ao/`) :
  * **Vague 2 (outils)** : ⑨ spritesheet 3D→2D (8 directions ortho fond transparent + atlas 4×2 + mapping JSON, prêt SpriteFrames Godot) ; ⑩ bake AO headless (Cycles, carte `T_Casque_AO.png` 1024²) ; ⑪ découpe multi-assets (GLB multi-meshes → GLB par mesh + rapport JSON — baril/chevalet séparés) ; ⑫ beauty + compositing headless (glare fog_glow + courbes + BrightContrast — rim éclatant, net progrès vs vague 1).
  * **Vague 3 (styles/recettes matériaux du pack, modes `styles_demo.py`)** : ⑬ toon/cel (Diffuse→ColorRamp CONSTANT 3 paliers + bande rim Layer Weight) ; ⑭ PS1 (snap sommets grille 0.01 + flat shading + AA 8 + brouillard Volume Scatter bleu-gris + 640×480 — rendu authentique) ; ⑮ usure d'arêtes (Pointiness→ramp→mix clair) ; ⑯ rouille (Noise→seuil→mix + metallic/roughness pilotés) ; ⑰ mousse (Pointiness creux × normale Z+ → vert, roughness ↑) ; ⑱ taches d'eau (Generated Z inversé × noise → assombrit + roughness) ; ⑲ variation panneaux (hue/value seedés par matériau — subtil) ; ⑳ cloth sim (drap subdivisé + sphère collision, 40 frames headless) ; ⑴ panel appliqué au baril (multi-matériaux).
  * **⚠️ Écueil MAJEUR découvert — chemins relatifs avalés** : blender headless sur blend NON sauvegardé + `render.filepath` RELATIF = fichier perdu SANS AUCUNE ERREUR (pas résolu contre le cwd, `bpy.path.abspath('//')` renvoie chaîne vide ; destination introuvable). → **Toute sortie bpy headless DOIT être en chemin absolu** (le driver `run_all.py` le fait systématiquement — règle à généraliser à tout futur workflow Blender ; cohérent avec l'écueil blendkit §1.23).
  * **Écueils API 5.2 supplémentaires** : compositing 5.2 = refonte TOTALE (fini MixRGB/Composite/Mist ; racine = `scene.compositing_node_group` + `NodeGroupOutput`, nœuds pilotés par SOCKETS : Glare Threshold/Size/Quality=`"High"`, Blur Size=(x,y) en entrée VECTOR ; EllipseMask sort `Mask` ; BrightContrast OK ; pas de nœud Math) ; `bake(type="AO")` sans `pass_filter` (plus de 'AO' dedans) ; `object.collision` lecture seule (modificateur COLLISION seul suffit, ses attrs `thickness_*` disparus) ; primitives incohérentes : sphères=`radius`, plane/cube=`size`.
* **Vague 4 — diversification des assets (même soir)** : 11 rendus sur la collection réelle (charrette, grille, potion mesh_ia, panier, hache, bûche, fauteuil médiéval, chaise, Marc) — `output/test_blender_skills/multi/planche_multi.png` + assembleur générique `planche_dossier.py`. Résultats marquants : beauty Cycles sur GLB blendkit texturés (charrette/grille superbes), potion TRELLIS.2 avec glare éclatant, toon/PSX/rust/moss transférés sur de vraies matières (rouille→grille, mousse→bûche).
  * **⚠️ Écueil GLB RIGGÉ (Marc = cas d'école)** : normaliser (échelle + posage au sol) via les MESHES d'un personnage skinné = corps coupé/membres flottants (les meshes suivent l'armature) ; via l'ARMATURE seule = parties non skinnées désynchronisées. **Bonne recette : transformer les objets RACINES (parent=None)** — l'import glTF met un EMPTY racine au-dessus de tout, tout suit de façon cohérente (beauty Marc réparé ainsi). À réutiliser pour tout futur workflow tournant sur personnages.

### 🧪 1.27. audio.cpp v0.8.1 + 3 nouvelles familles d'OUTILS audio (Apollo / UniverSR / PulseVAD) — installée et testée le 2026-09-18 : UniverSR VALIDÉ → workflow `audio_upscale`, Apollo REJETÉ, PulseVAD fonctionnel sans verdict

* **Maj v0.8.0 → v0.8.1** (accord utilisateur, `update.ps1`, backup `backup_v0.8.0_20260918_084915.zip`). Smoke ace_step turbo 28 s : **RTF 2,06 vs 2,22 (v0.8.0, A/B même jour depuis le backup) → pas de régression, léger gain −7 % ; sorties byte-identiques (MD5)**. La baseline 1,84-1,93 du 16/09 ne se reproduit pas (dérive machine, règle A/B même jour §1.11 réappliquée). Contenu : familles outils apollo/universr/pulsevad (PR #583), YuE2 LoRA AR/NAR + export plan ABC, kokoro misaki eSpeak complet, CLI `--out-format pcm16|pcm24|float32`.
* **⚠️ Ces 3 familles sont des OUTILS de post-traitement, pas des générateurs musicaux** (catégorie `audio_tools`, tâche `s2s`) — l'instruction permanente « tester tout nouveau modèle musical » ne s'appliquait pas telle quelle ; testés quand même le jour de la maj.
* **Apollo (restauration musique compressée, `apollo-orig.gguf` 66 Mo, `C:\Modeles_LLM\Apollo-GGUF\`)** : **Vulkan OK, stéréo préservée, RTF 0,08 (12× temps réel)** — le seul des 3 pleinement exploitable. ⚠️ **Entrée 44,1 kHz OBLIGATOIRE** (sinon « Apollo requires 44.1 kHz input audio »). Cas d'usage : dé-artefacter un MP3/source compressée avant master. Écoute : `output/test_audio_cpp_v081/G_apollo_441.mp3` vs `H_compressee_64k.mp3` (source = ace_step 12 s → MP3 64 kbps).
* **UniverSR (super-résolution → 48 kHz, `universr-{audio,speech}-orig.gguf` 229 Mo chacun)** : ⚠️ **Vulkan CASSÉ — « unsupported backend op 'MUL_MAT' » dans le conditionnement → CPU obligatoire** (`--backend cpu --threads 20`, RTF ~13,3 = très lent, 28 s → 6 min). ⚠️ **Sortie MONO seulement** (entrée stéréo avalée) → inutilisable tel quel pour lits musicaux stéréo. `--request-option input_sample_rate=8000|12000|16000|24000` (bande d'entrée). Sweep 1-19,2 kHz : restaure bien jusqu'au Nyquist d'entrée, **extrapole au-delà avec atténuation progressive** (16 kHz −11 dB, 19,2 kHz −19 dB) — vraie SR mais HF partielle. Écoute voix : A (originale) / B (naïf 16k) / C (UniverSR speech) ; musique : D/E/F dans `output/test_audio_cpp_v081/`. **À retester si un jour le chemin Vulkan est implémenté** (alors seulement l'upscale 48 kHz des voix de la chaîne deviendrait praticable : RTF GPU espéré ~1).
* **PulseVAD (VAD, `pulsevad-2.1k-f32.gguf` 0,1 Mo)** : **Vulkan OK, RTF 0,004 (230× temps réel)**. ⚠️ **Entrée 16 kHz mono obligatoire**. Utilité potentielle : découpe automatique de la voix off qwentts/kokoro. `--out` JSON resté vide (segments a priori sur stdout) — à confirmer à l'usage réel.
* **Statut : testés FONCTIONNELS, non validés** (règle du dépôt : aucun workflow avant verdict utilisateur). GGUF téléchargés et en place ; retéléchargeables depuis `audio-cpp/audio.cpp-gguf`.
* **✅ VERDICTS UTILISATEUR (2026-09-18, écoute des extraits `output/test_audio_cpp_v081/`) :**
  * **UniverSR VALIDÉ** — voix 16k→48k : « les voix sont ok copie sans bug parfait même » (A/B/C) ; musique 24k→48k : « très bien belle batterie ça sonne bien » (D/E/F). → **workflow `audio_upscale` créé** (`core/audio_upscale.py` + `workflows/audio_upscale.py`, testé end-to-end voix 16k : RTF 13,03 ; bande auto via ffprobe, pré-rééchantillonnage FFmpeg si besoin, refus > 24 kHz ; variante `speech` par défaut ; piège corrigé : le `--seed` global CLI vaut −1 « aléatoire » → forcé à 42 de la recette car universr exige un entier non signé). **À retester si audio.cpp livre le chemin Vulkan** (RTF ~1 → ouvrirait l'upscale des lits musicaux).
  * **Apollo REJETÉ** — G (restauré) vs H (compressé) : « ça va moins bonne qualité », aucune amélioration audible. GGUF conservé en place (66 Mo, retéléchargeable) — suppression possible à la demande.
  * Le sweep « nul » = signal technique (bips sinus), jamais destiné à l'écoute — mesure de bande seulement.
  * PulseVAD : pas de verdict (outil VAD, rien à écouter) — fonctionnel, disponible pour un futur besoin de découpe voix off.


### 🦴 1.28. Campagne RIG IA (Blender MCP agentique) — humain + quadrupède, rendus animés, retarget local, RigAnything — testée le 2026-09-18, verdict utilisateur en attente

* **Objectif** : démontrer qu'un agent IA peut riguer correctement un humain et un animal dans Blender puis générer des animations — boucle complète « code bpy → capture viewport multi-vues → analyse → correction ».
* **Boucle MCP Blender (PHASE 0, opérationnelle)** : addon BlenderMCP **v1.5 déjà installé** (socket **localhost:9876**, protocole = 1 JSON par connexion) ; serveur MCP déclaré dans `.zcode/config.json` (`uvx --python 3.11 blender-mcp`) ; autostart GUI `scripts/proto_rig/demarrer_blender_mcp.py` ; client direct `scripts/proto_rig/blender_client.py` (`--ping/--code/--scene/--screenshot`). L'addon expose `get_viewport_screenshot` **offscreen GPU** (fonctionne fenêtre en arrière-plan) et `execute_code` (stdout capturé). Protocole capture multi-vues réutilisable : `capture_multivues.py` (avant/¾/profil/dos, cadrage auto bbox, convention sujet face −Y, azimuts vérifiés numériquement).
* **Sujets de test (Phase 1, `output/test_rig/`)** : humain A = corps MPFB2 **sans rig** (`creer_corps_personnage_mpfb(rig="")`, 14,5 k sommets, T-pose) ; humain B = même + rig Mixamo 52 os (référence) ; loup + cheval **CC0 Quaternius** (Google Drive, ids fichier via gdown --folder, curl direct `drive.usercontent.google.com/download?id=<id>&export=download&confirm=t`) ; **loup riggé Quaternius = vérité terrain : 51 os + 12 actions** (Gallop, Idle, Attack…) sur exactement le mesh cible — source de retarget locale gratuite.
* **Vague 2A — rigging agentique VALIDÉE (poses propres)** : humain = méta-rig Rigify fitté par mesures tranches Z (épaules 0,78 m, hanches, cou), generate + poids auto (`ARMATURE_AUTO`) → **410 os**, poses P1 (bras baissés) et P2 (combinée) déformations propres (« game-ready, retouches mineures épaule »). Loup = **méta-rig `wolf` EMBARQUÉ dans Rigify 5.2** (aussi cat/horse/shark/bird + `basic_quadruped`) fitté (scale 0,676, museau aligné, queue comprimée, pattes recentrées X) → **823 os dont rig facial complet**, pose P1 propre. Scènes : `output/test_rig/scenes/humain_a_rigify.blend`, `wolf_rigify.blend`.
* **Phase 3 — rendus animés continus (MP4 EEVEE headless)** : `rendre_clip.py` (caméra orbitale + 3 points + sol + ffmpeg ; patterns `BLENDER_EEVEE` 5.2). Livrés : `loup_trot.mp4` (trot procédural diagonales + queue en vague), `humain_salut.mp4` (idle + salut avant-bras + regard), `loup_gallop_retarget.mp4` (cf. infra). Amplitude wave bras : garder les poses P1 éprouvées (signes Z miroirs L/R : −=baisser à gauche, +=baisser à droite) — la levée au-dessus de la tête par rotation Z seule NE MARCHE PAS (axes locaux FK post-roll à documenter avant usage).
* **Retarget local Quaternius → Rigify (`retarget_quaternius.py`) — RÉUSSI (après 4 bugs corrigés, verdict clips)** : append du fichier ref ENTIER (import sélectif de l'armature seule = contraintes IK cassées) ; **lien de l'armature importée dans la collection de la scène OBLIGATOIRE** (`libraries.load` charge les datablocks SANS instanciation → jamais évalués par le depsgraph : action ET contraintes IK figées au rest = symptôme « pose identique à toutes les frames ») ; **l'action importée (legacy 2.79 → slots 5.2) ne s'évalue pas même slot lié** → évaluation MANUELLE des fcurves (`channelbag.fcurves[i].evaluate(frame)`) ; le rig Quaternius est un rig **IK** : ses actions n'animent que les contrôleurs `IKFrontLeg/IKBackLeg` en LOCATION, le solveur IK fait le reste (appliquer locations puis update_tag+update) ; mapping explicite 27 os (**numérotation DEF-spine INVERSÉE — `DEF-spine` = bout de queue, `.011` = tête**) ; bake par **delta de rotation monde converti en basis locale `q_rest⁻¹ @ q_frame`** (quaternion armature direct = pose doublée) ; **contraintes des DEF mappés muted** ; **nettoyage des objets importés par DIFFERENCE avant/après (jamais de nom en dur : 'Wolf' est NOTRE mesh, le ref s'importe en 'Wolf.001' — le premier cleanup a supprimé notre mesh et les rendus montraient le mesh ref figé)**. Translation ignorée (sur place). Clips : gallop/attack/saut retargetés, déformation propre.
* **RigAnything (Adobe, SIGGRAPH TOG 2025) — testé CPU local, FONCTIONNEL, mais licence NON-COMMERCIALE** (Adobe Research License : éval/recherche OK, production interdite → adoption exclue, test évaluatif seul). Install : `C:\IA\RigAnything` (venv uv py3.11, torch CPU, ckpt 2,18 Go HF téléchargé à 213 Mo/s via le downloader parallèle). **4 patches CPU obligatoires** : `inference.py` device cuda→auto, autocast cuda→cpu, `utils_ar_transformer.py` device par défaut + KV cache fp16→fp32 si CPU, `mesh_simplify.py` chemins Windows (ligne 55 écrase output_path + join `/` ; objet exporté = `PointCloudObject`), balayage global `.cuda()` → fallback (fichier `gaussian_diffusion.py` : alias `th`, pas `torch`). Pipeline : mesh_simplify (obj→glb) → inference (transformer 1,1 s + diffusion 2,7 s **CPU**) → vis_skel → `*_rig.glb`. Résultat loup : **31 os auto + skinning 31 VG, pose de test déforme proprement** (`wolf_riganything.blend`, planche `planche_riganything.png`, clip `loup_riganything.mp4`). Nommage générique Bone_N (à renommer pour Godot), pas de rig facial.
* **⚠️ Écueils Blender 5.2 + MCP supplémentaires** : `read_factory_settings` depuis execute_code **coupe la réponse socket** de l'addon (purger objets à la place) ; générer Rigify depuis un **méta-rig caché** = `AssertionError __duplicate_rig` ; membres parfaitement rectilignes = crash `compute_pole_angle` (donner un léger pli coude/genou) ; `super_head` sans rig facial ne crée pas d'os DEF tête (contournement : `use_deform` sur contrôles + vertex groups manuels, ou paramètres `use_head`/`neck_pos` sur l'os spine) ; enum moteur = `BLENDER_EEVEE` (plus de `_NEXT`) ; `origin_set` 5.2 sans argument `mode` ; importer un GLB crée des matrices locales ROTÉES (Y-up) → tout décalage géométrique en espace MONDE via `W⁻¹` ; `bound_box` est un cache paresseux (mesurer par sommets) ; `next()` armature peut attraper le méta-rig caché.
* **✅ VERDICT UTILISATEUR (2026-09-18 soir) : `loup_trot.mp4` OK — tout le reste (retargets, humain) NON** → recette VALIDÉE = animation procédurale FK sur nos rigs (bascule `IK_FK=1` membres + clés sinusoïdales + caméra fixe). Retarget Quaternius : clés techniquement correctes mais résultat visuel NON retenu — PARKÉ (scripts conservés, rejouables). Marche humaine : « collerette » autour du cou = **conflit entre les vertex groups manuels en poids durs (REPLACE 1.0, frontières nettes) et les poids auto parasites (DEF-breast…)** dès que les bras bougent — **corrigé par dégradé doux tête→cou→spine (bandes de 6 cm) + purge des groupes parasites au-dessus de z=1.00** ; la « collerette » des vignettes 480 px était aussi en partie une illusion d'échelle (toujours vérifier au zoom 800 px avant de conclure).
* **Complément rendus animés (même jour, suite retour utilisateur « ça tourne, ça fait rien »)** : orbite caméra = mouvement parasite qui masque tout → `--fixe` ; amplitudes ×1,7 ; **sliders `IK_FK` sur les os `*_parent` à 1.0 (FK) + keyframe, sinon les contrôles fk sont INERTES** (membres Rigify en IK par défaut) — bascule STRICTEMENT limitée aux membres (la chaîne torse/cou basculée = blob d'épaules) ; `torso.location` animé = étirement tête/cou (conflit poids manuels) → rotations seulement. Clips finaux : gallop/attack/saut retargetés + trot procédural amplifié + salut humain (version propre conservée).
* **⚠️ Écueil « 2 persos l'un dans l'autre » (marche humaine, résolu)** : la réparation des yeux avait ré-importé le GLB MPFB dont **un objet mesh nommé `Human` (14,5 k sommets) restait dans la scène SANS armature ni parent** — le corps riggé animait par-dessus ce fantôme figé en T-pose (silhouette doublée, « collier », « longs doigts »). Leçon : un import GLB ramène TOUS ses objets — purge par différence avant/après (même recette que le retarget), jamais au nom près.
* **Kimodo (nv-tlabs, NVIDIA) — TEST RÉUSSI le 2026-09-18 soir : mocap IA généré en CPU** (« a person walking forward », 4 s, diffusion 100 steps en 50 s CPU, **BVH + NPZ livrés** `C:\IA\kimodo\outputs\marche.bvh`). Install : venv py3.11, torch CPU, `-e .` avec **CMAKE_GENERATOR=Ninja + gcc MSYS2** ; **6 obstacles surmontés** : ① device CPU déjà prévu dans generate.py ; ② texte-encoder gated **Meta-Llama-3-8B-Instruct → 403 sans approbation Meta** (token HF classic « read » de l'utilisateur + demande d'accès) ; ③ le vrai repo bloqué = **McGill-NLP/LLM2Vec-Meta-Llama-3-8B-Instruct-mntp (adaptateur PEFT) dont l'adapter_config pointe le base gated** → dossier local `text_encoders/McGill-NLP/…` (mécanisme TEXT_ENCODERS_DIR) avec **adapter_config + config retargetés vers le miroir non-gated NousResearch/Meta-Llama-3-8B-Instruct** (mêmes poids) + idem pour l'adaptateur « -supervised » (pipeline LLM2Vec = 2 étages) ; ④ **MotionCorrection** sous-paquet C++ à installer séparément (`pip install -e ./MotionCorrection`) ; ⑤ **DLL MinGW** (libgcc/libstdc++) à copier à côté du .pyd (le PATH ne suffit pas au chargeur Windows) ; ⑥ BVH importé à **global_scale=0.01** (le BVH sort en cm) + translation racine X/Y à supprimer pour une boucle sur place. Preuve : `output/test_rig/planche_kimodo.png` (3 poses), clip `kimodo_marche_IA.mp4` (stickman EEVEE, cadrage imparfait), scènes `kimodo_marche.blend`. Prochaine étape naturelle : BVH → retarget vers nos rigs Rigify (pipeline standard) ou lecture directe du BVH dans Blender (import→action native).
* **Kimodo (nv-tlabs, NVIDIA) — historique BLOQUÉ sur accès HF** : install Windows réussie (`C:\IA\kimodo`, venv py3.11, torch CPU, **extension C++ à compiler via CMake : `CMAKE_GENERATOR=Ninja` + gcc MSYS2 dans le PATH**, sinon MinGW introuvable) ; le CLI `generate.py` a le fallback CPU intégré ✓ et exporte du **BVH** (importable Blender) ; MAIS le texte-encoder LLM2Vec repose sur **meta-llama/Meta-Llama-3-8B-Instruct = repo HF GATED → 403 sans token autorisé par Meta**. Déblocage côté utilisateur : demander l'accès au repo Llama sur HF puis fournir un token HF (`hf auth login` ou `HF_TOKEN`). En attente.
* **État token Kimodo (18/09 soir)** : token HF révoqué par l'utilisateur après usage (sécurité). L'accès au repo Llama reste accordé au compte « laurentvv » (demande en attente de revue Meta au moment du test). Pour relancer Kimodo : régénérer un token HF (Read suffit) et le repasser à `huggingface_hub.login()`.
* **Run complet game-ready (18/09 soir) — fourrure** : le loup Quaternius n'a **pas d'UV** (OBJ flat-color) → texture de poils impossible ; fourrure testée en **shells géométriques** (4 copies + Solidify offset/thickness croissants + matériau semi-transparent alpha décroissant, visible_shadow=False) → exporte en GLB (alphaMode BLEND) MAIS **délave le style low-poly** (voiles clairs sur corps sombre + sur-exposition) : le style flat net reste supérieur pour ce style d'asset. Recette fourrure crédible = mesh UV + texture alpha de poils (MASK), ou hair particles (rendus vidéo SEULEMENT — pas exportable GLB/Godot). Yeux : roughness 0.08 (brillant). Livré : `loup_quaternius_fur_godot.glb` (2,3 Mo, optionnel) + `loup_quaternius_godot.glb` (recommandé). Export glTF headless : `bpy.ops.object.select_all` échoue (contexte) → `select_set` direct par objet.
* **Kimodo — validation visuelle** : mocap de marche généré, armature animée vérifiée en captures (poses distinctes f10/f40/f70). Habillage sur mannequin MPFB : **ARMATURE_AUTO sur squelette BVH = conflits avec les vertex groups hérités du rig humain** (tête étirée en tentacule — les poids durs head/neck interprétés par les os BVH aux pivots différents). À retravailler avant rendu habillé : nettoyer les anciens VG du mannequin avant ARMATURE_AUTO, ou retarget propre BVH→Rigify. Le BVH brut (`marche.bvh`) reste le livrable réutilisable.
* **✅ VALIDATION FINALE UTILISATEUR : « ça valide le galop parfait ! »** — le **gallop natif Quaternius est VALIDÉ** (rendu profil boucle, avec ou sans shells fourrure par-dessus : l'animation passe). Texture/fourrure shell = rejetée (flat net supérieur). Le loup du jeu = **loup_quaternius_godot.glb (flat, 12 clips AN_*)** — asset animal définitif.
* **✅ VERDICTS FINAUX (2026-09-18 soir) — hiérarchie de qualité établie par l'utilisateur** : ① **mocap natif sur personnage texturé = BEST** (ref_loup_gallop_natif.mp4 « parfait ») ; ② procédural FK = MOYEN (trot dégradé de « ok » à « moyen ») ; ③ retarget, humain, stickman Kimodo = NUL. **Conséquence actionable : les 12 anims Quaternius natives sont le standard du jeu** → **livré `output/test_rig/loup_quaternius_godot.glb`** (828 Ko, 51 os, clips renommés AN_Attack/AN_Gallop/AN_Walk/AN_Death/…, export glTF `export_animation_mode='ACTIONS'`, vérifié par ré-import) — glissable dans Godot (AnimationPlayer auto). Piège headless : `bpy.ops.object.select_all` échoue en -b (contexte) → sélectionner par `select_set` direct. Recette validée définitive pour les animaux du jeu : **assets pack natifs + rendu profil + boucle** ; rig agentique = réservé aux assets personnalisés ; procédural = boucles d'ambiance secondaires seulement.
* **Statut global : TESTÉ FONCTIONNEL, campagne CLOSE sur ces acquis (Phase 4 skill/workflow à lancer)** (règle du dépôt : aucun workflow ni skill avant validation — Phase 4 = skill `blender-rig` + workflow + docs si validé). Livrables : `output/test_rig/clips/*.mp4` (4), planches `output/test_rig/planche_*.png`, scripts rejouables `scripts/proto_rig/`. Mixamo/AccuRIG : côté utilisateur (comptes requis ; Mixamo déjà utilisé pour « L'Héritier du Vide » — pipeline manuel humain Blender→Godot existant).

### 🕺 1.29. ControlNet OpenPose SDXL via sd-cli (conditionnement pose→image RÉEL) + animation de personnage — VALIDÉ le 2026-09-26 (statique A/B + boucle animée cohérente)

* **Contexte** : inspiration veille ComfyUI `Work-Fisher/ComfyUI-Fisher-Pose` (studio de pose MakeHuman → LoRA → Qwen Image 2.1, hors périmètre) ; constat que notre workflow `pose_control` NE conditionne PAS réellement la génération (squelette dessiné + exporté pour Marker2D Godot, mais image générée par prompt seul — `generer_image_vulkan` n'exposait aucun flag de contrôle).
* **Recette techniquement validée (A/B convaincante)** : sd-cli **master-908 (88411ef)** + checkpoint SDXL `juggernautXL_ragnarok.safetensors` (mono, VAE+TE inclus) + **ControlNet `xinsir/controlnet-openpose-sdxl-1.0`** (`diffusion_pytorch_model.safetensors` 2,5 Go → `C:\Modeles_LLM\controlnet_openpose_sdxl_xinsir.safetensors`, téléchargé en 25 s sans bridage) + squelette OpenPose COCO 18 de NOTRE générateur (`core/pose_ops`, pose `slash_attack`, 768×1024). Flags : `--control-net <xinsir> --control-image <squelette> --control-strength 0.9`, steps 25, euler_a, cfg 7, seed 42 → **55 s** (768×1024).
* **Preuve A/B (`output/test_controlnet/`)** : même prompt/seed avec vs sans ControlNet → **poses totalement différentes sans** (épée basse, bras croisé) ; **avec = bras levé, second bras tendu bas-droite, jambes écartées épousant exactement les coordonnées du squelette**. Le conditionnement est bien ce qui impose la pose. Rendu via couche Python identique (déterminisme).
* **Prérequis amont** : PR **#1752 « fix: SDXL ControlNet (diffusers naming + graph size) » MERGÉE le 2026-07-06** — le support SDXL ControlNet est récent ; présent dans nos 2 binaires épinglés (88411ef ET 6b3edaa). L'issue #661 « SDXL Control Net support » reste ouverte (feature générique), mais le chemin testé fonctionne. ⚠️ Ne PAS prendre le `OpenPoseXL2.safetensors` de thibaud (= format Control-LoRA Stability, non supporté) ; xinsir choisi (107k dl, format diffusers = exactement ce que #1752 corrige).
* **Mémoire** : total **9 Go VRAM** (diffusion 4,90 + controlnet 2,39 + text_encoders 1,56 + VAE 0,16) sur 16 Go — OK, WARN « Failed to allocate pinned memory » connu non fatal. Une seule génération à la fois (règle générale confirmée).
* **Plumbing livré** : `generer_image_vulkan(..., control_image, control_net, control_strength=0.9)` — validation (les 2 params requis ensemble, fichier existant), affichage « + ControlNet » dans le mode, flags injectés dans les 2 branches (Flux/SDXL — non testé côté Flux, ControlNet Flux non couvert amont). ruff OK, 57 tests verts, **surface CLI inchangée** (params helper, pas de nouveaux flags workflow).
* **✅ VALIDATION + PROTOTYPE ANIMATION (même jour, accord « Génère 1. : ça validera le résultat pour skill et readme »)** — **sprite animé de 12 frames** généré depuis la chorégraphie réelle d'un rig : action `CharacterArmature|Punch` du GLB bunny Quaternius → keypoints COCO 18 par frame via **Blender headless** (`scripts/proto_anim_controlnet/exporter_squelettes_punch.py`, projection monde→image avec transformation FIXE sur la séquence) → génération frame par frame → GIF ping-pong + bande + planche README. Livrables : `output/test_anim_controlnet/` (12 frames, `boucle_punch.gif`, `bande_punch.png`, `planche_readme.png`), commités dans `docs/exemples/personnages/` (planche + GIF 384×512, 2,6 Mo) et README §Visual Showcase 6.
* **Recette animation retenue (passe finale)** : chaque frame = **txt2img + ControlNet (squelette k, 0,9) + IP-Adapter Plus (référence d'apparence unique, 0,7)**, même prompt + même seed. Modèle IP-Adapter : `h94/IP-Adapter` `sdxl_models/ip-adapter-plus_sdxl_vit-h.safetensors` (0,85 Go → `C:\Modeles_LLM\`), encodeur `--clip_vision` = notre `clip_vision_h.safetensors` (1,26 Go, celui de Wan I2V — compatible). ~50 s/frame 768×1024, VRAM ~9 Go. Prompt ALIGNÉ avec la référence (« shining steel » ↔ chevalier acier clair).
* **⚠️ 3 écueils mesurés sur les passes intermédiaires (ne pas refaire)** : ① **img2img « étoile » ancre la pose** — init = frame 0 à strength 0,55 → apparence parfaite mais le personnage NE BOUGE PLUS (le latent de départ domine le ControlNet) : le contrôle de pose exige txt2img ; ② **dérive d'apparence si prompt ≠ référence** (prompt « dark armor » + référence acier clair à IP 0,7 → armures sombres hétérogènes) et **sur-verrouillage du ton à IP 0,85 sur référence sombre** (silhouettes noires + fond lumineux) → référence alignée au prompt ; ③ **attaque vue de FACE = foreshortening** (bras vers la caméra → squelette 2D quasi statique, animation illisible) → projeter le rig en **vue de profil** (simple re-projection du JSON 3D, pas de re-passage Blender). Bonus : bug d'offset vertical corrigé (`oy = H/2 + cz*échelle` pour image_y = oy − z·éch) ; boucle non cyclique → GIF **ping-pong** (frames 0..11..1).
* **🔧 Itération « c'est pas terrible » (retour utilisateur sur la v1 punch, même jour)** : à IP 0,7 le **verrou d'apparence écrase la pose** (le personnage converge vers la pose de la référence → quasi immobile). Correctifs appliqués et validés : **ControlNet 1.0 + IP-Adapter 0.45** + chorégraphie **`Run`** (cycle naturellement cyclique → boucle directe sans ping-pong, action bien plus lisible de profil qu'un punch) → course explicite (jambes alternées, stride aérien). **Arbitrage à retenir : plus l'IP-Adapter est fort, plus la pose converge vers celle de la référence — pour une action dynamique rester ≤ 0,5** (l'alignement prompt/référence + seed fixe assurant la cohérence d'apparence). Livrables remplacés (`bande_run.png`, `boucle_run.gif`, planche README 1,4 Mo) ; archives des passes : `output/test_anim_controlnet/v1_vue_face/`, `v3_punch_ip07/`.
* **Câblage production** : `pose_control` conditionne désormais RÉELLEMENT sur son squelette (`controlnet_pose` en config, défaut `DEFAULT_CONTROLNET_POSE` = xinsir, strength 0,9 ; **repli automatique prompt seul** si le fichier manque). ⚠️ **Écueil d'intégration : le ControlNet xinsir est SDXL UNIQUEMENT** — sd-cli refuse (`code retour 1`) quand le modèle actif est Flux → `pose_control` bascule automatiquement sur le checkpoint SDXL (`controlnet_sd_model`, défaut `DEFAULT_SDXL_MODEL` = juggernautXL) quand le conditionnement est actif. Surface CLI inchangée, 57 tests verts, ruff OK. Skill (`SKILL.md` + `catalogue_workflows.md`) et `C:\SD\README.md` à jour. Modèles à provisionner pour une nouvelle machine : xinsir 2,5 Go + ip-adapter-plus 0,85 Go.
* **Suite convenue : route 2 à tester** — vidéo suivant une chorégraphie via `--control-video` (frames de squelettes en répertoire) sur LTX/Wan ; contraintes connues : marge mémoire LTX fragile sur master-908, crash Wan I2V 0xC0000409 non résolu (#1976).
* **✅ ROUTE 2 TESTÉE le 2026-09-26 : Wan 2.1 VACE 14B + `--control-video` — la tenue temporelle règle le « moyen » de la route 1** (verdict utilisateur sur l'esthétique : en attente). ① **Principe** : VACE = variantes Wan 2.1 entraînées pour suivre des frames de contrôle (pose/depth) ; c'est LE domaine de `--control-video` (support amont PR #819, 2025-09) ; **Wan 2.2 VACE PAS supporté** (issues #2062/#1953 ouvertes). Nos squelettes COCO passent tels quels (prétraités en `control-frame`). ② **Conversions GGUF** : `samuelchristlie/Wan2.1-VACE-1.3B-GGUF` **INCOMPATIBLE** (tenseur `vace_patch_embedding` absent des métadonnées → refus validation sd-cli ; seule conversion 1.3B existante) ; **`QuantStack/Wan2.1_14B_VACE-GGUF` = la bonne** (téléchargé Q3_K_S 7,84 Go → `C:\Modeles_LLM\Wan2.1_14B_VACE-Q3_K_S.gguf`). ⚠️ Les lignes `ERROR ... patch_embedding ... 5 > 4` au chargement sont **BÉNIGNES** (le chargeur ggml passe la main au chargeur interne 5D — issue #1759 fermée, « the file can still be loaded normally »). ③ **Recette testée** : `-M vid_gen --diffusion-model Wan2.1_14B_VACE-Q3_K_S --vae wan_2.1_vae --t5xxl umt5-xxl-Q4_K_M -p "..." -W 832 -H 480 --video-frames 13 --fps 16 --steps 20 --cfg-scale 6.0 --sampling-method euler --diffusion-fa --control-video <dir squelettes> --offload-to-cpu --vae-on-cpu --seed 42` → webm 13 trames. **Poids en RAM (offload) : 11,2 Go** (DiT 7,5 + TE 3,5 + VAE 0,24), VRAM libre pour le workspace. ④ **Perf** : sampling **~157 s/it × 20 = ~52 min** + décode VAE CPU 281 s = **60 min au total** pour 13 trames 832×480 (Q3_K_S offloadé — coûteux ; leviers : quant supérieure résidente si VRAM ok, variantes distillées `QuantStack/Wan2.1_T2V_14B_LightX2V_StepCfgDistill_VACE` 4-8 steps, ou 1.3B si une conversion compatible apparaît). ⑤ **Résultat** (`output/test_vace/` : webm + `planche_vace.png` + trames) : **cohérence temporelle native excellente** (fond et personnage identiques sur les 13 trames, contrairement à la route 1 frame par frame), course fluide 16 fps suivant le squelette ; interprétation « mech chromé » (pas notre chevalier héraldique) → **prochain levier : combiner `-i <image référence>` + `--control-video`** (I2V+VACE = apparence verrouillée par l'image, comme l'exemple amont dance_girl). Statut : **testé, verdict utilisateur en attente** (règle : pas de workflow avant validation).
* **⏸️ CLÔTURE DE SESSION (2026-09-26, stop utilisateur « stop tout ça suffit » après le verdict mouvement OK « pas super net »)** — état pour reprise : ① **Q4_K_S téléchargé** (`C:\Modeles_LLM\Wan2.1_14B_VACE-Q4_K_S.gguf`, 10,55 Go) ; **résident IMPOSSIBLE** sur 16 Go à 832×480×13 : poids 10,06 Go résidents puis OOM au weight-prep du segment 1 (need 1292 Mo / available 1057 Mo — ~240 Mo manquants ; `--max-vram 13` place les poids mais le runtime VACE dépasse) → Q4 ne passe qu'en **offloadé (~67 min)** ; ② **I2V + VACE prêt à retester** : référence chevalier générée (`output/test_vace/ref_chevalier_832x480.png`, txt2img + ControlNet skel_00 + IP-Adapter statique, 832×512) — commande = recette Q3 §ci-dessus + `-i <référence>` + quant Q4 au choix ; ③ pistes netteté non explorées : variante distillée `QuantStack/Wan2.1_T2V_14B_LightX2V_StepCfgDistill_VACE` (4-8 steps), upscale ESRGAN post-hoc du webm (core/upscaler.py), Q3 résident (`--max-vram 10` : 7,84 + réserve 2 < budget) ; ④ exporteur prototype laissé sur `Run`/13 trames (`points_run13.json`). Aucun workflow VACE créé (règle validation).
