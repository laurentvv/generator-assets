# 🔭 Journal de veille — évolutions de la stack

Journal alimenté par la veille (`scripts/veille_versions.py` — relancée automatiquement
à l'ouverture de session si la dernière date de plus de 20 h, via le hook SessionStart).
**But : garder les agents IA au courant** des nouveautés de la stack locale
(audio.cpp, FFmpeg, Python/paquets, modèles GGUF, llama.cpp) pour en tirer
parti dans le projet. Toute nouveauté pertinente y est consignée avec son
impact projet ; les écueils et stacks validées restent dans `MEMORY_BANK.md`.

Format : `AAAA-MM-JJ • source • changement • impact projet / action`

---

- **2026-09-26 (matin, veille lancée en live à la demande + tri « a voir » sur les 3 repos ComfyUI) • sd-cli master-920 signalée (rien pour nous, recommandation d'attente) ; 3 nouveaux repos ComfyUI triés — 1 idée confirmant une piste déjà trackée, 2 éliminés ; 6 tendances HF = LLM uniquement** — ① **Veille live (run manuel)** : tout à jour sauf **sd-cli master-920-2f88688** — 3 commits (`39ada08` support famille PixArt #2047, `19bbbca`/`2f88688` refactors VAE/weights PixArt #2059/#2061) : **rien H3, rien LTX, pas le fix #1976** (Wan I2V toujours bloqué pour video-analys-ia) → même traitement que master-913/917 : refactors VAE = risque mémoire type master-913, bénéfice nul (PixArt hors usage), **recommandation d'attente — entrée conservée en attendant l'accord**. ② **Work-Fisher/ComfyUI-Fisher-Pose (31★, MIT, early-stage, 5 commits)** : studio de pose pour **Qwen Image 2.1** — mannequin 3D basé **MakeHuman** + bibliothèque de 209 squelettes OpenPose + LoRA VNCCS PoseStudio (entraînée poupée→personnage) pour redessiner un personnage dans une pose contrôlée ; pas de ControlNet ni estimateur de pose à l'inférence (profondeur heuristique, 2D seulement). Le nœud lui-même : hors périmètre (Qwen Image 2.1 via ComfyUI/PyTorch, sd-cli ne l'expose pas). **L'idée confirme la piste déjà trackée #9 du rapport ComfyUI 09/09** (IC-LoRA LTX depth/pose/edges) : si besoin un jour de poses cohérentes côté jeu (sprites), notre route = pose MakeHuman → rendu Blender (profondeur/contours) → IC-LoRA LTX via sd-cli — rien de nouveau à tracker. ③ **HECer/ComfyUI-DLSS5 (27★, GPL-3.0, alpha)** : Super Resolution ×2-4 + frame generation via DLSS (VapourSynth + D3D12) — **NVIDIA RTX uniquement** (runtime nvngx, aucun chemin Vulkan), guides depth/flow en PyTorch (Depth Anything V2, RAFT) → **incompatible RX 6950 XT, éliminé**. ④ **piskie-dev/piskie (27★, MIT)** : PAS un nœud ComfyUI — agent desktop Electron d'automatisation navigateur (fingerprints, sessions persistantes) qui n'apparaît dans `topic:comfyui` que pour son intégration WebSocket ComfyUI optionnelle → **hors sujet, éliminé (bruit du topic search)**. ⑤ **hf-modeles-gguf : 6 tendances vérifiées = uniquement des LLM/coder** (google/gemma-4-31B-it, peculiar-ragdoll/Tiel-Coder-35B-A3B-GGUF-MTP, ukisai/Swift-1.5-Qwen3.8-27B, juspay/xor, badtheorylabs/Tinfield-1) — **aucun pipeline text-to-audio, instruction permanente 2026-09-07 non déclenchée**. ⑥ **Purge `maj_en_attente`** : comfyui-nouveaux-repos + hf-modeles-gguf (info triées, verdicts ci-dessus) ; **reste : sd-cli master-920 (accord demandé)**. Reste à jour : audio.cpp hotfix, trellis v0.8.1, ffmpeg 9.0.2, qwentts 6a3e912, llama.cpp v0.5.0, ComfyUI v0.37.0 + 4 repos surveillés, Vulkan SDK/Blender/Godot/uv/CMake, ACE-Step v0.1.8, sa3.cpp.

- **2026-09-26 (nuit du 25 au 26, accord « prépare le test et je reboot avant de le lancé » puis « reboot fait : go ») • trellis.cpp v0.8.1 INSTALLÉE EN PRODUCTION — la régression +51 % de v0.8.0 n'est PAS reproduite (A/B propre : 630 s vs 583 s, +8 %, marge de bruit) ; rendus équivalents** — ① **Protocole** : script idempotent `scripts/proto_ab_trellis_v081.sh` (préparé avant reboot, lancé sur machine fraîche) — 2 jambes STRICTEMENT séquentielles, check_charge avant chacune, même image (casque.png), même résolution 512, **seed 42 des deux côtés** ; v0.8.1 testée en install parallèle **`C:\trellis-081`** (pattern `C:\SD-6b3edaa`, production v0.6.0 intacte pendant le test). ② **Résultats** : v0.6.0 = 583 s (GLB 5,96 Mo, 121 132 sommets) vs **v0.8.1 = 630 s** (GLB 5,99 Mo, 123 210 sommets) = **+8 %, seuil convenu ±15 %** ; rappel : v0.8.0 était à ×1,51 (1193,8 vs 789,1 s). Variance intra-machine trellis notée au passage (v0.6.0 : 583 s aujourd'hui, 644 s le 24/09, 789 s la nuit du 25) — c'est le ratio intra-A/B qui fait foi. ③ **Vérification qualité** : GLB structurellement identiques (1 mesh/1 matériau/2 textures) + **rendus Blender headless comparés** (`output/trellis_ab_v081/rendu_v060.png` / `rendu_v081.png`, script `rendu_controle.py` réutilisable — ⚠️ piège : `blender -b` résout mal les chemins relatifs de sortie, passer des chemins ABSOLUS) : mêmes silhouette/textures, PBR v0.8.1 très légèrement moins saturé, aucun artefact propre à la version. ④ **Upgrade production** : backup `C:\trellis\backups\backup_20260926_v060\` (6 exe/dll) puis copie des binaires v0.8.1, `version.json` → v0.8.1, `--help` OK ; `C:\trellis-081` supprimé (doublon). README `C:\trellis\README.md` à jour. ⑤ **Docs dépôt** : `engines_manifest.json` (pin v0.8.1 windows+linux, sha256 des 2 assets recalculés, raison = verdict A/B), MEMORY_BANK §1.14 (bullet upgrade), script A/B committé. Entrée trellis purgée de `maj_en_attente` (appliquée). Pixal3D + réduction mémoire 1024p désormais dans le binaire production (à explorer au besoin) ; rétopo quad toujours absente du prebuilt Windows (builds sources ; amont : run complet non testé sur Windows). **`maj_en_attente` désormais VIDE.**

- **2026-09-25 (soir, accord « Go : llama.cpp ») • llama.cpp v0.5.0 APPLIQUÉE (0.4.1-dev b10964 → 0.5.0-dev b11146) — smoke Vulkan OK, hors production donc risque nul pour la fabrique** — ① **Spécificité release** : la release versionnée `v0.5.0` ne publie QU'UN asset `nightly-tag.txt` pointeur → binaires réels sur le nightly **`b11146`** (2026-09-23) : asset `llama-b11146-bin-win-vulkan-x64.zip` (30 Mo, 52 fichiers, zip à plat). ② **Installation** : aucun process llama actif ; backup `C:\llama.cpp\backups\backup_0.4.1-dev_b10964_2026-09-25\` (53 exe/dll) puis extraction par-dessus — `llama-cli --version` = **0.5.0-dev (build 11146, commit `7fe450e19`)** = commit exact du tag. ③ **Smoke test réel** (check_charge OK avant) : LFM2.5-8B-A1B Q6_K, `-ngl 99` Vulkan, `-st -n 40` → **241,9 t/s prompt / 197,1 t/s génération**, sortie cohérente. ⚠️ Surface CLI déplacée : `-no-cnv` et `-no-display-prompt` n'existent plus → `--single-turn`/`-st` pour un run one-shot (piège à retenir pour les scripts). ④ **Contenu utile de la release** : perf backends CUDA/Metal (conv2d implicit GEMM #29135, fusions MoE/SSM #28948), **kernels Vulkan nouveaux** (IQ4_XS MMQ/MMV #28415, IQ3_S MMQ #28822, FA Intel Xe #24406, sparse FA #28105, mul_mat_id jusqu'à 512 experts #28501), ggml 0.25.0, serveur multi-adresses #28690, nouveaux modèles HRM-Text/MiMo-V2.6/HunyuanOCR/Qwen4Exp — rien de cassant pour la fabrique (llama.cpp pas en production, usage expérimental/VLM). ⑤ **Docs** : `README.md` de `C:\llama.cpp` reste le README GitHub (ne pas modifier, règle AGENTS.md) ; `engines_manifest.json` INCHANGÉ (llama.cpp en `scan_releases: 8` = latest auto, pas de pin). Entrée purgée de `maj_en_attente` (appliquée). Rollback : backups ci-dessus. **Reste en attente d'accord : trellis.cpp v0.8.1.**

- **2026-09-25 (soir) • veille infos TRIÉES — 4 entrées info purgées (rien d'actionnable) ; uv 0.12.19 appliquée au passage (accord « go uv » puis « oublie uv » — déjà en place au moment du message)** — ① **uv 0.12.17 → 0.12.19** : `uv self update` a une nouvelle fois buté sur `uvx.exe` verrouillé par les serveurs MCP de session (piège connu du 19/09, cette fois 5 uvx + 5 uv dont les serveurs blender-mcp/crawl4ai de la session en cours, pas tuables) → **parade rename sans interruption** : binaires 0.12.19 (GitHub astral-sh, build `bea138450` 2026-09-24) copiés dans `C:\Users\laurent\.local\bin` après rename des anciens en `*.0.12.17.bak` (NTFS autorise le rename d'un exe en cours ; les serveurs MCP finissent leur vie sur l'ancienne image, purge au reboot), `uv --version`/`uvx --version` = 0.12.19, imports `core, workflows` OK. Entrée purgée (appliquée). ② **org-audio-cpp Nemotron-3-Diarization-GGUF** : déjà journalisé le 24/09 (diarisation locuteurs, support moteur arrivé dans audio.cpp v0.8.2 = notre hotfix installé) — pas notre besoin (voix off = TTS scripté, rien à diariser) → purge. ③ **hf-modeles-gguf** : les 2 tendances vérifiées via l'API HF — **IFM/K2-Horizon-MoVA-36B-A4B** = LLM MoE (100 experts, 8/token, 36B-A4B) `text-generation`, arch custom `k2_horizon` avec custom_code, aucun support llama.cpp identifié (pas cité dans les notes v0.5.0) → hors périmètre ; **Qwen/Qwen3.5-9B** = **VLM** `image-text-to-text` (arch `qwen3_5`, déjà en ligne depuis 2026-02, pic de tendance actuel) — intéressant UN JOUR pour l'analyse vidéo (`video-analys-ia`) ou le rewriting de prompts (le moteur PE-T2I de la liste Qwen-Image ci-dessous l'utilise), à condition d'un support llama.cpp `qwen3_5` encore absent de notre v0.4.1 → info, purge. **Aucun pipeline text-to-audio dans la tendance** (instruction permanente 2026-09-07 non déclenchée). ④ **comfyui-nouveaux-repos `wildminder/awesome-qwen-image`** : awesome-list pure (pas de code) autour de **Qwen-Image 2.1** — checkpoints, quants GGUF Unsloth, distillations turbo non interchangeables (Viggle DMD 6-step / PAI PDD 4-step / Pruna DMD 8/5-step), ControlNet-Union 8 conditions, LoRA viewpoint Orbit Alpha — **image seulement, pas de vidéo** ; ne devient pertinent que si on adopte un jour Qwen-Image via sd-cli (support amont présent depuis #1994/master-883, hors de notre pin production) → info, purge. ⑤ **comfyui-ltxvideo PR #567** : merge bot `lt-research-bot` « Automated PR - 2026-09-24 », 1 commit de sync de routine (même pattern que #560/#564/#565) → info, purge. **Restent en attente d'accord (vraies majs) : llama.cpp v0.5.0, trellis.cpp v0.8.1.**

- **2026-09-25 (fin d'après-midi, accord « fait comme tu l'indiques, on finalise ») • LES 5 WORKFLOWS RESTANTS COMBLÉS — couverture catalogue 41/42 exercés en réel ; seul trou restant = video/T2V-33 trames sur master-908, bloqué par le fix amont #1976 (règle de re-test respectée)** — ① **animal_godot ✅** : intrant absent du disque → **conversion GLB→.blend via Blender headless** du `bunny_quaternius.glb` (C:\GIT\roblox\assets3d, 13 actions CharacterArmature|*) → `bunny_packe.blend` → GLB game-ready 815 Ko produit. ⚠️ quirks de chemin (même famille que mesh_ia) : `-d` ignoré (GLB atterrit à la racine du dépôt) et le check « GLB non produit » se trompe de chemin alors que le fichier existe. ② **character3d ✅** : chaîne complète jamais testée depuis son intégration — YuNet + gamut + calques encre + **corps MPFB + rig Mixamo + vêtements + 4 rendus Cycles validés** (`output/test_p1_final/renders/marc_novice_*.png`). ③ **monoplan --4k ✅** : chaîne UHD complète (~35 min, vs ~18 min estimé — ralenti minterpolate 3328×1920 dominant) — upscale IA 65 trames → ralenti 30 fps → zoom → conform : **master h264 3840×2160 @ 30 fps vérifié** (`test_p3_monoplan4k_10.0s_4k.mp4`). ④ **h3_ref2va génération réelle ✅ (38,1 min turbo)** — première vraie génération H3 depuis la validation du 09/09 : source fabriquée pour l'occasion (**master monoplan muet + bed music_bg muxés AAC** — contournement du quirk ffprobe a_audio), 12 trames + WAV de référence extraits, DiT + modèle audio minimax_h3 → **`test_p3_h3.webm` 1,1 Mo vp8+pcm vérifié**. La chaîne H3/recâblage cinéma+process validée en production réelle. ⑤ **video/T2V-33 trames master-908** : non testable — **il manque le fix amont #1976** (règle documentée : re-test à la première release le contenant ; master-917 ne l'a pas). Sous-cas T2V-17 trames déjà vert (24/09). **Couverture finale : 41/42 workflows exercés en réel sur la journée** (35+ verts, échecs tous imputés aux binaires épinglés, 0 régression refactor) ; quirks ouverts à arbitrer : chemins `-d` ignorés (mesh_ia, animal_godot), ffprobe a_audio, `--force-bpm`/`--ambience-type` inertes, crash Wan I2V 0xC0000409 sur 6b3edaa.

- **2026-09-25 (après-midi/soir, accord « grosse phase de test complète ») • COUVERTURE CATALOGUE PORTÉE À ~37/42 WORKFLOWS — 19 workflows supplémentaires verts, 0 régression refactor ; crash moteur Wan I2V découvert sur 6b3edaa (0xC0000409, nouveau, impacte video-analys-ia) ; veille : sd-cli master-917 triée (rien H3, purgée)** — Sorties `output/test_p1_final/` (98 Mo, 90 fichiers) + `output/music_chanson|musique_essence|retrait_voix/`. ① **Famille Flux/2D ✅ (7)** : skybox (env .tres), turnaround3d, spritesheet (+JSON Godot), variations (2 thèmes), pose_control (scène + rig JSON), rpg_portrait (2 émotions + manifeste dialogues), ip_adapter (planche cohérence + palette .tres). ② **2D traitement ✅** : upscale ESRGAN réel (4x-UltraSharp, 1024²), rembg (détourage IA). ③ **Audio ✅ (6)** : chanson 60 s xl-turbo (136 s wall), musique_adn (ADN auto 125 BPM/F majeur depuis la boucle music_bg), musique_essence (SA3 init_audio + HTDemucs stems), audio_ambience (procédural + bus .tres), tts_dialogue (manifeste lip-sync), retrait_voix (CPU forcé). ④ **Personnage ✅ (3)** : outfit sur marc_novice (textures PBR appliquées + rendu studio), makehuman_clothes (bibliothèque MPFB + scène .blend), character_makeup --makeup-only (calque encre + yeux). asset_blendkit : recherche anonyme --list-assets ✅ (téléchargement prop = compte requis, hors scope). ⑤ **⚠️ CRASH MOTEUR Wan I2V sur 6b3edaa** : `-w video -i <img>` 17 trames 14B → **0xC0000409 (fast-fail Windows) en fin de sampling (25/25 étapes, 124 s/it)** — les matrices 23-24/09 validaient le Wan uniquement en T2V ; **le mode I2V (pattern exact du consommateur video-analys-ia) n'avait jamais été testé sur ce binaire épinglé**. Commande et plomberie correctes (flags complets, EngineError propre) → crash binaire, pas refactor. **À creuser : tester I2V sur master-908 (T2V 17 trames y est vert) avant le prochain run video-analys-ia.** ⑥ **Couverture finale** : ~37/42 workflows exercés en réel sur la journée ; restent hors test : animal_godot (pas de pack .blend sur disque), character3d (classe héritée de character_makeup, run() partagé), h3_ref2va génération réelle (38 min — dry-run câblage ✅), monoplan --4k (base 1080p ✅, chemin 4K validé 12/09). ⑦ **Veille (run frais 14:39)** : **sd-cli master-917-4c3cf75 sortie** — 4 commits (Qwen Image VAE/LoRA, serveur pnpm, safetensors shards), **rien H3 ni mémoire LTX → purgée de maj_en_attente** (règle : re-test seulement à un vrai fix H3) ; 403 rate-limit comfyui-nouveaux-repos (écueil connu) ; restent 7 entrées info (llama.cpp v0.5.0, uv 0.12.19, org-audio-cpp Nemotron-3-Diar, GGUF tendance, comfyui).

- **2026-09-25 (après-midi, accord « Grosse phase de test : GO ») • CAMPAGNE DE TESTS MOTEURS RÉELS post-refactor : 15 workflows testés, 14 ✅ — le câblage run_engine et les PARAMETRES validés bout en bout ; video/Wan et monoplan/master-908 échouent moteur (régression #1976 connue, production 6b3edaa inchangée) ; 2 quirks préexistants découverts** — Sorties `output/test_p1_final/` (+ `output/mesh_ia/test_p1_meshia/`), seed 42. ① **Chaîne cinéma complète ✅** : monoplan_ia 65 trames → master h264 1920×1080 @ 24 fps, 9,83 s — les 12 sites ffmpeg/sd-cli recâblés de cinema.py éprouvés (amorce, LTX, ralenti minterpolate, zoom, assemblage). **⚠️ 3 OOM consécutifs (`ErrorOutOfDeviceMemory` au submit ~135 s) avec le binaire par défaut C:\SD (master-908)**, dont une fois à cause d'une collision Flux concurrente (voir ⑤) — **succès immédiat via `SD_CLI_PATH='C:/SD-6b3edaa/sd-cli.exe'`** : confirmation que le monoplan 65 trames DOIT rester sur le binaire production épinglé (régression mémoire LTX #1976 ; la matrice 24/09 ne couvrait que 33 trames). ② **3D ✅** : mesh_ia 512 (TRELLIS 12 min 37 s, GLB 5,7 Mo, rendus de contrôle Blender recâblés), mesh3d cube, material3d (pack PBR complet). ③ **Audio ✅** : sfx procédural + sfx IA SA3, voix_off voxcpm2 (−16,1 LUFS ; qwen3 exige --voix-ref, attendu), voix_robot Kokoro (scratch build-357), audio_upscale 16 kHz→48 kHz (sonde ffprobe + UniverSR CPU + MP3 — les logs montrent le tracé `▶️ [etiquette]` de run_engine), music_bg (smoke matin). ④ **2D/batch ✅** : pixelart, flowmap, voxel3d (Blender CPU), ui_9slice, autotile_pack (images biomes), batch 2/2 exit 0 (recette JSON → flowmap + pixelart avec génération Flux). ⑤ **Comportements d'échec validés en conditions réelles** : EngineError remonte commande complète + code retour (batch attrapé, lot arrêté proprement à la 1re erreur) ; collision GPU Flux↔LTX vécue → sd-cli refuse, aucune corruption ; **règle réaffirmée : jamais 2 jobs GPU en parallèle, et check_charge avant chaque lancement**. ⑥ **video `-w video` (Wan 14B défaut) : échec moteur à 15 s sur master-908** — régression #1976 (production vidéo = 6b3edaa), le chemin diffusion n'a pas été retouché par le refactor et LTX 33 trames était vert le 24/09 sur machine propre → non bloquant, à re-tester sur machine rebootée si besoin. ⑦ **Deux quirks PRÉEXISTANTS découverts (à décider, non corrigés)** : (a) `h3_ref2va._duree_et_audio` détecte une piste audio sur vidéo muette — ffprobe renvoie rc=0 même sans flux (`-select_streams a` vide), vieux code et câblage neuf identiques ; ne surface que si on feed une vidéo sans audio (production H3 = chunks avec audio) ; fix = parser stdout non vide. (b) `mesh_ia` ignore `-d/--output-dir` (chemin codé en dur `output/mesh_ia/<nom>/`, présent depuis 222890d). ⑧ **h3_ref2va --dry-run ✅** : probes ffprobe + extraction 12 trames + WAV de référence via run_engine (l'échec d'extraction audio ci-dessus est le quirk (a), pas le câblage). Bilan : **aucune régression du refactor détectée** ; les 2 échecs sont moteurs (binaires épinglés) et les 2 quirks préexistaient.

- **2026-09-25 (après-midi, accord « Finalise le plan puis test et push ») • PLAN D'AUDIT 2026-09-24 FINALISÉ — §2.2 keystone terminé (88 flags migrés + gel de surface), §2.4 achevé (run_engine sur 100 % des moteurs, timeouts partout), §2.5 P2 fait (ruff élargi E4/E7/E9/F/W, 464→0 alertes) — smokes réels image/SFX/musique OK, poussé sur main** — ① **§2.2 (5 commits `2df654c`→`5048781`)** : gel de la surface CLI (`tests/surface_cli.json`, 152 options — toute dérive de dest/défaut/type casse le test ; procédure de régénération documentée dans la docstring) puis migration des 4 familles restantes en `PARAMETRES` déclarés : audio (30 flags/10 workflows — `--duration` hébergé par sfx, `--moteur`/`--variante`/`--music-backend`/`--tonalite`/`--lyrics` par music_bg, `--langue` par chanson), vidéo/3D (12 flags), personnage (13 flags — **fix au passage : `Character3DWorkflow` hérite de `CharacterMakeupWorkflow` (même objet PARAMETRES) → `parametres_declares()` dédoublonne par identité, sinon argparse refuse `--portrait`**), 2D (33 flags/15 workflows). La table plate ne garde que les 9 options cross-familles (`--factor --mode --columns --frames --emotions --fps --samples --width --height`, groupe « Options partagées entre familles »). ② **Deux flags inertes documentés, NON corrigés (changement de comportement à valider)** : `--force-bpm` expose dest `force_bpm` mais music_bg lit `bpm_force` (inerte depuis toujours) ; `--ambience-type` lu nulle part (audio_ambience lit `flow_type`/`vfx_type`). ③ **§2.4 suite (`4fc3596`)** : 24 sites subprocess nus supplémentaires câblés sur `run_engine` (cinema 12 dont la génération sd-cli LTX, music_ai 7 avec boucles de retry passées à `except EngineError`, voix_off 5, mpfb_ops 4, mesh_ia 3, h3_ref2va 5, separation 3, audio_upscale 3, sfx_ia/voix_robot 2+2, music_essence/animal_godot/voxel_ops/blendkit/llm/outfit 1 chacun) — **100 % des appels moteurs de core/ + workflows passent désormais par le helper, timeout systématique dimensionné sur les repères MEMORY_BANK (7200 s générations lourdes, 120-1800 s ffmpeg)** ; `process.py` force l'UTF-8 uniforme ; sites streaming restent `capture=False`, sites à marqueur SUCCESS restent `check=False` (dégradation gracieuse mesh_ia préservée). ④ **§2.5 P2 (`7e889d3`)** : ruff 464→0 alertes (360 auto-fix dont 151 F401, 33 manuels : 21 E741 renommés `ligne`/`lora`/`gauche`/`lp`, 16 F841 variables mortes — appels à effet de bord conservés) puis gate élargie **E4/E7/E9/F/W** (E501 exclu volontairement ; per-file-ignores E402 pour bootstraps délibérés main.py/scripts/clothes_catalog ; `.agents/` exclu = scripts vendus des skills). ⑤ **Tests** : 57 verts (gagnent test gel surface + 4 tests familles + dédoublonnage héritage) ; **smokes réels machine libre** : generate Flux ✅ (`output/smoke_p1_image.png`), sfx procédural ✅, music_bg ace_step 12 s ✅ — bed −30 LUFS, chaîne complète audiocpp+ffmpeg via run_engine (`output/music_bg/smoke_p1_bed*`). Plan d'audit 24/09 : **P0 + P1 entièrement clos** ; restent hors plan : `--force-bpm`/`--ambience-type` (décision utilisateur), A/B trellis v0.8.0 (re-test à la prochaine release amont), hygiène profonde vulture/duplication (rapport santé 03/09, jamais au plan).

- **2026-09-25 (matin, accord « go ») • audio.cpp hotfix INSTALLÉ (crash ace_step corrigé) ; trellis ROLLBACK v0.6.0 (v0.8.0 À RETESTER) ; sd-cli master-913 REPORTÉE (rien pour nous, risque mémoire)** — ① **audio.cpp v0.8.2-audio8-perf-hotfix installée** : le crash ace_step de v0.8.2 pure (`STATUS_ILLEGAL_INSTRUCTION` au chargement) est **CORRIGÉ** — smoke 12 s turbo OK (RTF 3,82/3,53), sfx stable_audio OK, aucun changement ace_step côté changelog (le commit réparateur est entre v0.8.2 et le hotfix, non identifié). **A/B même jour vs v0.8.1** : moyennes 3,68 vs 3,32 (+11 %, marge de bruit ace_step du poste — v0.8.1 faisait 3,25-3,85 intra-jour le 24/09) → pas de régression nette, conservée (gains TTS/CUDA : BS-RoFormer #666, higgs #669, MOSS mémoire, Pascal CUDA #682, metrics mémoire #688). Windows épinglé hotfix dans `engines_manifest.json` (raison), README `C:\audio-cpp\README.md` + MEMORY_BANK §1.11 à jour. **Leçon : le rollback du 24/09 n'avait pas restauré `version.json` (encore v0.8.2) → la veille a cru v0.8.2 installée ; toujours restaurer binaires + version.json ensemble.** Entrée purgée (appliquée). ② **trellis.cpp : ROLLBACK v0.6.0 appliqué** (suite à la régression +51 % confirmée par A/B propre de la nuit) — backup des binaires v0.8.0 dans `C:\trellis\backups\backup_20260925_v080\`, restauration v0.6.0 depuis `backup_20260924_v060/`, smoke fonctionnel post-rollback OK (GLB 5,3 Mo ; mesure 1028 s non propre — utilisateur actif, la référence perf reste l'A/B de la nuit). **⚠️ v0.8.0 À RETESTER (demande utilisateur du 25/09)** : à la prochaine release amont (un fix de perf suffira à refaire l'A/B) ou sur demande — binaires et README prêts (Pixal3D + mémoire 1024p à découvrir). ③ **sd-cli master-913-b167b94 REPORTÉE** : 5 commits (cfg guidance #2033, upscale endpoint serveur #2026, Qwen cache #2045, **headroom monolithique 128 MiB #2046**, Qwen flow schedule #2048) — aucun fix H3 (régression #1976 : 54 segments sur 908, toujours ouverte), Qwen/serveur hors usage, et #2046 touche la zone du capacity check là où LTX n'a que ~120 Mo de marge → gains nuls attendus, risque mémoire possible, matrice de 40 min non justifiée. **Déclencheur de re-test : la première release contenant un vrai fix H3** (ou sur demande). Entrée purgée (décision d'attente consignée ici). ④ **Automatisation 2 h supprimée** (A/B trellis fait et consigné — runs no-op depuis le marqueur). ⑤ Restent en attente : llama.cpp v0.5.0 (info, jamais sans accord) + entrées info (org-audio-cpp, hf-modeles-gguf, comfyui, uv, comfyui-ltxvideo).

- **2026-09-24 (23h30, post-reboot, tests demandés par l'utilisateur) • Tests de non-régression post-refactoring : image ✅, SFX ✅, musique ✅, vidéo LTX ✅ sur machine propre ; A/B trellis TOUJOURS en attente (interrompu 2ᵉ fois — machine reprise par l'utilisateur)** — Sorties dans `output/test_refactor/` (seed 42 partout, `OUTPUT_DIR` redirigé hors `godot_assets/`) : ① **generate** Flux 1024² → export 512² RGBA ✅ (première tentative de la veille échouée — `audiocpp_cli` tiers en cours depuis 20h30 universr, 7,5/16 Go VRAM : le « available 123 MB » du capacity check sd-cli était un vrai refus de contention, pas un bug de refactoring — règle `check_charge_systeme` validée une fois de plus) ; ② **sfx** audio.cpp v0.8.1 ✅ RTF 1,04, WAV+OGG ; ③ **music_bg** ace_step turbo 12 s ✅ (boucle+OGG dans `output/music_bg/`) ; ④ **monoplan_ia** LTX-2.5 65 trames via production `C:\SD-6b3edaa` ✅ — master h264 1920×1080 @ 24 fps, 9,8 s, 11 Mo (pièche bash : `SD_CLI_PATH='C:/...'` avec slashes et quotes — les backslashes non quotés sont avalés). ⑤ **A/B trellis v0.8.0 vs v0.6.0** : lancé à 23:14 sur machine propre, interrompu à la demande (utilisateur reprend CPU+GPU) — **à relancer en créneau libre** (commande : `cd /c/trellis && time ./trellis-cli.exe -i godot_assets/casque.png -o … --res 512` puis idem depuis `backups/backup_20260924_v060/`) ; baseline v0.6.0 = 644 s. La chaîne monoplan (cinema.py) garde ses `subprocess.run` nus (sans timeout) — câblage `run_engine` restant (avec mpfb_ops, animal_godot, FFmpeg).

- **2026-09-24 (soir, accord utilisateur « lance les maj applicatif ») • 3 majs appliquées d'un coup — audio.cpp v0.8.2 REJETÉE (régression ace_step, retour v0.8.1) ; trellis.cpp v0.6.0 → v0.8.0 (smoke OK, verdict perf reporté post-reboot) ; qwentts.cpp 0cbde9b → 6a3e912** — ① **audio.cpp** : `update.ps1 -Yes` → v0.8.2 (4d88768f, vague TTS/ASR : ZipVoice, Confucius4-R2T2, AuK, Piper/KittenTTS, MOSS-TTS-v1.5, Nemotron-3-diar, vieneu_v3_turbo, LiveAvatar, Wan S2V — rien ace_step). **Smoke ace_step 12 s turbo : crash `STATUS_ILLEGAL_INSTRUCTION` (0xC000001D) au chargement** (`runtime.model.variant acestep-v15-turbo`), vulkan ET cpu, CLI direct ET `core.music_ai`. Discriminant : même commande/GGUF sur binaires v0.8.1 (backup extrait à part) → OK (RTF 3,85) → **rollback** depuis `backup_v0.8.1_20260924_172826.zip` → smoke revalidé (RTF 3,25). Suspicion : ggml lowering APIs (#532/#533) / quantization parallèle (#649). Aucune issue amont sur le sujet au 24/09. Windows épinglé **v0.8.1** dans `engines_manifest.json` (raison consignée) ; retenter à partir de v0.8.3 en vérifiant les issues. Détails : `C:\audio-cpp\README.md` + MEMORY_BANK §1.11. ② **trellis.cpp** : backup `C:\trellis\backups\backup_20260924_v060\` puis extraction v0.8.0 (`version.json` à jour) — Pixal3D (`--model pixal3d` + `--fov`, MoGe-2 non porté), mémoire réduite 1024p, `--dump-post`, GGML bump ; **`--retopo` Linux-only, hors scope Windows prébuilt**. **Smoke fonctionnel 512 OK** (casque.png → GLB texturé complet, 144 k faces, atlas 1024) en **1149 s vs baseline 644 s** — mesure POLLUÉE par la charge de session (accord utilisateur : « la perf qui baisse c normal, test = PC reboot ») → **verdict de régression reporté à un A/B propre post-reboot** (binaires v0.6.0 conservés dans le backup ; 1ʳᵉ A/B v0.6.0 arrêtée en cours pour libérer le GPU utilisé par l'utilisateur). README `C:\trellis\README.md` à jour. ③ **qwentts.cpp** : `manage_qwentts.py --update` → `0cbde9b..6a3e912` (2 commits : `c4c99cb` « ggml: fork update », `6a3e912` « docker: build portable CPU backend variants » — infra/build, rien de fonctionnel TTS), build Vulkan OK, **smoke 9,0 s OK** (WAV 142 Ko), modèles vérifiés, 5 sauvegardes (`backup_20260924_181311_0cbde9b`). Entrées `maj_en_attente` purgées (3 appliquées/rejetées) ; **reste en attente : llama.cpp v0.5.0 (info)**, entrées info (org-audio-cpp, hf-modeles-gguf, comfyui). ④ Même session : **fix P0 du plan d'audit poussé** (`6adf242` — contrat CLI des consommateurs : défauts workflow-specifiques à None, menu 33-35, temporaires tempfile, batch exit code + `--continue-on-error`, `tests/test_cli_contract.py` 10 tests) puis **chunk P1** : découpage `main.py` (1130 → ~140 lignes) vers `cli/parser.py` + `cli/interactive.py` + `cli/maintenance.py`, temporaires Blender (`temp_blender_*.py`, `_reduction.py`) passés à `tempfile`.

- **2026-09-24 (matin, accord utilisateur « reboot ok » pour la matrice convenue) • sd-cli • master-908 INSTALLÉE et gardée en pin — le fix H3 espéré (#1900) ne corrige PAS la régression : 54 segments au lieu de 51, sain = 2 ; les 3 autres jambes stables ou meilleures** — ① Installation : zip téléchargé la veille, backup `C:\SD\backups\backup_20260924_master908\` (binaires 28b454b = 899) puis extraction `sd-master-88411ef-bin-win-vulkan-x64.zip` par-dessus `C:\SD\`, `--version` = 88411ef. ② **Matrice post-reboot** (bureau 0,4 Go, conditions identiques à 899 la veille) : **Flux 1024² 4 steps ✅ 27,00 s** (= 26,98 s) ; **LTX 33 trames ✅ 54,21 s** (= 53,75 s, +0,9 %, 1 segment, aucun refus capacity check, même WARN pinned non fatal) ; **Wan 17 trames ✅ 1999,5 s** (vs 2023 s = −1,2 %, 1 segment résident 9211,74 Mo, décode VAE CPU 209,5 s) ; **H3 turbo ❌ — `minimax_h3 using 54 segments` (sain : 2) + `ErrorOutOfDeviceMemory` au submit à 257,37 s** : le commit **#1900 « add graph cuts for MiniMax-H3 text conditioning »** a bien MODIFIÉ le découpage H3 (51→54 segments) mais ne restaure pas le plan sain 2 segments — **la régression #1976 reste ouverte de master-864 à master-908** (issue toujours sans réponse amont). ③ **Décision (pin)** : 908 gardée — aucune jambe régressée, gains tangibles (conditioning cache #2034, fix alpha upscale ESRGAN RGBA #2029, perf Wan VAE #2038, préparation Qwen Image 2.1) ; **production LTX/H3 INCHANGÉE via `C:\SD-6b3edaa`** ; rollback : `backups/backup_20260924_master908/`. ④ Docs mises à jour : `C:\SD\README.md` (release + encadré), MEMORY_BANK §1.17, SKILL.md + `references/catalogue_workflows.md` (bloc écueils : master-908, H3 seul bloquant, LTX marge fragile), `scripts/engines_manifest.json` (pin 88411ef sur les 3 plateformes, JSON validé). Entrée `maj_en_attente` purgée (appliquée). Logs `output/test_m908/` (Flux/LTX/Wan + webm, H3). **Datapoint 54-segments posté en commentaire de l'issue #1976 le jour même (accord utilisateur « oui envoie l'info ») : [issuecomment-5813245104](https://github.com/leejet/stable-diffusion.cpp/issues/1976#issuecomment-5813245104)** — texte EN (tableau 841/864-899/908, preuve que #1900 modifie le cut H3 51→54 sans le corriger, OOM 257 s, offre de tester tout fix candidat) ; source conservée `output/test_m908/issue1976_comment_908.md`.

- **2026-09-24 (09h33, veille arrière-plan session) • 8 nouveautés — sd-cli master-908 : PREMIER FIX AMONT H3 (PR #1900 « graph cuts ») depuis notre régression → recommandation GO/test ; audio.cpp v0.8.2 (vague TTS/ASR, rien ace_step) ; trellis.cpp v0.8.0 (Pixal3D + rétopo quad Linux-only) ; llama.cpp v0.5.0 (info)** — ① **sd-cli master-908-88411ef** (2026-09-23, 9 commits depuis 28b454b = 899) : **`3674693 fix: add graph cuts for MiniMax-H3 text conditioning (#1900)`** — première intervention amont sur le plan de graphe H3 depuis la régression #1976 (51 segments, identique 864→899) ; + `e6281b6` conditioning cache configurable tous modèles (#2034, perf potentielle), `70c1dbc` perf Wan VAE conv 2D (#2038), `c92d73c` fix alpha lors d'upscaler ESRGAN RGBA (#2029), `241518b`/`2dc7f54` Qwen Image 2.1 (latent2rgba preview + prefix KV cache). Issue #1976 toujours sans réponse amont (seul notre datapoint 899 du 23/09 en commentaire). **Recommandation : GO/test master-908 (matrice standard sur machine rebootée : Flux, LTX 33 trames, Wan 17 trames, H3 turbo) — en attente d'accord.** ② **audio.cpp v0.8.2** (~45 PRs) : grosse vague TTS/ASR — ZipVoice (zh/en), Piper + KittenTTS community, MOSS-TTS v1.5, Vieneu v3 turbo, **AuK Base/Flash** (Tencent : TTS instruction + édition audio par instruction, category `tts`, tags CUDA-only, expérimental — pas de la musique, règle test musique non déclenchée), Nemotron 3 diarization (PR #660 = le nouveau repo org vu en veille), speech speed serveur/TTS, Yue2 : NAR tiling « 4 GiB » + continuation depuis tokens sémantiques (Yue2 reste rejeté §1.22). **Rien sur ace_step/minimax_music3 (nos familles validées) → maj non prioritaire, pas de test à lancer.** ③ **trellis.cpp v0.8.0** (saut v0.6.0 → v0.8.0) : nouvelle famille **Pixal3D** image→3D (`--model pixal3d`, 5 GGUF partageables avec les décodeurs TRELLIS.2, NAF upsampler, caméra `--fov` explicite car MoGe-2 non porté) ; **rétopo quad native** `--retopo` / `--retopo-dual-pbr` mais **Linux-only (LEMON+CGAL, absentes des archives prébuild Windows → pas pour nous en l'état)** ; mémoire réduite pour les générations 1024 (notre 1024 ≈ 55 min) ; Metal macOS/ARM64. Maj candidate au prochain besoin 3D (pin v0.6.0 inchangé d'ici là). ④ **llama.cpp v0.5.0** (2026-09-23) : perf backends CUDA/Metal (conv2d implicit GEMM, MoE/SSM fusion), serveur multi-adresses, nouveaux modèles HRM-Text/MiMo-V2.6/HunyuanOCR/Qwen4Exp — info, pas en production dans la fabrique. ⑤ **qwentts.cpp** amont `0cbde9b` → `c4c99cb` : 1 seul commit `ggml: fork update` (bump sous-module, build/infra — même pattern que les majs acceptées des 20-22/09) — en attente d'accord, priorité basse. ⑥ Org audio-cpp : repo **Nemotron-3-Diarization-GGUF** (diarisation locuteurs, support moteur ajouté en v0.8.2) — info, pas notre besoin actuel. ⑦ 12 GGUF tendance HF, tous LLM/VLM (halogen-qwen3.8, Qwen3.8-27B-Heretic, apple/LensVLM-9B, ThinkingCap…), **aucun pipeline text-to-audio** → pas d'action. ⑧ 4 nouveaux repos ComfyUI dont **supElement/ComfyUI_MinimaxH3_AutoContext (31★)** — auto-context H3 côté ComfyUI (idée à évaluer, pas notre moteur) ; AmazingDraw (50★), ComfyUI-Anima-2.9B-loraPatch (34★), tamanitomo (31★).

- **2026-09-23 (après-midi/soir) • sd-cli • master-899 INSTALLÉE et gardée en pin (accord « Oui go ») — matrice complète passée : H3 TOUJOURS cassé (signature identique 864), LTX passe sur machine propre, Wan −4 %** — ① Installation : backup `C:\SD\backups\backup_20260923_master899\` (binaires ca37fad) puis extraction `sd-master-28b454b-bin-win-vulkan-x64.zip` par-dessus `C:\SD\`, `--version` = 28b454b. ② **Matrice pré-reboot** (bureau 0,8 Go VRAM) : Flux 1024² 4 steps ✅ ; **LTX 33 trames ❌ à 21,5 s** — `ltxav workspace capacity check` need 908,75 Mo / available 787,26 Mo (**marge de 121 Mo seulement**) → stop du batch, **reboot utilisateur** sur suggestion (l'analyse montrait ~1,2 Go de VRAM détenus hors sd-cli, dont 0,8 Go de bureau). ③ **Matrice post-reboot** (bureau 0,4 Go) : Flux ✅ ; **LTX 33 trames ✅** (53,75 s, webm OK — WARN non fatal pinned memory) ; **Wan 17 trames ✅** (sampling 2023 s vs 2116 s sur 864 = −4,4 %, webm OK) ; **H3 turbo ❌** — `minimax_h3 using 51 segments` (sain : 2) + `ErrorOutOfDeviceMemory` au submit à 276,68 s, **signature identique à 864/866 : #2020 n'améliore que le reporting mémoire, le plan de graphe H3 est inchangé (issue #1976 toujours ouverte)**. ④ **Décision (pin)** : 899 gardée — ≥ 864 sur toutes les jambes mesurées, aucun chemin production régressé ; question posée à l'utilisateur (garder vs rollback), sans réponse → application de la recommandation (garder), rollback facile : `backups/backup_20260923_master899/`. **Production LTX/H3 INCHANGÉE via `C:\SD-6b3edaa`** (LTX sur 899 = marge ~120 Mo fragile ; I2V 65 trames d'`monoplan_ia` non retesté). ⑤ **Leçon** : le verdict LTX dépend de la VRAM du bureau → **toute matrice sd-cli sur machine fraîchement rebootée** (les « chiffres identiques entre builds » du 14/09 = artefact machine pour LTX, vrai comportement build pour H3). ⑥ Docs mises à jour : `C:\SD\README.md` (release + encadré), MEMORY_BANK §1.17, SKILL.md (bloc écueils : master-899, H3 seul bloquant, règle reboot), `scripts/engines_manifest.json` (pin 899 sur les 3 plateformes, JSON validé). Entrée `maj_en_attente` purgée (appliquée). Logs `output/test_m899/` (+ `ltx33_m899_reboot.log`, `wan_t2v_m899_17f.log/webm`, `h3_turbo_m899.log`). **Datapoint 899 posté en commentaire de l'issue #1976 le jour même (accord utilisateur) : [issuecomment-5795779449](https://github.com/leejet/stable-diffusion.cpp/issues/1976#issuecomment-5795779449)** — texte EN (tableau 841/864-866/899, LTX marge ~121 Mo + under-reporting 319→787 Mo réduit par #2020, H3 51 segments inchangé, offre de bisect) ; source conservée `output/test_m899/issue1976_comment.md`.

- **2026-09-23 (13h33, veille arrière-plan session) • 7 nouveautés — sd-cli master-899 : LA release attendue avec le fix #2020 enfin publié → recommandation GO/test ; Yue2 variante ios-q4_0 (info, déjà rejeté §1.22) ; 6 entrées info purgées** — ① **sd-cli master-899-28b454b** (2026-09-22, asset `sd-master-<sha>-bin-win-vulkan-x64.zip`) : compare `74988b2` (master-890 ignorée) → `28b454b` = **9 commits**, dont les 8 déjà relevés sur master hier 18h05 — en tête **#2020 « fix: handle GPU memory reports and LLM encoding failures »** = le fix de l'issue #1989 (model manager « available 0.00 MB » → rejet des poids au weight prep), **exactement le mécanisme de notre régression LTX-2.5 + H3 en 16 Go Vulkan depuis master-864** ; #2019 VAE tiling retries restreints aux échecs d'allocation ; #1966 cache text conditioning H3 (serveur text-only, zéro bénéfice h3_ref2va --turbo) ; #2018 ggml permute metadata truncation ; 3 fixes images de référence serveur/Qwen 2.1 (#2007, #2011, #2025) — + **1 NOUVEAU depuis hier : #2028 « feat: add configurable image input preprocessing »**. **Conforme à l'action du 22/09 (« quand master-89x publiera ces commits, ne PAS l'ignorer par défaut ») → recommandation GO** : installer l'asset (sauvegarde `C:\SD\backups\` avant), smoke Flux steps 4 + `--version`, puis **matrice complète Flux 1024² + Wan 17 trames + LTX 33 trames + H3 turbo** (exigence §1.16/§1.17) ; si la régression est levée → retrait du contournement `C:\SD-6b3edaa` (et du bloc écueils du SKILL.md), sinon rollback. En attente d'accord utilisateur, entrée conservée dans `maj_en_attente`. ② **org audio-cpp : nouveau fichier `Yue2-3B-GGUF/yue2-3b-ios-q4_0.gguf`** (repo modifié 2026-09-23 00h04) — variante q4_0 « ios » (mémoire réduite) d'un modèle **déjà testé le 13/09 et rejeté** (verdict « encore du POP », cible gothique CLOSE §1.22 ; q8_0 + vae-f16 déjà en local) ; ni nouvelle famille ni levier anti-pop (quant plus petite = qualité ↓) → info, pas de re-test. ③ **hf-modeles-gguf** : 9 nouveaux en tendance, **aucun text-to-audio** — LLM (zai-org/GLM-5.3 dl 1,08 M ♥1909, Sharp-Spark-X2.5-4B, limite-1b-violetto, Ternary-Bonsai-2-27B-Abliterated) + `ntc-ai/yue2-particle-sliders` (sliders LoRA yue2 — famille rejetée §1.22) → info, purgée. ④ **sa3.cpp** : PR #49 mergée (`feature/ggml-check-cuda`) — suite des PR test-infra #47/#48 d'hier, toujours pas installé (famille stable_audio couverte par audio.cpp) → info, purgée. ⑤ **uv 0.12.18** dispo (0.12.17 installé) — patch, `uv self update` si accord, jamais de maj auto → purgée. ⑥ **comfyui-ltxvideo** : PR #565 merge (bump auto routine) → info, purgée. ⑦ **comfyui-nouveaux-repos** : nrodriguez1997/lora-docker-aliyun-pipeline (55★) — pipeline Docker LoRA côté ComfyUI/PyTorch, rien d'actionnable en CLI → info, purgée. **Reste à jour** : audio.cpp v0.8.1, qwentts.cpp 0cbde9b, trellis.cpp v0.6.0 (30 GGUF TRELLIS.2 inchangés), FFmpeg 9.0.2, llama.cpp v0.4.1, Vulkan SDK 1.4.357.0, Blender 5.2.2, Godot 4.7.2, CMake 4.4.3, paquets Python à jour, ACE-Step v0.1.8, ComfyUI v0.37.0. **maj_en_attente : seule sd-cli master-899 conservée** ; **pin master-864-ca37fad + contournement `C:\SD-6b3edaa` inchangés** en attendant le verdict.

- **2026-09-22 (18h05, relevé manuel) • sd-cli • TOUJOURS RIEN pour nous en release (#1976 muette depuis le 16/09), MAIS signal positif sur master : 8 commits inédits dont 2 dans la zone mémoire/weight-budget qui nous casse → tester la PROCHAINE release** — master-890 reste la dernière release ; `74988b2...master` = 8 commits non publiés, dont : **#2020 « handle GPU memory reports and LLM encoding failures »** — corrige l'issue #1989 (model manager qui évalue « available 0.00 MB » sur GPU discret → rejet des poids dès le weight prep ; fix = rejet free-memory underflow restreint à Vulkan + logs mémoire aux échecs de capacity check) : c'est EXACTEMENT le mécanisme (weight-budget/capacity check du model manager) qui nous casse LTX-2.5 + H3 en 16 Go Vulkan depuis master-864 — pas notre fix, mais preuve que leejet retravaille activement cette zone ; **#2019** retries VAE tiling restreints aux échecs d'allocation/mémoire (fix #2015 — sémantique de retry, pas notre régression) ; **#1966 cache du text conditioning H3** — only text-only/sans LoRA/sans réf, mode serveur → **zéro bénéfice pour h3_ref2va --turbo** (LoRA + refs). Les 5 autres = fixes images de référence serveur/Qwen 2.1 + ggml permute. **Action : quand master-89x publiera ces commits, ne PAS l'ignorer par défaut — passer la matrice LTX 33 trames + H3 turbo sur le binaire de release (le contournement `C:\SD-6b3edaa` reste en place tant que la régression n'est pas levée).** #1976 : 0 commentaire, 0 réaction, inchangée depuis le 2026-09-16. Pin master-864 + contournement inchangés.

- **2026-09-22 (17h36) • qwentts.cpp • maj appliquée `0bfb923` → `0cbde9b` (accord « go »)** — 2 commits amont : `4eb8c7d` « tts-server: a request may set its language, an unknown one is a 400 » (validation de la langue côté serveur OpenAI — `src/qwen.cpp` +26 lignes, `src/tts-server.cpp`/`.h` ; 1er changement fonctionnel depuis la maj d'hier) + `0cbde9b` « ggml: fork update » (bump sous-module). Vérif préalable : aucun process audio/ffmpeg/qwentts. Via `manage_qwentts.py --update` : backup `backup_20260922_173608_0bfb923`, build Vulkan 27 s, **smoke test 5,70 s RÉUSSI** (WAV 122 924 octets), `--check` vert (git à jour origin/master, binaires compilés 17h36, 4 modèles conformes ~6,16 Go, sauvegardes OK). Entrée purgée de `maj_en_attente` (appliquée). Détail détection dans l'entrée 17h30 ci-dessous.

- **2026-09-22 (17h30, veille lancée en session) • 5 nouveautés — qwentts.cpp 2 commits amont en attente d'accord, stable-audio-open-small en tendance HF mais SANS GGUF ni support moteur (info), sa3.cpp 2 PR test-infra, ComfyUI LTXVideo bump auto + 4 nouveaux repos** — ① **qwentts.cpp** : amont `0bfb923` → `0cbde9b` = 2 commits : `4eb8c7d` « tts-server: a request may set its language, an unknown one is a 400 » (validation de la langue côté serveur OpenAI — 1er changement fonctionnel depuis la maj d'hier, pertinent pour ai-doc2video si on fixe la langue des requêtes) + `0cbde9b` « ggml: fork update » (bump sous-module). En attente d'accord utilisateur, entrée conservée dans `maj_en_attente` (procédure `manage_qwentts.py --update`). ② **hf-modeles-gguf** : 5 nouveaux en tendance = 4 LLM (AikidoSec/altar-1, incoai/Qwen3.8-27B-Splash, peonist-ai/halogen-qwen3.8-flash-next, peculiar-ragdoll/Cyber-Tiel-Coder-35B-A3B-GGUF-MTP — rien pour nos moteurs) + 1 audio : **stabilityai/stable-audio-open-small** (dl 1807, ♥285, pipeline text-to-audio) — **NON ACTIONNABLE** : le repo officiel ne contient AUCUN GGUF (base_model.ckpt + model.safetensors seulement), c'est de la famille stable-audio-tools (lignée Stable Audio **Open**) alors que la famille `stable_audio` d'audio.cpp ne couvre que Stable Audio **3** medium/small (9 paquets dans `model_specs/stable_audio.json`, aucun « open ») → il faudrait un port type sa3.cpp ; l'instruction permanente du 2026-09-07 ne se déclenche pas (modèle pas « potentiellement compatible »). ③ **sa3.cpp** : 2 PR mergées aujourd'hui (#47 ggml-check, #48 portable-test-rng) + commits « gradient test portable » — 100 % infra de test (RNG portable, oracle gradient), zéro changement moteur ; toujours pas installé (info). ④ **comfyui-ltxvideo** : PR #564 « Update ComfyUI-LTXVideo - 2026-09-22 » auto-générée, 1 fichier (bump de routine). Info. ⑤ **comfyui-nouveaux-repos** : mkamranr/reelforge (66★), jplenio/ComfyUI-MiniMax-Music-Production-Toolkit (61★ — idée musique MiniMax, mais écosystème ComfyUI/PyTorch donc info seulement), ruashots/open-h3-ir (60★ — autour de H3), Saganaki22/ComfyUI-Hyperflow (55★). Info idées workflows. **Reste à jour** : audio.cpp v0.8.1, sd-cli master-890 (rien de nouveau depuis l'ignoration d'hier), trellis.cpp v0.6.0, FFmpeg 9.0.2, org audio-cpp 8 repos sans nouveau fichier (AuK toujours sans famille `auk`), ACE-Step v0.1.8, llama.cpp v0.4.1, outils système OK. Entrées info purgées de `maj_en_attente`, seule qwentts.cpp conservée. **Pin sd-cli 864 + contournement `C:\SD-6b3edaa` inchangés**.

- **2026-09-21 (fin de session) • qwentts.cpp / sd-cli • maj qwentts appliquée `e793d73` → `0bfb923` (accord « Mets à jour sauf sd ») + sd-cli master-890 IGNORÉE (même décision que 879/881/883)** — ① **qwentts.cpp** : 3 commits amont tous build/infra (« cmake: select the ggml source tree with GGML_SOURCE_DIR », « ggml: fork update », « build: macOS portability ») + bump sous-module **ggml `0572d602` → `e2568d7b` = 1 commit « metal: support left and circular padding in GGML_OP_PAD » — backend Metal macOS uniquement, zéro impact sur notre chemin Vulkan/Windows** (aucun changement fonctionnel TTS, pas d'impact recettes §1.20). Via `manage_qwentts.py --update` (vérif préalable : aucun process audio/ffmpeg/qwentts) : backup `backup_20260921_234749_e793d73`, build Vulkan 9 s, **smoke test 11,77 s RÉUSSI** (WAV 130 604 octets), `--check` vert (binaires, 4 modèles conformes ~6,1 Go, 5 sauvegardes). Entrée purgée de `maj_en_attente` (appliquée). ② **sd-cli master-890-74988b2 IGNORÉE** (décision utilisateur « mets à jour sauf sd ») — contenu détaillé dans l'entrée veille 23h40 ci-dessous (9 commits : LLaDA-Image, CUDA SageAttention, fp8/ggml amont, garde-fou ; **toujours RIEN sur model manager #1976 ni la régression mémoire**) ; entrée purgée de `maj_en_attente` (décision durable — la veille ne re-mentionnera que sur release POSTÉRIEURE à 890, qui devra passer la matrice complète Flux 1024² + Wan 17 trames + LTX 33 + H3 turbo, cf. MEMORY_BANK §1.16/§1.17) ; **pin master-864-ca37fad + contournement `C:\SD-6b3edaa` inchangés**.
- **2026-09-21 (23h40, veille arrière-plan session) • 5 nouveautés journalisées — sd-cli master-890 toujours sans fix mémoire (recommandation ignorer), qwentts.cpp 3 commits build en attente d'accord, KittenTTS + Piper publiés en avance sur le moteur** — ① **sd-cli master-890-74988b2** (compare `17860c0` master-881 ignorée → `74988b2` = **9 commits**, dont les 2 déjà connus de 883 : #1994 Qwen Image 2.1 + #1999 build ggml amont) — nouveaux : #1968 « add LLaDA-Image support » (famille diffusion text-to-image, jamais cataloguée chez nous), #2005 native CUDA SageAttention (**CUDA only**), #2001 restore legacy fp8 handling (chemin build ggml amont), #2002 fix logs ggml passés comme format strings, b56c686 fix SigVQ patch embedding, #2017 fix « reject video models in image generation » (garde-fou CLI), docs CONTRIBUTING. **Toujours RIEN sur le model manager #1976 ni la régression mémoire (#1940/#1956/#1957)** → 4ᵉ release d'affilée hors sujets bloquants, **recommandation ignorer**, pin master-864-ca37fad + contournement `C:\SD-6b3edaa` inchangés ; entrée maintenue dans `maj_en_attente` jusqu'au verdict utilisateur. ② **qwentts.cpp `e793d73` → `0bfb923` = 3 commits, tous build/infra** : « cmake: select the ggml source tree with GGML_SOURCE_DIR », « ggml: fork update » (bump sous-module — même mécanique que les majs du 16→20/09 qui apportaient des fix Vulkan, contenu à vérifier au build) et « build: macOS portability » — rien de fonctionnel TTS ; `manage_qwentts.py --update` si accord (faible risque, intérêt = suivre les fix ggml amont). ③ **org audio-cpp : 2 NOUVEAUX dossiers dans `audio-cpp/audio.cpp-gguf`** : **KittenTTS-GGUF** (`kitten-tts-mini-0.8-orig.gguf`, TTS mini) et **Piper-TTS-GGUF** (`piper-en-us-lessac-medium-orig.gguf`, voix anglaise medium du projet Piper) — **vérifié : aucune spec `kitten`/`piper` dans `model_specs` de la v0.8.1 installée** → publication en avance sur le moteur (même pattern qu'AuK le 20/09, Apollo/UniverSR/PulseVAD le 17/09) ; watch à chaque release audio.cpp ; deux TTS voix (Piper anglais uniquement) — hors instruction permanente musique (chasse close §1.24), le clonage de voix reste couvert par qwentts. ④ **hf-modeles-gguf** : 16 nouveaux en tendance, **aucun text-to-audio** — LLM chat (Hemmingway-1 ♥362, yandex AliceAI-Foundation-80B, Xiaomi MiMo-V2.6 Flash/Pro RL, Qwen3.8-35B-A3B-Distill…) → hors périmètre média, info. ⑤ **ComfyUI v0.37.0** (release info idées, notes `output/veille/notes/comfyui-core_v0.37.0.md`) : MoGe 3 (géométrie/depth — écho de l'extractor OmniCam suivi le 19/09), Qwen Image 2.1 (même feature que sd-cli #1994), fix ace step VAE decode hors bf16 (signale le point faible, nous en Vulkan non concernés), fix MiniMax Music 3 bruit avec CUDA graphs (CUDA only), Meshy 7.1, YuE2 CFG — rien d'exploitable directement, sd-cli reste le moteur. Mineur : 403 rate-limit GitHub sur comfyui-nouveaux-repos (comme le 18/09), re-vérif à la prochaine veille. Entrées info ③④⑤ purgées de `maj_en_attente` (journalisées, rien de testable) ; restent en attente : sd-cli master-890, qwentts.cpp.
- **2026-09-20 (soir, relevé manuel) • sd-cli • master-883-137f740 publiée 16h58 UTC (après la veille 9h58) — PREMIÈRE release avec une FEATURE depuis le pin 864 : support Qwen Image 2.1, mais toujours RIEN sur nos 2 sujets bloquants → recommandation ignorer, en attente du verdict utilisateur** — notes de release vides ; compare `17860c0` (master-881 ignorée le matin) → `137f740` = **2 commits** : ① **#1994 « feat: add Qwen Image 2.1 support »** (réf. diffusers #14804) — nouvelle version du modèle d'image Qwen Image ; première feature amont depuis master-864, mais famille jamais cataloguée chez nous (absente du README `C:\SD`, Flux couvre nos besoins image) et en cas d'install la matrice complète serait exigée (Flux 1024² + Wan 17 trames + LTX 33 + H3 turbo) ; ② #1999 « feat: support building with upstream ggml » — infra de build (compiler contre le ggml amont), sans effet runtime sur nos binaires préconstruits. **Model manager #1976 : toujours ouverte, 0 commentaire, dernière activité 16/09 ; régression mémoire (lignée #1940/#1956/#1957) : aucun commit ne la touche.** Pin master-864-ca37fad + contournement `C:\SD-6b3edaa` inchangés. La veille re-mentionnera 883 à son prochain passage (release postérieure à 881) — purge de l'entrée si décision identique. Notes : `output/veille/notes/sd-cli_master-883-137f740.md`.
- **2026-09-20 • qwentts.cpp • maj appliquée `09e0038` → `e793d73` (accord utilisateur « go » puis « tu peux le faire » sans attendre la fin du batch vidéo ai-doc2video)** — 1 commit amont « log: every diagnostic goes through qt_log » (refactor mécanique de logging : 28 fichiers +237/−201, rien de fonctionnel) via `manage_qwentts.py --update` : backup `backup_20260920_101644_09e0038`, build Vulkan 13 s, **smoke test 8,49 s RÉUSSI** (WAV généré), `--check` vert (git à jour, binaires opérationnels, 4 modèles conformes ~6,1 Go, 5 sauvegardes — rotation a purgé la plus ancienne). **Nuance process : maj lancée PENDANT le batch vidéo `jev_ai` d'ai-doc2video (scene_03 en encodage AMF) sur autorisation explicite de l'utilisateur — risque analysé nul côté TTS : la phase vocale du projet était déjà terminée (`master_soundtrack.wav` 03h42, wav par scène présents), plus aucun appel qwentts restant ; la règle « jamais de maj en plein batch » reste la règle par défaut, et le script d'attente automatique préparé (`output/qwentts_maj_auto_20260920.sh`, poll ffmpeg + fraîcheur fichiers avant `--update`) a été stoppé pour aller plus vite — réutilisable pour une future maj pendant un batch.** Entrée `qwentts.cpp` purgée de `maj_en_attente` (appliquée — fichier désormais vide).
- **2026-09-20 • sd-cli • master-881-17860c0 IGNORÉE (décision utilisateur) — 3e release perf-only d'affilée, nos issues restent sans réponse → purge durable** — 7 commits `656a135` → `17860c0`, **tous perf ou mineurs** : #1992 pad têtes d'attention < 64 canaux pour MMA FA (CUDA only), #1993 ggml « faster direct convolutions », #1996 VAE conv 3D direct, #1997 réduction overhead CPU graph/sampling, #1998 ops élément par élément/broadcast host parallélisées, fix #1995 crop center clip_preprocess, 9982c9c dépendance driver CUDA. **Toujours RIEN sur le model manager** (#1976/#1989 : 0 commentaire de leejet) ; verdict utilisateur : « notre issue va passer à la trappe, on en a plusieurs même jamais répondues » → **entrée purgée de `maj_en_attente`** (la veille ne re-mentionnera que sur release POSTÉRIEURE à 881, qui devra passer la matrice complète Flux 1024² + Wan 17 trames + LTX 33 + H3 turbo) ; **pin master-864-ca37fad + contournement `C:\SD-6b3edaa` inchangés**. Option toujours ouverte sans toucher au pin : build parallèle `C:\SD-881` pour mesurer les gains conv/VAE plausibles en Vulkan (#1993/#1996) et re-sonder la régression Flux 1024².
- **2026-09-20 • audio.cpp • NOUVEAU repo org `audio-cpp/AuK-Base-and-Flash-GGUF` (créé cette nuit 00h07) : Tencent AuK en GGUF officiels — PAS encore de famille `auk` en v0.8.1 → watch à chaque release audio.cpp (accord utilisateur « ok à surveiller »)** — modèle **MIT** (base `tencent/AuK`), pipeline tag text-to-speech mais périmètre bien plus large d'après les tags + exemples du repo : **TTS zero-shot, clonage de voix, ÉDITION vocale par instruction** (exemples : replace_words, replace_lyrics, raise_pitch, speed_1_5x, increase_volume, add_cough, whisper, happy, second_speaker, deep_voice), **séparation de sources + enhancement** (vocals_only, get_what_speaker, enhance) ; GGUF `auk-base`/`auk-flash` f16/f32/q8_0 (~3,1 Go f16) + `auk-vae-f32` + conditionneur `qwen2.5-omni-3b-bf16` (~7,4 Go). **Vérifié le jour même : aucune spec `auk` dans `C:\audio-cpp\model_specs` de la v0.8.1 installée** → publication en avance sur le moteur (même pattern LiveAvatar/Apollo/UniverSR avant v0.8.1). **Action : à chaque 🆕 release audio.cpp, vérifier `model_specs/auk.json` — si présente, lancer le test (instruction permanente 2026-09-07) : priorité capacités voix (clonage sur la référence utilisateur + édition d'une voix off existante) ; gain potentiel chaîne YouTube : retoucher la voix off sans regénérer.** Info même source : tendance HF du matin = LLM hors périmètre (gemma-4-31B, Bonsai ternary 27B…) + 2 **LoRAs yue2** au registre pourtant dans notre cible (chœurs bulgares/kargyraa, qawwali sombre) mais **safetensors ComfyUI sous CC-BY-NC → incompatibles stack zéro PyTorch + chaîne monétisée** (rien à tester) ; à noter que la communauté pousse YuE2 (famille déjà couverte, spec `yue2.json` présente en v0.8.1) vers le registre ethno/sombre. Entrées info purgées de `maj_en_attente`.
- **2026-09-19 (~15h, signalée par l'utilisateur) • sd-cli • master-879-d32b4e8 publiée 13h14 UTC (après la veille 10h09) — TOUJOURS RIEN sur le model manager (#1976 ouverte, 0 commentaire, aucune réaction leejet) → cohérent avec la décision du 18/09 : à ignorer, en attente du verdict utilisateur** — notes de release vides ; compare `ca37fad` (pin master-864) → `d32b4e8` = **15 commits**, dont 6 nouveaux depuis 874 (déjà ignorée) : ① perf #1992 pad des têtes d'attention < 64 canaux vers 64 pour MMA Flash Attention — **CUDA only, non pertinent Vulkan** ; ② perf #1993 maj ggml « faster direct convolutions » (implicit GEMM tuilé F16 pour `--diffusion-conv-direct`, gain annoncé 1,64 → 17,43 it/s mais mesuré SD1.5 **CUDA** — gain Vulkan plausible, à mesurer) ; ③ perf #1996 `--vae-conv-direct` étendu aux conv 3D (Wan VAE) — l'apport ggml est surtout CUDA, intérêt Vulkan incertain ; ④ fix #1995 clip_preprocess center crop (mineur, img2img) ; ⑤ 9982c9c propagation dépendance driver CUDA (non pertinent). ⑥ Rappel utile du contenu de 874 : **#1987 (absente de notre pin 864 ET du contournement 6b3edaa) révèle que `--fa` n'était JAMAIS appliqué aux encodeurs LLM (Qwen/Mistral/Gemma) — sur Vulkan, au-delà de ~4096 tokens l'attention LLM matérialise sa matrice et retombe en CPU (11,5 s → 239 s à 4214 tokens sur RX 7900 XTX)** — à garder en tête si des prompts LTX/H3 longs (MotionScene OmniCam) passent par un encodeur LLM. Ni #1940/#1956/#1957 ni #1976/#1989 touchés ; **pin master-864-ca37fad + `C:\SD-6b3edaa` inchangés**. Toute release postérieure à 874 devra passer la matrice complète (Flux 1024² + Wan 17 trames + LTX 33 + H3 turbo) ; option intermédiaire sans toucher au pin : build parallèle `C:\SD-879` pour mesurer les perfs conv/VAE et re-sonder la régression Flux 1024.
- **2026-09-19 (fin de session) • tests • Test 2 INTERROMPU sur décision utilisateur (session trop longue) — écueil piégeux découvert : le proto endless/h3_ref2va résout `C:\SD\sd-cli.exe` (master-864, régression mémoire) si `SD_CLI_PATH` n'est pas préfixé — besoin exprimé : scripts de nuit pour les tests longue durée** — chronologie Test 2 : ① lancement SANS `SD_CLI_PATH` → 2 échecs `ErrorOutOfDeviceMemory` identiques (22 min chacun, ~5,5 min de sampling) d'abord attribués aux refs 4K (`--ref-scale 1.0` sur la source dragon 4K = trames réf 3840×2160) ; ② relance `--ref-scale 0.15` (refs 576×320, dossier `output/test_endless_aicg3d_s015/`) → même échec à 6,6 min en crash dur (exit 3221226505) → **cause racine trouvée dans le log : `commit ca37fad` = master-864, la build à régression mémoire** (défaut `core/config.py:15` = `C:\SD\sd-cli.exe`, même écueil que Flux 1024 du midi) ; ③ relance sur 6b3edaa (`SD_CLI_PATH` préfixé) → sampling H3 démarré proprement (buffers 9 GB stagés), **interrompu par l'utilisateur avant le checkpoint 10 min — raccord chunk_01 jamais jugé**. **À reprendre en script de nuit** (le proto a déjà la bonne forme : idempotent par chunk, `--deadline-min` jusqu'à 23 h, retries automatiques) — à préparer : wrapper nuit `SD_CLI_PATH=C:\SD-6b3edaa\sd-cli.exe` + sonde 2 chunks 5 trames (`--ref-scale 0.15`) puis A/B 12 trames, jugement des planches de raccord au réveil. **Leçon transversale de la session : tout lancement video/H3 doit porter `SD_CLI_PATH=C:\SD-6b3edaa\sd-cli.exe` tant que le pin 864 tient — y compris via le proto et les scripts appelant les workflows** (envisager : écrire le contournement dans core/config.py au lieu du env-only, cf. SKILL.md écueil). Livrables Test 1 conservés : `godot_assets/test_omnicam_{A,B,B2}_*.mp4/.webm` + planches `output/test_omnicam/planche_*.png` (A freeform OK, B habillage OmniCam = split-screen, B' phrases seules OK).
- **2026-09-19 (après-midi, test) • recettes OmniCam/AICG3D • Test 1 (prompt caméra LTX) TERMINÉ : l'habillage « Camera motion: … Follow closely » d'OmniCam déclenche du SPLIT-SCREEN sur LTX-2.5 Distilled ; les phrases seules sont saines — Test 2 (handoff 5 trames AICG3D) LANCÉ** — protocole : même ancre Flux 1024² (tour sur éperon, seed 42, `godot_assets/omnicam_ancre.png`), même seed, 65 trames LTX via `C:\SD-6b3edaa` (monoplan_ia), 3 variantes de prompt caméra jugées sur planches 6 frames (`output/test_omnicam/planche_*.png`). **A (freeform)** : push-in + dérive latérale droite avec bonne parallaxe des rochers, mais pas d'arrêt net en fin (mouvement continu jusqu'à la dernière trame). **B (habillage OmniCam verbatim : « Camera motion: … Finally it eases to a stop. Follow the camera motion and timing closely. »)** : **artefact dur — chaque trame contient DEUX vues côte à côte séparées par un trait noir (split-screen), vidéo inutilisable** ; l'habillage impératif + « follow closely » a fait interpréter LTX comme une consigne multi-vues. **B' (mêmes phrases AXIS_PHRASES en phrase fluide, sans l'habillage)** : propre — push-in net qui accélère, latéral droit avec parallaxe, fin en gros plan sur la tour ; aucune vue double. **Leçon recette : transplanter le VOCABULAIRE OmniCam (pushes in / tracks laterally to camera-right / eases to a stop) oui, JAMAIS l'habillage « Camera motion: / Follow the camera motion and timing closely » sur LTX** (conçu pour H3 avec playblast en référence, pas du text-only). Écueils de mesure : extraction de frames du webm VP9 — le seek rapide `-ss` avant `-i` produit des trames partielles noires en début de GOP et le montage `tile` peut noircir une cellule (toujours vérifier la luminosité YAVG d'une cellule douteuse avant de conclure à un artefact de génération). Temps : ~25 min/run. Restant : Test 2 = sonde `proto_endless_h3 --ref-frames 5` (défaut AICG3D, aligné grille 17k+5) en cours (~1 h 15), A/B `--ref-frames 12` si raccord ambigu.
- **2026-09-19 (midi) • sd-cli • régression mémoire master-864 ÉTENDUE à Flux 1024² en image — contournement 6b3edaa confirmé pour le test en cours, test GPU des recettes OmniCam/AICG3D reporté (GPU occupé)** — pendant la préparation des tests des recettes ComfyUI (OmniCam/AICG3D, clones d'analyse dans `C:\GIT\`) : le workflow `generate` (Flux 1024², 25 steps) échoue sur master-864 avec la signature mémoire connue — `model_manager.cpp:1767 WARN cannot make enough memory available … need 4445.25 MB device / 3933.25 MB budget, available 2857.74 MB` puis `ggml_runner.cpp:877 flux segment failed during workspace capacity check` (= même famille que #1976/#1989, impactait jusqu'ici les chemins vidéo LTX/H3) ; **une sonde Flux 512² 4 steps PASSE sur le même binaire** (exit 0, 23 s) → le blocage dépend de la taille du workspace, pas d'un OOM réel (VRAM ~12 Go libres). **Statut des tests : analyse code terminée des 2 repos (synthèses dans l'entrée après-midi), ancre Flux non générée (annulée), aucun test GPU lancé — à lancer sur `SD_CLI_PATH=C:\SD-6b3edaa\sd-cli.exe` quand la machine est libre : (1) OmniCam → A/B `monoplan_ia` même seed, prompt caméra freeform vs bloc timecodé `AXIS_PHRASES` (« [0.0-1.7s] The camera pushes in toward the subject… ») ; (2) AICG3D → sonde `proto_endless_h3 --ref-frames 5 --deadline-min 100` (handoff 5 trames = défaut AICG3D, aligné grille H3 17k+5) puis A/B `--ref-frames 12` si raccord ambigu.** Le smoke image de validation du pin 864 (steps 4) passait sous le radar : trop petit pour déclencher le capacity check — retenir qu'**un smoke image ne vaut validation qu'à la résolution de production**.
- **2026-09-19 (après-midi) • analyse demande utilisateur • OmniCam + AICG3D en détail (transposables à nos workflows sans ComfyUI) + LiveAvatar-GGUF identifié CUDA-only (suivi à chaque release audio.cpp)** — ① **ComfyUI-Majoor-OmniCam (76★, MIT)** : layout/animation caméra décrite UNE fois (MotionScene, schémas JSON indépendants des modèles dans `schemas/`) puis « compilée » par modèle cible (profils wan_camera_native, ltx25_motion_track, h3_native/h3_scene_coverage/h3_api…), avec Extractor (track 6DoF depuis un plan, reconstruction MoGe/SAM/VGGT), Director (viewport Three.js, presets Dolly/Orbit/Spiral, tracé à main levée) et Monitor (compilateur + preflight bloquant). **Réutilisable hors ComfyUI (MIT : schémas JSON portables, frontend JS autonome)** ; pour nous : vocabulaire de trajectoires et formats de tracks → enrichir les prompts caméra de `monoplan_ia`/H3 ; expérimental (« expect breaking changes ») → figer une version si on s'en inspire. ② **comfyui-AICG3D (75★, GPL-3.0)** : workbench H3 unifié (fusion ComfyUI-MiniMaxH3-Easy MIT + Goohai GPL) — **recettes directement transposables à notre chaîne H3 CLI** : génération séquentielle par segments illimités (grille 5/22/39/56/73 frames = notre chunk 22 de `proto_endless_h3` ; frames de connexion 1-20 chaînées en latent OU RVB pour le raccord chunk→chunk — exactement notre point critique documenté), upscale latent 3D en 2 passes (denoise 0,4-0,6 en pass 2, tuiles 512/512/128/32 pour VRAM limitée), paliers VRAM 8/12/16G avec déchargement de l'encodeur texte Qwen3VL (~25 Go int8 : backbone H3 31,7 Go streamé CPU→GPU), 144 templates de prompts (8 catégories), filtrage des médias par tags `<Picture N>` du prompt. Références de réimplémentation : `h3easy/sampling_strategies.py`, `h3_latent_upscaler.py` (⚠️ GPL-3 : citation obligatoire si code copié — pour nous lire les idées et réécrire). ③ **LiveAvatar-GGUF (enquête demandée par l'utilisateur, « super intéressant »)** : le moteur est **audio.cpp lui-même** (`audiocpp_cli --family liveavatar`, support natif ajouté sur main par `a074d6b` #605 après `5185508` #601 framework Wan S2V — **NON encore release, v0.8.1 reste la dernière**) ; Wan2.2-S2V-14B + LoRA Quark-Vision/Live-Avatar fusionné (**Apache 2.0**), 3 fichiers GGUF (denoiseur **NVFP4** 13,9 Go + support/encodeurs Q4_K_S-F16 + VAE F16), modes offline/streaming, 48 frames/clip 16 fps, 4 steps Euler guidance 0, SageAttention, seed 420. **Bloquant pour nous : la spec `model_specs/liveavatar.json` porte `runtime.tags = ["cuda","gguf","stream"]` — la chaîne Wan S2V passe par des lowering CUDA (#532 lowering APIs, #533 implémentations, #600 fusion SSM), aucun chemin Vulkan, et le NVFP4 est un format NVIDIA** → injouable sur la 6950 XT ; VRAM au demeurant au ras : 720p ~16 Go avec weight streaming (mesuré sur RTX 5090, ×1,63 plus lent), 480p 2 blocs cache ~15,3 Go/318 s. **Suivi mis en place : à chaque 🆕 release audio.cpp, lire la spec liveavatar et vérifier (a) tag runtime Vulkan, (b) quant non-NVFP4 du denoiseur (la veille org couvre les nouveaux GGUF)** — si Vulkan arrive un jour : avatar présentateur piloté par la voix off = gros gain chaîne YouTube (conseil du card : itérer en 480p puis upscaler). ④ Aucun ComfyUI/PyTorch installé sur le poste (philosophie zéro PyTorch) : tester les plugins nativement exigerait d'installer ComfyUI — décision utilisateur, pas fait ; analyses extraites des README/schémas/workflows JSON, directement exploitables.
- **2026-09-19 (matin, suite) • uv / qwentts.cpp / FFmpeg • 3 mises à jour appliquées (accord utilisateur « Go les 3 maj », process 1 par 1)** — ① **uv 0.12.16 → 0.12.17** : `uv self update` a d'abord échoué sur `uvx.exe` verrouillé par l'outillage ZCode (même piège que le 15/09, désormais sur l'install standalone) → parade identique : rename `uvx.exe` → `uvx.exe.old` (NTFS autorise le rename d'un exe en cours), relance → **0.12.17 (635500036 2026-09-18) installée**, `uvx --version` + imports `core, workflows` OK ; résidus `uvx.exe.old` (+ `uv.exe.old`/`uvx-0.12.13.exe.bak` de Program Files du 18/09) disparaîtront au reboot. ② **qwentts.cpp `4323282` → `09e0038`** via `manage_qwentts.py --update` (backup `backup_20260919_105216_4323282`, rotation a purgé `backup_20260912_163546_a69194f`) : commit `09e0038` (C ABI expose la table des langues, +15/+10 lignes qwen.cpp/h) + **bump sous-module ggml `3f88e6fe → 0572d602` = 2 commits « address review from jeffbolznv »** (fix Vulkan de revue, faible risque). Build Vulkan 30 s, **smoke test 9,44 s RÉUSSI**, `--check` vert (git à jour, binaires, 4 modèles conformes, 5 sauvegardes). ③ **FFmpeg 9.0.1 → 9.0.2 « Lei »** (18/09, point release branche 9.0, correctifs) : recette `update.sh` — **nouvel écueil : pacman a profité du passage pour mettre à jour `msys2-runtime` et a tué le terminal en cours de build** → relancer `update.sh` une 2e fois, il termine proprement (consigné au README C:\ffmpeg). Vérif post-maj : `ffmpeg -version` 9.0.2, encodage AMF GPU OK, **mesure loudnorm sur `output/music_bg/combat_drums.ogg` = sortie normale** (chaîne audio music_bg intacte). Ancien build en `C:\ffmpeg\dist.bak`. ④ **Entrées `ffmpeg`, `qwentts.cpp`, `uv` purgées de `maj_en_attente`** (appliquées — fichier vide). Manifest `engines_manifest.json` : aucun changement (FFmpeg install fraîches reste build standard BtbN latest ; sd-cli reste épinglé master-864).
- **2026-09-19 • veille 10h09 (arrière-plan session) • 7 nouveautés journalisées — 3 majs actionnables en attente d'accord, sd-cli 874 purgé (décision déjà prise), découverte notable : LiveAvatar-GGUF** — ① **org audio-cpp : NOUVEAU repo `audio-cpp/LiveAvatar-GGUF` (18/09 20h50, 0 dl)** : GGUF **Wan2.2-S2V-14B** (Speech-to-Video : vidéo/avatar parlant animé depuis une image de référence + un fichier audio — `Wan2.2-S2V-14B-NVFP4-LORA.gguf`, support/LoRA Q4_K_S-F16, VAE F16) + exemples mp4 480p/720p (options cache, weight-streaming, layerwise, vae-tile = inférence ggml vidéo streamée) → **potentiel fort pour la chaîne YouTube** (avatar présentateur piloté par la voix off TTS), MAIS **aucune famille liveavatar/s2v dans les model_specs de audio.cpp v0.8.1 installée** → publication en avance sur le support moteur (même pattern qu'Apollo/UniverSR/PulseVAD avant v0.8.1) ; à re-vérifier à la prochaine release audio.cpp (l'instruction permanente 2026-09-07 est audio : elle s'appliquera si la famille apparaît ; sinon surveiller un éventuel outil dédié). ② **FFmpeg 9.0.1 → 9.0.2 « Lei »** (18/09, point release de la branche 9.0 coupée du master le 26/06 — correctifs, pas de feature) : maj via la recette validée (update.sh MSYS2) si accord ; vérif post-maj `ffmpeg -version` + loudnorm rapide. ③ **qwentts.cpp** : 2 commits amont `4323282` → `09e0038` (`09e0038` C ABI « expose the language table », mineur ; `1628ef1` bump sous-module ggml fork — même mécanique que les majs 16/09 et 18/09 qui apportaient des fix Vulkan, contenu du bump à vérifier au `--update`) : `manage_qwentts.py --update` si accord. ④ **uv 0.12.16 → 0.12.17** : patch, et la migration standalone d'hier rend la maj triviale (`uv self update`, sans élévation). ⑤ **comfyui-nouveaux-repos (2)** : `MajoorWaldi/ComfyUI-Majoor-OmniCam` (76★, layout/animation de caméra — keyframes, préview motion, vidéo de référence pour modèles vidéo → idées de trajectoires pour `monoplan_ia`/H3) et `JGRFW/comfyui-AICG3D` (75★, workbench unifié MiniMax H3 = fusion ComfyUI-MiniMaxH3-Easy + Goohai-MiniMax-H3_Integration → directement lié à notre moteur H3, à fouiller pour réglages/workflows). ⑥ **hf-modeles-gguf** : 11 nouveaux en tendance, essentiellement LLM chat hors périmètre (GLM-5.3 ♥1874 déjà vu, K2-Horizon, Xing4.0-29B-A4B, Bonsai-2 ternary, needle3) — rien text-to-audio. ⑦ **Entrée `sd-cli` master-874 purgée de `maj_en_attente`** (journalisée hier soir, décision durable « attendre un fix mémoire » — complétait la « purge anticipée par journalisation ») ; entrées info ①⑤⑥ purgées à leur tour ; **restent en attente d'accord : FFmpeg 9.0.2, qwentts.cpp, uv**.
- **2026-09-18 (soir) • sd-cli • master-874-656a135 IGNORÉE (décision « attendre un fix mémoire » — rien sur le model manager) — purge anticipée par journalisation** — signalée par l'utilisateur ~1 h après publication (18h44) ; contenu réel (compare `cc515a0` → `656a135`, **2 commits seulement**, notes de release vides) : ① fix « honor flash attention flag in LLM text encoder attention » (#1987 — 1 ligne dans `llm.hpp`, les flags FA s'appliquaient mal à l'attention des encodeurs LLM) ; ② refactor « remove obsolete unused tensor filtering » (#1984 — suppression de 76 lignes de filtrage devenues obsolètes dans `model_loader.cpp`, suite logique de #1983). **AUCUN commit sur la refonte mémoire #1940/#1956/#1957, rien pour #1976 ni #1989** (toujours 0 commentaire de leejet sur les deux à cette date). **Pin master-864-ca37fad + binaire parallèle `C:\SD-6b3edaa` inchangé** ; la veille ne re-mentionnera que sur release POSTÉRIEURE à 874, qui devra passer la matrice complète (Flux + Wan 17 trames + LTX 33 + H3 turbo, cf. MEMORY_BANK §1.16/§1.17).
- **2026-09-18 • sd-cli • issue #1976 : CONFIRMATION INDÉPENDANTE par cross-reference (#1989, damenordev, CUDA/NVIDIA) — toujours aucune réponse du mainteneur, aucun fix** — pas un commentaire mais une **nouvelle issue liée** : damenordev (RTX 4070 Ti SUPER 16 Go + RTX 2060, CUDA Linux, master `cc515a0`) rapporte la **même signature WARN du model manager** (`cannot make enough memory available … available 0.00 MB device / unlimited budget`, chiffres byte-identiques entre runs) sur des chemins **IMAGE** (flux2 klein + z-image avec conditionneur LLM Qwen3-4B) → **élargit le périmètre du bug au-delà de nos chemins vidéo Vulkan** ; `--auto-fit off` ne sauve PAS ces cas (contrairement à Wan chez nous). **Point fort pour notre dossier** : les mêmes fichiers GGUF passent sur la même RTX 2060 sous le sd.cpp embarqué de KoboldCpp 1.121 (≈1,4 s/step, images propres) → **régression master-only confirmée par un tiers, pas les poids**. Zone suspectée par l'auteur : `ModelManager::check_capacity` / `ggml_backend_dev_memory` (retour `free=0` sur le chemin CUDA) — cohérent avec la refonte #1940/#1956/#1957. **Toujours aucun commentaire de leejet sur #1976 ni #1989** ; commits amont depuis master-872 (`2ea8aff` ggml conv, `adcac69` FA MMA, `656a135`, `269e726` du 18/09) : perf/FA uniquement, RIEN sur le model manager → **pin master-864 + parallèle `C:\SD-6b3edaa` inchangé**. Effet attendu : un rapport CUDA touchant du matériel/cas d'usage grand public (image simple, grosse carte) élargit l'audience du bug → pression accrue pour un fix ; re-tester à la prochaine release postérieure à 872 (matrice complète requise, cf. MEMORY_BANK §1.16/§1.17). Notes : <https://github.com/leejet/stable-diffusion.cpp/issues/1989>.
- **2026-09-18 (fin) • audio.cpp familles outils • VERDICTS UTILISATEUR : UniverSR VALIDÉ → workflow `audio_upscale` créé, Apollo REJETÉ** — écoute des extraits `output/test_audio_cpp_v081/` : **UniverSR** voix 16k→48k « les voix sont ok copie sans bug parfait même » (variante speech) + musique 24k→48k « très bien belle batterie ça sonne bien » (variante audio) → **workflow `audio_upscale`** (`core/audio_upscale.py` + `workflows/audio_upscale.py` + main.py menu 41/CLI `--upsr-variante`/`--upsr-rate`, testé end-to-end RTF 13,03 ; bande auto ffprobe 8/12/16/24 kHz, pré-rééchantillonnage FFmpeg, refus > 24 kHz ; défaut `speech` ; **piège : `--seed` global CLI = −1 « aléatoire » → forcé 42, universr exige un entier non signé**). **Apollo** « ça va moins bonne qualité » = rejeté (aucune amélioration audible vs la source compressée ; GGUF conservé, supprimable à la demande). Sweep « nul » : normal, signal technique. PulseVAD : sans verdict (VAD). Limites persistantes UniverSR : CPU seul RTF 13, sortie mono → à re-tester si un chemin Vulkan arrive (README §4.15, MEMORY_BANK §1.27, skill SKILL.md + catalogue à jour).
- **2026-09-18 (suite) • qwentts.cpp / uv / paquets Python • 3 mises à jour appliquées (accord utilisateur « continue ») + MIGRATION uv pip → standalone officielle** — ① **qwentts.cpp `90efdc6` → `4323282`** via `manage_qwentts.py --update` (backup `backup_20260918_092050_90efdc6`, rotation a purgé `backup_20260912_163511`) : les 2 commits attendus — bump sous-module **ggml fork `6431c6a2 → 3f88e6fe`** + nit CLI (erreur d'argument → usage + exit 1). Build Vulkan 27 s, **smoke test 5,84 s RÉUSSI**, `--check` : git à jour, binaires opérationnels, 4 modèles conformes, 5 sauvegardes. ② **uv 0.12.15 → 0.12.16** : la tentative pip (recette du 15/09) a de NOUVEAU tourné au mélange — **4 dist-info empilées dont DEUX 0.12.15**, doublon user-site, `pip show` incohérent avec le binaire → **sur proposition de l'utilisateur, MIGRATION vers l'install standalone officielle** (script astral.sh, sans élévation) : installée dans `C:\Users\laurent\.local\bin` (déjà dans le PATH utilisateur), puis purge complète des versions pip : `pip uninstall` élevé ×4 + résidus Roaming supprimés. **Vérifs : `uv` résout UNIQUEMENT vers le standalone (0.12.16, 761ff1379 2026-09-17), `uv self update` FONCTIONNE → les prochaines majs uv = 1 commande sans élévation, la recette pip du 12/09+15/09 (rename + pip élevé + --ignore-installed) est OBSOLÈTE ; venv du dépôt intact (`import core, workflows` + `main.py --help` OK).** Résidus inertes à purger lors d'un prochain passage élevé : `C:\Program Files\Python313\Scripts\{uv,uvx}.exe.old` (tenus par des processus uv actifs, disparaîtront au reboot) + `uv*-0.12.13.exe.bak`. ③ **Paquets Python** : idna 3.19 → 3.20, protobuf 7.36.1 → 7.36.2 (`uv lock --upgrade && uv sync`) ; imports core/workflows/idna/protobuf + `main.py --help` OK. ④ **Entrées `qwentts.cpp`, `uv`, `paquets-python` purgées de `maj_en_attente`** (appliquées — fichier désormais vide).
- **2026-09-18 • audio.cpp • v0.8.0 → v0.8.1 APPLIQUÉE (accord utilisateur, process 1 par 1) : 3 nouvelles familles d'OUTILS audio (Apollo / UniverSR / PulseVAD) testées le jour même** — ① Update via `update.ps1` (backup `backup_v0.8.0_20260918_084915.zip`, rotation a purgé le backup v0.7.3). ② **Smoke ace_step turbo 28 s : RTF 2,06 vs 2,22 (v0.8.0, A/B même jour depuis le backup) → pas de régression, léger gain (−7 %) ; sorties byte-identiques (MD5)** — la baseline 1,84-1,93 du 16/09 ne se reproduit pas (dérive machine, règle A/B même jour). ③ **Les 3 familles en attente depuis le 17/09 sont exposées et TESTÉES** (détails complets MEMORY_BANK §1.27, GGUF installés) : **apollo** (restauration musique compressée 44,1 kHz — Vulkan OK, stéréo, RTF 0,08, le seul pleinement exploitable ; écoute G vs H), **universr** (super-résolution → 48 kHz — ⚠️ Vulkan cassé `MUL_MAT` → CPU seul RTF 13, ⚠️ sortie MONO → inutilisable lits stéréo tel quel ; extrapole le HF au-delà du Nyquist avec atténuation ; écoute A/B/C voix), **pulsevad** (VAD 16 kHz mono — Vulkan OK, RTF 0,004). **Statut : fonctionnels, non validés** — verdict utilisateur en attente sur les extraits, aucun workflow (règle). ④ Autres changements : YuE2 LoRA AR/NAR indépendants + export plan ABC, kokoro misaki eSpeak complet, CLI `--out-format`. ⑤ **Entrée `audio.cpp` purgée de `maj_en_attente`** (appliquée). Les entrées info de la veille 08h43 (hf-modeles-gguf, sa3.cpp PR#46, comfyui-ltxvideo PR#560) sont purgées à leur tour (journalisées le matin même, rien de testable).
- **2026-09-18 • veille 08h43 (arrière-plan session) • audio.cpp / qwentts.cpp / uv / paquets / sa3.cpp / comfyui-ltxvideo / hf-modeles-gguf — 7 nouveautés journalisées, en attente verdict utilisateur pour les majs** — ① **audio.cpp v0.8.0 → v0.8.1 (2026-09-17, 22 PRs — la plus dense depuis longtemps)** : **les 3 familles en attente depuis le 17/09 sont maintenant exposées** (PR #583, vérifié dans `model_specs` amont, 86 specs) : **apollo** = restauration de musique compressée 44,1 kHz (band-sequence, `audio_tools`, tâche s2s), **universr** = super-résolution audio → 48 kHz (complex-STFT flow-matching), **pulsevad** = VAD — **ce sont des outils de post-traitement, PAS des générateurs musicaux** → l'instruction permanente 2026-09-07 (chasse au moteur musical gothique, CLOSE §1.24) ne s'applique pas telle quelle ; intérêt potentiel réel : **UniverSR 48 kHz pour la chaîne YouTube** (upscale IA des lits musicaux/voix au-delà d'un resample soxr FFmpeg) et Apollo pour nettoyer des sources compressées — à tester APRÈS maj (GGUF déjà dans le repo org : `apollo-orig.gguf`, `universr-*`, `pulsevad-*`). Reste du changelog v0.8.1 : **YuE2** — LoRA AR et NAR indépendants (#586, #567), export du plan ABC en partition (#561), fix eager NAR attention Intel Vulkan (#557) ; **kokoro_tts** — port complet du frontend misaki eSpeak anglais (#565), skip phonèmes sans id vocab (#564), stream de phonèmes fourni (#577) ; **CLI `--out-format`** (format WAV de `--out`/`--out-dir`, #591) ; diarization textless (#584) + 6 familles ASR/outils dans la WebUI (#589) — ASR hors cible ; fixes clap mel-projection (#593), RoFormer FP32 HIP (#573), qwen decode zero buffer (#560), GGUF sidecars (#579) ; CUDA Turing/cmake ccache hors périmètre. Smoke test post-maj : 1 génération ace_step 12 s turbo + comparer RTF (A/B v0.8.0 même jour : fausse alerte résolue). ② **qwentts.cpp** : 2 commits amont (`90efdc6` → `4323282`) — bump sous-module **ggml fork `6431c6a2 → 3f88e6fe`** (même mécanique que la maj du 16/09 qui apportait 2 fix Vulkan ; contenu du bump ggml à vérifier au `--update`) + nit CLI (erreur d'argument → usage + exit 1). ③ **uv 0.12.15 → 0.12.16** (patch). ④ **paquets Python** : idna + protobuf (changelogs mineurs). ⑤ **sa3.cpp** : PR #46 mergée (keybed FX chain) — cible gothique CLOSE (§1.24), install conservée pour SFX/autre genre → pas de maj prévue, pour mémoire. ⑥ **ComfyUI-LTXVideo** : PR #560 (sync auto du 17/09, corps vide) — info, rien d'adaptable identifié. ⑦ **hf-modeles-gguf** : 11 nouveaux en tendance — essentiellement des LLM hors périmètre (Ternary-Bonsai-2-27B ♥395, Xing4.0-29B-A4B, Qwen3.8-35B-A3B-Distill) ; `jinaai/jina-ocr-v1` (OCR GGUF) potentiellement utile un jour pour `video-analys-ia` mais multimodal llama.cpp, hors périmètre immédiat. Mineur : rate-limit GitHub (403) sur comfyui-nouveaux-repos, re-vérifier à la prochaine veille.
- **2026-09-17 • veille (entrées info consommées) • org-audio-cpp / hf-modeles-gguf / comfyui-nouveaux-repos — journalisés puis purgés de `maj_en_attente`, rien de testable aujourd'hui** — ① **org audio-cpp** : 3 nouveaux dossiers dans le repo principal `audio-cpp/audio.cpp-gguf` : **Apollo-GGUF** (`apollo-orig.gguf`), **PulseVAD-GGUF** (a priori VAD — voice activity detection : `pulsevad-2.1k-f32`, `pulsevad-81k-f32`), **UniverSR-GGUF** (a priori restauration de parole : `universr-audio-orig`, `universr-speech-orig`) — **aucune famille correspondante dans `model_specs` de la v0.8.0 installée (= dernière release)** → publication en avance sur le support moteur, aucun test possible ; l'instruction permanente 2026-09-07 (tester sans attendre tout nouveau modèle audio) s'appliquera dès qu'une release audio.cpp exposera ces familles — à re-vérifier à la prochaine release. ② **hf-modeles-gguf** : 4 nouveaux en tendance, rien d'exploitable — 2 LLM hors périmètre média (Nex-N2.5-Pro, Qwen3.8-27B-Uncensored-Cyber-agentic-imatrix), `ACE-Step/acestep-v15-xl-turbo` (repo officiel PyTorch d'avril 2026, déjà couvert : section `modeles-gguf` = nos 4 paquets ACE-Step à jour), `OpenMOSS-Team/MOSS-SoundEffect` (text-to-audio vérifié **PyTorch uniquement** — safetensors ×4, aucun GGUF → écarté comme Dasheng la veille). ③ **comfyui-nouveaux-repos** : `alesha-pro/tools` (107★), `ralksta/ComfyUI-Photoshoot` (79★) — info idées à évaluer au besoin (cf. `docs/recherche_comfyui_2026-09-09.md`). Re-mentions sur nouveau diff seulement.
- **2026-09-17 • sd-cli • master-872-cc515a0 IGNORÉE (décision « attendre un fix mémoire » re-confirmée) — purge entrée `maj_en_attente`** — contenu réel de 872 (3 commits `07a85c7 → cc515a0`, toujours AUCUN sur la refonte mémoire #1940/#1956/#1957 qui casse LTX/H3 sur 16 Go) : ① fix règles de pré-tokenization spécifiques au tokenizer (#1975), ② retrait des tensors `vision_model.` inutilisés (#1983), ③ perf Philox — élimination d'allocations temporaires dans les rounds (#1982). Rien qui justifie de toucher au pin **master-864-ca37fad (+ binaire parallèle `C:\SD-6b3edaa` pour H3/LTX)**. Re-mention seulement sur release POSTÉRIEURE à 872 (matrice complète requise : Flux + Wan 17 trames + LTX 33 + H3 turbo, cf. MEMORY_BANK §1.16/§1.17). Notes : `output/veille/notes/sd-cli_master-872-cc515a0.md`.
- **2026-09-16 • veille (entrées info consommées) • sa3.cpp / comfyui-core / modèles GGUF HF — journalisés puis purgés de `maj_en_attente`, rien à installer** — ① **sa3.cpp** : PR #44 mergé (« retire legacy ABI ») — l'install locale (`C:\IA\sa3.cpp`, commit C-ABI #43, NON VALIDÉ cible gothique §1.24, conservée pour SFX/autre genre) n'est pas mise à jour : aucun besoin, le retrait de l'ABI legacy ne change ni la qualité ni les leviers (cfg-rescale/APG restent la piste si besoin un jour) ; à reprendre au prochain usage réel. ② **ComfyUI v0.36.0** (15/09) : release info idées (notes `output/veille/notes/comfyui-core_v0.36.0.md`) — rien d'exploitable directement, sd-cli reste le moteur. ③ **hf-modeles-gguf** : 8 nouveaux modèles en tendance, **aucun en pipeline text-to-audio utilisable** — le seul candidat média, `mispeech/Dasheng-AudioGen` (audio-gen), a été vérifié **PyTorch uniquement = incompatible moteurs Vulkan** (écarté) ; le reste = LLM chat (Qwen3.8-27B déclinaisons, Ornith-1.5, occamy-1.0, K2-Horizon…), hors périmètre média. Re-mention seulement sur nouveau diff.
- **2026-09-16 • sd-cli • master-869-07a85c7 IGNORÉE DURABLEMENT (décision « attendre un fix mémoire » confirmée) — purge entrée `maj_en_attente`** — contenu réel de 869 (3 commits `42d6c0a → 07a85c7`, aucun sur la refonte mémoire #1940/#1956/#1957 qui casse LTX/H3 sur 16 Go) : ① tokenizers externes HF JSON (#1973) + passage de Gemma 2/GPT-OSS à des tokenizers externes requis (#1974 — nous n'utilisons ni l'un ni l'autre), ② Brownian tree noise dans tous les samplers à injection de bruit (#1899, cosmétique qualité, pas un fix mémoire). **Le pin master-864-ca37fad (+ binaire parallèle `C:\SD-6b3edaa` pour H3/LTX) reste la config de production** ; la veille ne re-mentionnera que sur release POSTÉRIEURE à 869, qui devra passer la matrice complète (Flux + Wan 17 trames + LTX 33 + H3 turbo, cf. MEMORY_BANK §1.16/§1.17). Notes : `output/veille/notes/sd-cli_master-869-07a85c7.md`.
- **2026-09-16 • qwentts.cpp • 71ad93d → 90efdc6 appliqué (accord utilisateur, process 1 par 1) : un seul commit amont « ggml: fork update » — sous-module ggml → 6431c6a2** — contenu utile du bump : **2 fix Vulkan** (dimensionnement des ranges in-place A/B par leur extent strié ; lecture du batch stride d'un src0 in-place depuis nb[2]) + fix conv_1d pour entrées batchées + bitonic argsort CUDA lignes larges + activation snake fusionnée CPU. Procédure : `manage_qwentts.py --update` (backup `backup_20260916_181504_71ad93d`, build Vulkan 27 s, **smoke test 6,07 s RÉUSSI**) ; `--check` vert (git à jour, binaires 18:15, 4 modèles conformes, 5 sauvegardes). Aucun changement de features TTS — correctifs de fond ggml seulement ; pas d'impact recettes (§1.20 inchangé). Entrée `maj_en_attente` purgée.
- **2026-09-16 • audio.cpp • v0.7.4 → v0.8.0 INSTALLÉE (accord utilisateur, process 1 par 1) : 4 nouvelles familles dont `yue2` enfin en release — et FAUSSE ALERTE RTF résolue par A/B même jour** — ① Update via `update.ps1` (backup `backup_v0.7.4_20260916_175507.zip`, rotation a purgé le backup v0.7.2). ② **Familles nouvelles** (76→80) : `yue2` + `sheetsage2` (fusion PR #553 — YuE2 déjà testé le 13/09 via branche dev, verdict « encore du POP », cible gothique CLOSE §1.22 ; licence poids CC BY-NC 4.0 = toujours inutilisable chaîne monétisée ; la release ne change rien au verdict), `niagara_asr` (ASR léger), `builtin_audio_utils` (utilitaires audio exposés comme famille). ③ **Reste de la release** : fix dispatch Vulkan **COL2IM_1D pour grands tenseurs**, prefill Qwen chunké avec réutilisation KV cache backend, C ABI opt-in in-process, frontend serveur optionnel, streaming texte par incréments, fix contextes 32 bits. Org audio-cpp en parallèle : GGUF **Canary-180M-Flash** (f32+q8_0, ~200 Mo) et **Cohere-Transcribe-03-2026** (bf16+q4_0) publiés — transcription, aucun besoin actuel (candidat sous-titres si un jour). ④ **Smoke ace_step turbo 28 s : RTF 1,85-1,93 vs baseline 1,613 (14/09) → alerte « +16 % », rollback v0.7.4 fait, MAIS v0.7.4 remesuré le même jour = 1,836 → dérive machine ≈ +15 % depuis le 14/09, parité v0.7.4/v0.8.0 dans le bruit, v0.8.0 RE-APPLIQUÉE et gardée.** Leçon consignée (README + §1.11) : **toute alerte RTF se tranche par A/B même jour, jamais par comparaison à une baseline d'un autre jour** ; nouvelle référence ace_step turbo 28 s au 16/09 : ≈ 1,84-1,93 (~52-54 s). **Verdict utilisateur à l'écoute (`smoke_v080_final.wav`, recette « subtle minimal techno groove ») : « c'est bien, un beat simple » — v0.8.0 validée aussi en qualité** (pas un test cible gothique — cible close §1.11/§1.22/§1.24, simple contrôle post-maj). ⑤ Manifest : audio.cpp reste `release: latest` (update validée, rien à épingler). Entrée `maj_en_attente` purgée (appliquée + validée). Détail : `C:\audio-cpp\README.md` §v0.8.0, MEMORY_BANK §1.11 (stack + perf mis à jour).
- **2026-09-15 (fin de session 2) • llama.cpp • VERDICT 0.4.1 : VALIDÉ — l'incident de tout à l'heure était transitoire, pas une régression** — retest smoke `--single-turn < /dev/null` sur b10964 : exit 0, prompt 249,8 t/s, génération 200,5 t/s, sortie propre (LFM2.5-8B-A1B, 24 tokens) → **on garde 0.4.1 (b10964)**. Leçons : ① **toujours lancer les smoke tests llama-cli avec `--single-turn` (ou `-no-cnv`) ET stdin fermé `< /dev/null`** — le mode conversation par défaut attend une entrée et, dans un contexte pipé/background, le processus peut se coincer côté console/driver au point de devenir incertainable (taskkill/Stop-Process timeout, énumération de processus système figée ~30 min, résorbée seule sans reboot) ; ② le combo « processus coincé côté noyau » se diagnostique via `tasklist` qui se fige ; ③ backup `backup_0.4.0-dev_b10819_2026-09-15` conservé. Entrée `maj_en_attente` purgée (appliquée + validée).
- **2026-09-15 (fin de session 2) • uv • 0.12.13 → 0.12.15 appliqué dans Program Files — piège de la mise à jour pip documenté** — recette : uv était une install pip système (`C:\Program Files\Python313\Scripts`), donc ① `uv self update` inopérant (réservé aux installs standalone), ② pip non élevé retombe silencieusement sur l'user site (0.12.15 y finissait masqué par le PATH), ③ même élevé, pip échoue en WinError 5 tant que des processus uv/uvx tournent (l'outillage ZCode en garde ~10) → **parade : renommer les exes (NTFS autorise le rename d'un exe en cours) puis `pip install --upgrade --ignore-installed uv` élevé** — vérifié : `uv --version` = 0.12.15 (d35f1f270 2026-09-15) sur le PATH. Entrée `maj_en_attente` purgée.
- **2026-09-15 (fin de session 2, verdict) • sa3.cpp • verdict utilisateur « non concluant » sur le WAV gothic rock → testé NON VALIDÉ, aucun workflow, cible gothique CLOSE sur la famille SA3** — cf. MEMORY_BANK §1.24 (mis à jour) : conforme au plafond pop SA3 via audio.cpp (§1.22) → 6ᵉ moteur au plafond ; install conservée à `C:\IA\sa3.cpp` (RTF 0,12, utile pour SFX/autre genre), leviers cfg-rescale/APG/negative prompt et medium 1,5B non exercés.
- **2026-09-15 (fin de session 2) • sa3.cpp • PREMIÈRE GÉNÉRATION RÉUSSIE : gothic rock 30 s rendu en 3,6 s sur la 6950 XT (8 steps, small-music 0,5B) — en attente du verdict à l'écoute** — `output/test_sa3cpp/gothic_rock_30s_seed42_smallmusic.wav` (PCM 16 bits 44,1 kHz stéréo, 30,00 s, seed 42, recette de référence : « German gothic rock 1990, dark wave, hypnotic tribal groove, deep pulsing bass, chiming chorus guitars, 83 BPM, C# minor » passés dans le prompt) — backend Vulkan détecté proprement, VRAM raisonnable, vitesse exceptionnelle (3,6 s pour 30 s audio = RTF 0,12 en 8 steps ; à comparer ACE-Step §1.11). **Statut : testé, VERDIF UTILISATEUR PENDANT** (pas de workflow avant validation, règle du dépôt) ; si validé : A/B medium 1,5B puis encapsulation workflow. Commande exacte dans `C:\IA\sa3.cpp\gen_test.log` (journal `SA3_MODELS_DIR` + `--model small-music --duration 30 --seed 42`).
- **2026-09-15 (fin de session 2, suite) • sa3.cpp • installation terminée (cf. entrée génération ci-dessus)** — `C:\IA\sa3.cpp` (betweentwomidnights, commit C-ABI #43 inclus) ; **écueil build.cmd : il cherche le générateur « Visual Studio 17 2022 » absent du poste (VS « 18 » 2026 Community installé, MSVC 14.44/14.51)** → recipe qui marche : `cmake -S . -B build-vulkan -G "Visual Studio 18 2026" -A x64 -DCMAKE_BUILD_TYPE=Release -DSA3_VULKAN=ON` puis `cmake --build build-vulkan --config Release` (binaires dans `build-vulkan\bin\Release\`, `sa3-generate.exe` fonctionnel) ; modèles via `models.cmd --variant small-music` (5 fichiers HF thepatch, ~65 Mo/s sans bridage CDN ; les GGUF Stable-Audio-3 de `C:\Modeles_LLM` sont des fichiers uniques format audio.cpp, non réutilisables — sa3.cpp attend un jeu tok/t5/cond/dit/same).
- **2026-09-15 (fin de session) • ComfyUI-FL-YuE2 • écarté : poids YuE2-3B en CC BY-NC 4.0 (non commercial) + NVIDIA BF16 uniquement** — nodes « paroles+style → chanson 48 kHz » avec piano roll ABC (filliptm, 82★, idée séduisante pour la chaîne) : licence poids incompatible chaîne YouTube monétisée, et aucun support AMD/Vulkan documenté (validé RTX PRO 6000 seulement). Rien à installer ; à revoir seulement si ports/quantizations Vulkan un jour.
- **2026-09-15 (fin de session 2) • uv/blender • état intermédiaire documenté puis résolu en session 2 (cf. entrées uv et llama.cpp ci-dessus)** — uv 0.12.15 installé Program Files ; **Blender 5.2.1 → 5.2.2 LTS INSTALLÉ ET VALIDÉ (accord utilisateur)** : MSI blender.org 348 Mo installé en silencieux élevé (`msiexec /qn /norestart`, exit 0 — l'UAC reste le seul clic), `blender --version` = 5.2.2 LTS (hash d13f752e3b9c, build 2026-09-15), **addons mpfb + blenderkit toujours actifs** après maj de patch (config %APPDATA% préservée — vérifié headless via `addon_utils.check()`), rien à épingler dans `engines_manifest.json` (Blender absent). Workflow `asset_blendkit` (validé sur 5.2.1 le matin même) à re-vérifier au prochain usage — aucune raison connue d'une casse entre patch LTS.
- **2026-09-15 (fin de session) • Blender • procédure maj 5.2.1 → 5.2.2 arrêtée volontairement (reboot déjà requis)** — installateur MSI blender.org (`https://download.blender.org/release/Blender5.2/blender-5.2.2-windows-x64.msi`), mise à jour en place de `C:\Program Files\Blender Foundation\Blender 5.2\` (élévation UAC), config/addons préservés dans `%APPDATA%\Blender Foundation\Blender\5.2\` ; vérif post-maj : `blender --version` = 5.2.2 + MPFB (§1.15) se charge en headless. À faire après le reboot.

- **2026-09-15 (matin 2) • sd-cli • issue #1976 RÉÉCRITE en place (0 commentaire, accord utilisateur) — un seul poste propre avec les 2 fenêtres de régression + l'A/B 6b3edaa** — titre et corps remplacés via `gh issue edit` : tableau récapitulatif 841/864/866 (Wan ❌→✅→❌-contournable / LTX+H3 ✅→❌→❌ / image toujours OK), Wan suspect #1967 (fenêtre 864→866), LTX+H3 suspects refonte mémoire 841→864 (#1940/#1956/#1957, interaction possible #1958) avec le contrôle **6b3edaa en installation parallèle = 2 segments × 8 steps webm 2279 s** en pièce maîtresse de l'argumentation, + le paradoxe LTX (15 Go libres vs « available 320 Mo ») et l'I2V non segmenté. Texte source : `scratch/issue_m866_wan_t2v_BROUILLON.md` (copie canonique locale). **Complément (même matin, demande utilisateur « soit très précis ») : section « Current mitigation on our workstation » ajoutée au poste** — double installation détaillée : `C:\SD\`=m864 (image + Wan I2V/T2V ≤ ~20 trames `--vae-on-cpu` obligatoire, décode GPU 19,4 Go / refus workspace 33 trames) + `C:\SD-6b3edaa\`=m841 (H3 re-validé 15/09 recette turbo exacte 2279 s ; LTX dernier validé 10-13/09 sur ce même binaire), mêmes modèles/même GPU, `--version` vérifié avant chaque run ; perte en attendant le fix = les gains mémoire 841→864 sur les chemins LTX/H3.
- **2026-09-15 (09h43) • sd-cli • RE-VALIDATION H3 sur 6b3edaa via installation parallèle `C:\SD-6b3edaa\` : le test turbo exact PASSE (2 segments × 8 steps, webm 2279 s, format §1.16 exact) — cause 100 % build confirmée par A/B** — créé depuis `C:\SD\backups\backup_2026-09-13_m864/` (m864 du dossier principal intacte), `--version` = 6b3edaa vérifié ; même prompt/seed/LoRA/assets que le test m864 d'hier soir → **graphe 2 segments** (`blocks.0..37` + suite, 8 exécutions de chaque) vs **51 segments + OOM** sur m864/m866, webm 864×480 22 trames + PCM 32 kHz stéréo en 2279 s (timing historique exact). **Production H3 (et a priori LTX) redevient possible dès aujourd'hui via le binaire parallèle** (commande brute : `output/test_h3_6b3edaa/h3_turbo_6b3edaa.log` ; le workflow `h3_ref2va` appelle `C:\SD\sd-cli.exe` en dur — override à ajouter si besoin). Correction #1976 enrichie (A/B 2 vs 51 segments à 24 h d'écart sur même machine) — toujours à poster sur accord. Détail : MEMORY_BANK §1.16 (mis à jour).
- **2026-09-15 (00h18) • sd-cli • VERDICT H3 sur master-864 : CASSÉ AUSSI — `using 51 segments` + `ErrorOutOfDeviceMemory` à 263 s, signature identique à m866 (6b3edaa validé = 2 segments) — test post-reboot lancé par l'utilisateur via `scripts/statut_h3_m864.py`** — la fenêtre de régression H3 = **841→864** comme LTX (refonte mémoire #1940/#1956/#1957/#1958, pas #1967). **Bilan vidéo complet du poste** : 6b3edaa = LTX+H3 OK mais Flux+Wan cassés (l'époque #1946) ; m864 = Flux+SDXL+Wan ≤20 trames OK mais **LTX+H3 cassés** ; m866 = tout cassé sauf image+Wan contourné. **Aucun build ne fait tout** — m864 reste le pin (image + Wan), la production LTX/H3 attend un fix amont (issue #1976, correction à poster sur accord : last-good LTX/H3 = 6b3edaa). Options présentées à l'utilisateur : installation séparée 6b3edaa en parallèle de m864 (binaires dispo dans `C:\SD\backups\backup_2026-09-13_m864/`) pour produire LTX/H3 en attendant le fix. Détail : MEMORY_BANK §1.16/§1.17 (mis à jour).
- **2026-09-14 (nuit 2) • test idée « planche de poses → danse » + DÉCOUVERTE • LTX-2.5 cassé depuis master-864 AUSSI (dernier build bon = 6b3edaa/master-841) — la fenêtre de régression LTX est 841→864, pas 864→866 ; H3 sur m864 également inconnu** — test de la technique `idee/` (planche 4×4 → i2v danse) : ① **planche de poses Flux dev 1024² : excellente cohérence personnage** (zéro dérive costume/cheveux sur 8 panneaux) mais 8 panneaux au lieu de 16 + numérotation fautive — la comptabilité de grilles reste le point faible local (`output/test_idee_poses/planche_16poses.png`, seed 42) ; ② **i2v LTX impossible** : portrait 480×832 (même budget pixels que le validé) = submit OOM étape 5/8 sur m864, paysage 832×480 = submit OOM ~115 s (graphe NON segmenté), et — test décisif — **le benchmark T2V 33 trames échoue sur m864 avec le MÊME refus calculé que m866** (workspace capacity check, need 910,16/budget 398,16 Mo identiques) → ce n'est ni l'état machine (VRAM 1,3/16) ni #1967 : **la casse LTX vient de la refonte mémoire 841→864 et était passée inaperçue** (m864 n'avait été validée que Flux + Wan T2V ; le plafond « 81 trames » de §1.17 = mesures 6b3edaa). ③ Vidéo danse reportée (aucun i2v rapide disponible : Wan 17 trames = ~35 min pour 0,7 s). **Actions : correction à poster sur #1976 (last-good LTX/H3 = 6b3edaa, pas m864) + test H3 sur m864 (~38 min) à lancer pour connaître le statut réel de la production chaîne.** Détail : MEMORY_BANK §1.17 (nouvel addendum).
- **2026-09-14 (fin de session) • verdicts TTS • casting qwentts JUGÉ (5 voix retenues, 2 rejetées accents EN, 1 trop lente, 4 speakers non écoutés) + kokoro re-confirmé (EN OK / FR moyen)** — ① **§1.20 qwentts** : piste « plein de voix FR » du jeu VALIDÉE dans son principe — speakers dylan/eric/ono_anna validés, sohee retenu (accent léger), aiden rejeté (fort accent anglais) ; design : héraut jeune retenu (« aigu marrant »), ancien gardien retenu avec réserve (trop lent — piste `atempo` au montage ou trait de personnage), capitaine Sterne rejeté (« anglais qui parle français ») ; serena/vivian/uncle_fu/ryan restent à l'écoute. Workflow de production de dialogues créé au moment du besoin réel. ② **§1.21 kokoro** : ré-écoute conforme au verdict du 12/09 — af_heart EN « OK » (validé usage EN limité/dépannage), ff_siwis FR « moyen » (écarté) ; v0.7.4 ayant intégré kokoro_tts en release, un éventuel workflow EN ne demanderait plus que la recette CLI. Détail MEMORY_BANK §1.20/§1.21 (mis à jour).
- **2026-09-14 nuit • sd-cli • verdict définitif sur master-866 après test de l'idée « on la garde avec `--auto-fit off` » : REJET ÉLARGI — les 3 chemins vidéo de production sont cassés, seul Wan est contournable ; master-864 restaurée (2e fois) et revalidée** — l'utilisateur a proposé de garder master-866 grâce au contournement Wan ; validation complète avant adoption (recettes exactes, seeds fixes, logs `output/test_m866/`) : ① **image OK** — Flux schnell ✅ (déjà fait), SDXL juggernaut 512×512 20 steps ✅, upscale 4x-UltraSharp 512→2048 ✅ (syntaxe CLI : `--upscale-model`, pas positionnel) ; ② **Wan 2.2 T2V 17 trames** : ❌ défaut / ✅ `--auto-fit off` (confirmé bout en bout) ; ③ **LTX-2.5 33 trames** (recette benchmark, backends explicites) : ❌ défaut (`ltxav segment 1/1 failed during workspace capacity check`, need 910,16/available 319,37 Mo, à 21 s) **ET ❌ avec `--auto-fit off`** (budget passe à « unlimited » mais available reste 317 Mo → même mort à 19 s) — **pas de contournement** ; ④ **MiniMax H3 Ref2VA turbo 22 trames** (workflow `h3_ref2va` complet, réf `ref_frames12`+`ref_audio_05.wav`, seed 42) : ❌ — encodes réf/conditionnement OK (~20 min), puis `minimax_h3 using 51 segments` (864 : graph-cut **2 segments** 8,5+1,7 Go) et `vk::Queue::submit: ErrorOutOfDeviceMemory` à 359 s de sampling ; flag non testé sur ce chemin (38 min/run, LTX prouvant déjà qu'il n'est pas universel). **Conclusion : le changement de segmentation/budgets mémoire de la gamme 864→866 (suspect #1967) casse Wan+LTX+H3 sur 16 Go — master-866 inadoptable ; master-864 restaurée depuis le backup, `--version` = ca37fad, smoke Flux ✅ (3e revalidation du jour).** Matrice de retest future (release postérieure à 866) : Flux + Wan 17 trames + LTX 33 + H3 turbo. Brouillon d'issue amont enrichi (4 datapoints, EN) : `scratch/issue_m866_wan_t2v_BROUILLON.md` — **POSTÉE le soir même sur accord utilisateur : [leejet/stable-diffusion.cpp#1976](https://github.com/leejet/stable-diffusion.cpp/issues/1976)** (texte nettoyé, sans marque de brouillon ; question reboot tranchée au passage : machine déjà fraîche de 4 h 16, échecs déterministes type refus calculé, pas le mode « pilote dégradé » du 10/09). README `C:\SD`, manifest, MEMORY_BANK §1.17 mis à jour (rejet élargi + matrice de retest).
- **2026-09-14 soir • sd-cli (suite) + audio.cpp + qwentts.cpp • contournement `--auto-fit off` VALIDÉ pour la régression Wan de master-866 + brouillon d'issue amont prêt ; audio.cpp v0.7.4 INSTALLÉE (RTF parité) ; qwentts.cpp 71ad93d installé** — ① **Test `--auto-fit off` sur master-866 (accord utilisateur)** : la recette Wan 2.2 T2V 17 trames exacte qui échouait en défaut PASSE **bout en bout** avec le flag — retour au compute **résident 1 segment** (941,58 Mo, le « 42 segments » disparaît), sampling 8/8 ~272 s/it (+3 % vs 864), webm OK 2399 s ; sortie même scène/trajectoire mais divergence numérique visible (trame médiane 14,8/255 — attendu entre layouts différents). **Décision : master-864 reste épinglée quand même** — H3/LTX non retestés sur 866 et #1967 touche précisément les « explicit backend assignments » que la recette H3 utilise. **Brouillon d'issue amont finalisé (EN, 2 datapoints) : `scratch/issue_m866_wan_t2v_BROUILLON.md` — à poster sur accord utilisateur.** Détail : MEMORY_BANK §1.17 (2 addendums). ② **audio.cpp v0.7.4 installée** via `update.ps1` (backup `backup_v0.7.3_20260914_193712.zip`) : smoke **ace_step turbo 28 s = RTF 1,613 vs 1,617 baseline = parité**, niveaux sains (mean −11,7 dB) ; `model_specs` : **`kokoro_tts` + `moonshine_asr`** arrivent en release (familles attendues), + VibeVoice ASR streaming perf, sanotts +11 voix (dont fr), fix dispatch Vulkan FILL ; rien côté musique (ace_step inchangé, RTF confirmé). README `C:\audio-cpp` mis à jour. ③ **qwentts.cpp 779c7cb → 71ad93d** via `manage_qwentts.py --update` (backup `backup_20260914_194137_779c7cb`, build Vulkan OK, **smoke test 6,76 s RÉUSSI**) : 5 commits amont — sampling « single source of truth » (chaque knob surchargeable sur toutes les surfaces, nouveau `sampling-defaults.h`), refonte `sampling-graph.h` (176 lignes), tts-server : signalement des échecs de synthèse sur le chemin PCM + rejet d'enregistrement de voix hors modèles de base, ggml fork update (bitonic argsort) + retrait des setters de précision dépréciés. `--check` vert. ④ **Org audio-cpp : nouveaux GGUF `Niagara-ASR-GGUF`** (niagara-19m/38m-batch **en uniquement**, f32 — nouvelle famille ASR légère ; non installé, à garder en tête si besoin STT EN rapide côté audio.cpp). ⑤ `maj_en_attente.json` : entrées audio.cpp / org-audio-cpp / qwentts.cpp purgées (appliquées/consignées) — ne reste que `hf-modeles-gguf` (info LLM tendance, rien de pertinent média).
- **2026-09-14 • sd-cli • master-866-42d6c0a installée (accord utilisateur « go 1 ») puis REJETÉE le jour même : RÉGRESSION Wan 2.2 T2V sur la recette validée — rollback master-864 appliqué et revalidé** — ① **Contenu 864→866 (7 commits)** : #1967 « preserve explicit backend assignments during auto-fit » (44dd137), fix regex attention (#1919), **Wan 2.2 S2V audio+img→vidéo lipsync (#1925** — portrait réf + WAV pilote, encodeur wav2vec2 `--audio-encoder` + `--audio`, audio muxé dans le conteneur ; Q4_K_M = 13,4 Go VRAM mesurés sur 24 Go → juste sur nos 16 Go ; 1 chunk 77-81 frames @16 fps, pas d'extension longue ; modèles S2V non téléchargés), métadonnées de génération embarquées dans les vidéos (#1901), warnings MSVC (#1969) ; #1958/#1918 déjà en 864. ② **Smoke Flux 4 steps : PASSÉ** (exit 0, image renard propre 20,8 s, seed 42 — `output/test_m866/smoke_flux_42d6c0a.png`). ③ **Test de non-régression Wan 2.2 T2V 17 trames (recette exacte §1.17 : LowNoise Q4_K_M 832×480, 8 steps, `--diffusion-fa --temporal-tiling --vae-on-cpu`, seed 42) : ÉCHEC** — même placement de poids résidents qu'en 864 (9211,74 Mo/1095 tensors en VRAM) mais le DiT est désormais découpé en **42 segments** (864 : 1 segment résident, compute 941 Mo) et la génération meurt à **l'étape 5/8** sur `segment 24/42 (wan.blocks.22) failed during weight preparation` — « cannot make enough memory available on Vulkan0: need 664.34 MB device / 1092.16 MB budget, available 631.30 MB device / 2366.17 MB budget » (`model_manager.cpp:1767`) après 1390 s de sampling. **Suspect n° 1 : #1967** (seul commit mémoire/auto-fit de la gamme) — même famille que #1946 (manque de quelques dizaines de Mo au weight preparation) mais sur le NOUVEAU chemin segmenté. Pistes NON testées (coût ~40 min/run) : `--auto-fit off` (restaurerait le comportement résident ?), issue amont à ouvrir (brouillon possible depuis ces logs). ④ **Décision process : rollback immédiat master-864** (backup `backup_2026-09-14_ca37fad/` conservé pour un futur retest), `--version` = ca37fad, **smoke Flux revalidé post-rollback** (exit 0). Manifest ré-épinglé master-864-ca37fad (les 3 plateformes, asset corrigés — incohérence linux/macos de la maj précédente réparée) ; entrée sd-cli purgée de `maj_en_attente.json` (rejet durable — la veille ne re-mentionnera que sur release POSTÉRIEURE à 866, qui devra passer le même double test). README `C:\SD` mis à jour (encadré « NE PAS INSTALLER » + contenu 866 pour mémoire). Détail : MEMORY_BANK §1.17 (addendum).
- **2026-09-13 nuit • sd-cli • master-864-ca37fad INSTALLÉE (accord utilisateur « Go ») : fix #1946 validé sur l'officiel, Wan 2.2 T2V sampling 8/8 OK (1re fois depuis master-841) mais décode VAE 19,4 Go impossible sur 16 Go résident — contournement `--vae-on-cpu` VALIDÉ bout en bout** — procédure complète : téléchargement asset officiel (39,1 Mo), backup `C:\SD\backups\backup_2026-09-13_m864/` (binaires 6b3edaa), extraction, `--version` = ca37fad. **Smoke Flux (repro #1946 exact, test_bisect.sh) : PASSÉ** (exit 0, image 23,7 s). **Wan 2.2 T2V LowNoise Q4 832×480 sur master-864** : ① 33 trames résident = refus PROPRE du workspace (besoin 2,2 Go / 1,0 Go dispo — le nouveau gestionnaire ne crash plus) ; ② `--offload-to-cpu` = `ErrorDeviceLost` au 1er submit DiT (idem avec `GGML_VK_DISABLE_HOST_VISIBLE_VIDMEM=1`) → **nouveau bug à remonter en issue séparée** (chemin streamé, logs `output/test_m864/`) ; ③ **17 trames résident = sampling COMPLET 8/8 à 264 s/it (~35 min, 1re fois depuis master-841 !)** puis échec décode VAE (besoin 19,4 Go / 2,1 Go dispo à côté des 12,7 Go de poids DiT) → ④ **`--vae-on-cpu` = SUCCÈS BOUT EN BOUT** (webm 832×480 17 trames @24 fps, 0,67 s, 720 Ko — sampling 264 s/it ~35 min + décode VAE CPU ~5 min) : **recette Wan validée sur master-864 = résident ≤ ~20 trames + `--vae-on-cpu` obligatoire** (décode GPU = 19,4 Go) ; 1re production vidéo Wan depuis master-841. Reste à ouvrir : issue amont pour le chemin `--offload-to-cpu` (ErrorDeviceLost) et le plafond workspace ≥33 trames. Hook issue #1946 RETIRÉ (fonction `bloc_issue_sdcli` + état + mentions AGENTS.md/README) — la veille sd-cli classique reprend la surveillance des versions. Manifest `engines_manifest.json` épinglé master-864-ca37fad. Détail : MEMORY_BANK §1.17/§1.19 (mis à jour).

- **2026-09-13 soir • sd-cli • issue #1946 RÉSOLUE : leejet a MERGÉ le PR #1958 (13:35 UTC) — ⚠️ master-860 (12:33 UTC) est ANTÉRIEURE au merge et ne contient PAS le fix, à SAUTER ; attendre master-861+** — après notre commentaire de test avec images (« From my side the regression is resolved »), leejet : « Thanks for testing. Since all the issues have been fixed, I'll merge the corresponding PR » → **merge commit `9a97738`**. Vérifié par API compare `44dd137...9a97738` : status « ahead », le fix est le 1 commit absent de master-860 → **la nuance est importante pour la décision de reprise sd-cli différée (2026-09-12)** : la règle « re-mention sur release POSTÉRIEURE à master-859 » va signaler master-860, mais c'est la release SUIVANTE (contenant `9a97738`) qu'il faut installer (accord utilisateur requis, smoke test Flux + Wan 2.2 vid_gen §1.17) puis retirer le hook issue #1946. L'issue a aussi mobilisé d'autres utilisateurs touchés par la même fenêtre de régression (Minimax H3 crash text encoder post-841, bisect d'un tiers isolant le commit fautif) — le merge les débloque aussi. Détail §1.19 (mis à jour).

- **2026-09-13 • sd-cli • issue #1946 : leejet a poussé une tentative de correctif sur le PR #1958 (blocs de résidence 1 Gio) — RE-TESTÉ le jour même : LES DEUX CHEMINS PASSENT, régression résolue sur 16 Go** — réponse leejet sur l'issue (09:43 UTC) : « I made an attempt to fix the issue », 3 commits depuis le head testé le 10/09 (`da4e841` blocs 256 Mio, `5c7fc62` **blocs 1 Gio**, merge master `1bf76e6`). Re-test conditions identiques au §1.19 (même repro, même pin ggml `e20c3a14`, build scratch ~10 min) : ① chemin plain **corrigé** (13,1 s, exit 0 — vs échec déterministe segment 30/59 le 10/09) ; ② `--offload-to-cpu` **corrigé** (17,4 s, exit 0 — vs exit 127 silencieux) ; ③ contrôle pixel vs binaire installé 6b3edaa : visuellement identique (diff moy. 1,6/255) ; ④ bonus : sous pression VRAM (process zombie), l'ancien 6b3edaa OOM là où le PR réussit. Détail en MEMORY_BANK §1.19 (mis à jour) ; brouillon de commentaire pour leejet : `scratch/bisect_1946/commentaire_pr1958_head2_BROUILLON.md` (à poster par l'utilisateur). **Suite : merge #1958 + release > master-859 → maj sd-cli (accord utilisateur) puis retrait du hook issue #1946.**

- **2026-09-13 • veille + test YuE2 • audio.cpp v0.7.4 (kokoro_tts passe en release, Moonshine ASR…) + TEST YuE2-3B réalisé (branche dev) : clip court OK, écueil Vulkan sur certains clips longs — écoute utilisateur en attente** — ① **audio.cpp v0.7.4 sortie ce matin** (installée : v0.7.3 — **maj en attente, accord requis**) : **`kokoro_tts` y est intégré** (PR #496 + #515 — la « release > v0.7.3 » attendue par §1.21 existe donc, utile SI la voix EN Kokoro est un jour validée), + **Moonshine ASR** (STT streaming, 3 GGUF tiny/small/medium dans l'org), **VibeVoice ASR Streaming 7B** (transcription attribuée par locuteur, en/zh), sanotts +11 voix de langues (dont fr), perf mira-tts, fix Vulkan FILL dispatch — détail : `output/veille/notes/audio-cpp_v0.7.4.md`. **MAJ UNIQUEMENT sur accord** (une composante à la fois). ② **Nouveaux repos org audio-cpp** : `SheetSage2-GGUF` (transcription musique → MIDI/ABC — s'associe au conditionnement ABC de YuE2, piste cover à garder en tête mais covers = rejetées §1.11), `VibeVoice-ASR-Streaming-7B-GGUF`. ③ **TEST YuE2-3B LANCÉ ET TERMINÉ (instruction permanente)** — support `yue2` **branche `dev` uniquement** (pas dans v0.7.4) : scratch `C:\IA\audio_cpp_master_test` basculé `main`→`dev` (cda0e3a du 13/09), rebuild `build-357` OK (~8 min) ; modèle `audio-cpp/Yue2-3B-GGUF` téléchargé (q8_0 4,3 Go + vae-f16 265 Mo + 4 sidecars → `C:\Modeles_LLM\Yue2-3B-GGUF\`, ~50-70 Mo/s). Résultats : **chanson complète gothic rock de 2:53 générée en 8 min** (recette de référence : style sobre + 83 BPM/C# minor dans le texte du style, graine 42, `cot=off`, **8 steps** = réglage des démos officielles → **RTF ~2,8 wall**, vs 9,6 à 32 steps ; niveaux sains mean −16 dB, crête −0,0 dB), clip court 47 s à 32 steps. **⚠️ écueil** : crash `vk::Queue::submit: ErrorDeviceLost` au 1er submit du graphe NAR sur UN prompt 6 lignes (4021 frames), reproductible ×3, ni arène NAR 10 Go ni `GGML_VK_DISABLE_COOPMAT=1` ne corrigent, alors que 1180 / 4325 / 5655 frames passent → **bug non monotone en taille, dépend du contenu du prompt** ; quirk relevé : **la graine ne rend pas l'AR sémantique reproductible** (durée/composition variable à réglages égaux — le seed ne commande que le bruit NAR). Détail complet en MEMORY_BANK **§1.22** (nouveau). Fichiers d'écoute : `output/test_yue2/` — `gothic_short_seed42_8steps.wav` (2:53, livrable principal) + `gothic_short_seed42.wav` (47 s). **Verdict utilisateur rendu le jour même : « mieux mais encore du POP dedans c est fou » → rejeté pour la cible gothic rock — 5e moteur au plafond pop (§1.22), cible CLOSE sur YuE2 aussi ; pas de workflow.** ④ **qwentts.cpp : 2 commits amont en attente** (`3133782` vs installé `779c7cb`) — `scripts/manage_qwentts.py --update` sur accord. ⑤ Issue sd-cli #1946 : inchangée (open, 11 commentaires, dernier check 11:12). Reste en `maj_en_attente` : DeepSeek-V4-Flash-0731 (LLM tendance, info).

- **2026-09-12 • veille (corrections) • sd-cli master-859 DIFFÉRÉE (décision utilisateur) + fin du « décalage de savoir » : notes auto-complétées par le compare commits quand la release amont n'a pas de changelog** — ① **Décision « attendre »** : l'entrée sd-cli master-859-7f410a3 (re-mentionnée à chaque session par le hook tant qu'elle traînait dans `maj_en_attente.json`) est purgée ; **durabilité prouvée par re-run complet de la veille** : le 🆕 ne se déclenche qu'une fois par version (`derniere_vue` dans l'état), le rapport repasse « ✅ à jour (6b3edaa, master-859) » et l'entrée ne réapparaît pas — la re-mention ne reprendra que si une release POSTÉRIEURE à 859 sort. ② **Contenu réel de 859** (les releases master de leejet sont publiées SANS changelog — la note archivée était vide, d'où le décalage ressenti) : 18 commits `6b3edaa → 7f410a3` dont 3 gros refactors d'architecture (#1940 résidence des poids — identifié en §1.19 comme lié à la régression #1946, #1956 sources/poids, #1957 pipeline de génération séparé — la branche où vit le PR #1958), fixes embeddings SDXL/K-quant/BF16 (#1939/#1936/#1959 — pertinent SDXL+LoRAs), auto-fit mono-GPU à paliers (#1942), prefetch des layers streamés (#1905), réutilisation des plans de graphe CFG (#1943/#1963), fallback tiling VAE (#1932), support SenseNova U1.5 (#1935). Notes régénérées : `output/veille/notes/sd-cli_master-859-7f410a3.md`. ③ **Fix veille** : `_archiver_notes` complète désormais les notes avec la liste des commits (API compare GitHub, `_commits_entre`) quand le corps de release est vide — branché sur le check sd-cli (param `repo`/`ref_avant`), applicable aux autres sources au besoin.

- **2026-09-12 • mises à jour appliquées • uv 0.11.6 → 0.12.13 + SDK Vulkan 1.4.304.1 → 1.4.357.0 (accord utilisateur « Go »)** — ① **uv** : canal = pip (Program Files Python313, pas le standalone — `uv self update` refusé). Saga de mise à jour : pip non élevé part en user-site (invisible car le PATH **machine** est prioritaire), puis **3 uv.exe verrouillaient le binaire** (WinError 5 « Accès refusé » même en élevé — serveurs MCP blender-mcp/crawl4ai, ils se relancent seuls) → résolution : `taskkill /F /IM uv.exe` puis pip **élevé** avec `--ignore-installed` (sinon pip voit le user-site et déclare « already satisfied »), suppression du `uv-0.11.6.dist-info` résiduel + du doublon user-site. Vérifs : `uv --version` = 0.12.13 (binaire 41,5 Mo vs 68 Mo en 0.11.6), `import core, workflows` + `main.py --help` OK. ② **SDK Vulkan** : l'installeur LunarG 1.4.357.0 **ignore `/S`** → installation GUI (composants par défaut = core seul ; les « Shader Toolchain Debug Symbols » auraient ajouté plusieurs Go inutiles aux builds). **Cohabitation** avec 1.4.304.1 (rollback trivial, rien de supprimé). Vérifs : `glslc` = shaderc v2026.3, `vulkaninfoSDK.exe` voit la RX 6950 XT (pilote 26.8.1, API 1.4.315 — ⚠️ `vulkaninfo` est renommé `vulkaninfoSDK.exe` dans les SDK récents), **rebuild complet** du scratch audio.cpp (`build-357`) contre le nouveau SDK + 2 générations kokoro FR valides de bout en bout. Perf : A/B final machine libre, même phrase (12 s d'audio) — **nouveau SDK RTF 0,1092/0,1091 vs ancien 0,1082 → parité à ~1 %, aucune régression** (kokoro = ~9× le temps réel en warm ; les premières mesures 0,23-0,39 étaient polluées par la contention d'un job sd-cli utilisateur alors en cours — GPU 100 %/VRAM 97 % —, et le 0,85 de la 1re re-mesure auto comparait une phrase 5× plus courte : le coût fixe de chargement y écrase le calcul, RTF non comparable entre textes de longueurs différentes). ③ `maj_en_attente.json` : entrées vulkan-sdk + uv purgées (appliquées).

- **2026-09-12 • qwentts.cpp + kokoro • VERDICT utilisateur Kokoro (« pas mal en EN, je préfère Qwen en FR ») + premier casting FR du jeu sur qwentts.cpp : 12/12 synthèses OK — la piste « plein de voix FR » pour L'Héritier du Vide** — ① Kokoro : test cross-language (7 voix non-fr sur `--language fr-fr`) **refusé par le moteur** (`frontend.cpp:93`, voix verrouillée sur sa langue natale) → FR = ff_siwis uniquement, Kokoro écarté du casting FR du jeu, gardé candidat voix EN. ② qwentts.cpp (§1.20 — premiers tests du moteur !) : **9/9 speakers nommés en FR** (serena, vivian, uncle_fu, ryan, aiden, ono_anna, sohee, eric, dylan, ~5-7 s/synthèse Vulkan) + **3/3 voix designed** par `--instruct` anglais (ancien gardien / héraut jeune / capitaine sterne) → 12 WAV dans `output/casting_voix_jeu/` (phrase du jeu « L'héritier du vide s'éveille… »). **Action : écoute utilisateur du casting en attente — statut « testé, non validé », aucun workflow avant verdict ; c'est qwentts.cpp (9 voix immédiates + design illimité, + clonage zero-shot candidat) qui répond au besoin « plein de voix FR » du jeu, pas Kokoro (MEMORY_BANK §1.20 + §1.21 mis à jour).**

- **2026-09-12 • veille (extension) • 5 nouvelles sources « outils système versionnés » : SDK Vulkan, Blender, Godot, uv, CMake — 2 découvertes immédiates (SDK Vulkan 1.4.357.0 dispo, uv 0.12.13 dispo)** — demande utilisateur (« c'est obligatoire » pour le SDK Vulkan + « vérifie si on doit en gérer d'autres ») suite à l'usage du SDK pour le build scratch audio.cpp (§1.21). Audit complet du poste vs `etat.json` : composants locaux NON suivis = **SDK Vulkan LunarG** (`C:\VulkanSDK\1.4.304.1`, prérequis de tous les builds natifs : sd-cli CMake, qwentts.cpp, builds audio.cpp), **Blender** (5.2.1, pipeline skins MPFB §1.15), **Godot** (`C:\Godot\godot.exe` 4.7.2 stable, moteur du jeu), **uv** (0.11.6, gestionnaire env du dépôt), **CMake** (4.4.3, builds natifs). Implémentation : helper générique `_veille_version_outil` (alerte 🆕 une seule fois par version amont nouvelle, version installée toujours affichée, jamais de maj auto) ; SDK Vulkan via l'URL « latest » officielle `sdk.lunarg.com` (version lue dans l'en-tête `Content-Disposition` — LunarG n'a ni releases GitHub ni flux JSON ; le repo GitHub `LunarG/VulkanSDK` n'existe plus), Blender via tags GitHub (pas de releases), les autres via releases GitHub + `--version` local. Premier run : **🆕 vulkan-sdk 1.4.357.0 (installé : 1.4.304.1 — 53 versions de correctifs de retard)** et **🆕 uv 0.12.13 (installé : 0.11.6)** ; ✅ Blender v5.2.1, Godot 4.7.2, CMake v4.4.3 identiques. Écartés en toute connaissance : pilotes AMD Adrenalin (pas d'API propre — la leçon §1.17 « pilote dégradé → redémarrer » reste manuelle), MSVC/Visual Studio (pas de flux versionnable fiable), MSYS2 (rolling, pas de releases), Git for Windows (autonotifiant). Les 2 🆕 sont dans `maj_en_attente.json` — mises à jour info, jamais sans accord explicite.

- **2026-09-12 • org-audio-cpp • GGUF Kokoro-82M TESTÉS (instruction permanente) : fonctionne sur master d'audio.cpp uniquement — RTF 0,13 sur RX 6950 XT, écoute utilisateur en attente** — suite directe de la détection du matin (`audio.cpp-gguf/Kokoro-82M-GGUF/kokoro-82m-{q8_0,bf16}.gguf`, entrée retirée de `maj_en_attente` : téléchargés → appliquée). Découverte clé : **le support `kokoro_tts` n'existe QUE sur master** d'audio.cpp (PR #496 multilingual + #515, commit `5bea9c7` du 11/09) — la release installée v0.7.3 ne le gère pas. Test réalisé par **build scratch** `C:\IA\audio_cpp_master_test` (2,8 Go, supprimable — `C:\audio-cpp` INTACT, pas de maj de l'installé) : cmake `GGML_VULKAN=ON` + `AUDIOCPP_STATIC_ESPEAK=ON` (espeak-ng-data.bin auto-généré), GGUF dans `C:\Modeles_LLM\Kokoro-82M-GGUF\`. **Résultats** : fr-fr `ff_siwis` q8_0 → 12 s d'audio en 3,09 s (RTF 0,258, 1er run incl. compile pipelines Vulkan) puis **RTF ~0,13 (7,3-7,8× temps réel)** warm ; bf16 fr et en-us `af_heart` conformes ; niveaux sains. Détail complet en MEMORY_BANK **§1.21** (nouveau). **Action : 3 WAV soumis à l'écoute utilisateur (`output/test_kokoro/`) — statut « testé, non validé », aucun workflow avant verdict ; si validé → attendre release audio.cpp avec kokoro_tts puis créer le workflow (voix off / previews ultra-rapides).**

- **2026-09-12 • qwentts.cpp • gestion RÉCUPÉRÉE d'`ai-doc2video` vers CE dépôt (retour au point du matin, outillage désormais local)** — décision utilisateur du soir : après le va-et-vient du jour (branché ici le matin → transféré à `ai-doc2video` l'après-midi, qui a appliqué la maj `779c7cb` + recompile 16:39), la gestion (maj + modèles) revient à generator-assets. Côté CE dépôt : **`scripts/manage_qwentts.py`** créé (recopié du `manage_qwentts.py` d'`ai-doc2video`, mêmes garanties : backup binaire → git pull --rebase + submodules → build CMake Vulkan Release → smoke test audio → rollback auto en cas d'échec ; catalogue 4 GGUF avec vérification en-tête/taille + SHA256 vs LFS OID HF ; User-Agent et textes adaptés) — commandes : `--check` (état git/binaires/modèles/sauvegardes), `--models [--verify-hash]`, `--update`, `--backup`, `--rollback [nom]`, `--list-backups`, `--test`, `--download-model <fichier>`. Source de veille `qwentts.cpp` **réintégrée** à `scripts/veille_versions.py` (commits amont vs HEAD local ; `etat.json` conservait `779c7cb` → continuité parfaite, aucun faux 🆕) ; AGENTS.md (note outil) et MEMORY_BANK §1.20 mis à jour. Côté `ai-doc2video` : `manage_qwentts.py` **supprimé**, 5 sous-commandes `factory.py qwentts-*` retirées (factory.py recompile OK), appel `check_periodic_due` retiré de `run_voices()`, AGENTS.md/README/memory bank mis à jour avec le pointeur vers ce dépôt (le moteur reste utilisé en lecture par `generate_voices.py`). Vérifié après migration : `--check` vert (clone `779c7cb` à jour, binaire opérationnel, 4 modèles intègres, 3 sauvegardes archivées). **Action : rien à faire — prochaine maj qwentts.cpp = `uv run python scripts/manage_qwentts.py --update` (jamais sans accord explicite).**

- **2026-09-12 • veille (run manuel du soir) • RAS général — seule nouveauté : 2 LLM tendance HF sans rapport audio** — re-vérification complète en fin de journée : **issue #1946 inchangée** (toujours 11 commentaires, open — rien depuis leejet le 10/09), **aucune release sd-cli depuis master-859** (hier 20:38 UTC), audio.cpp/trellis.cpp/FFmpeg/Python/paquets/ComfyUI à jour. Nouveauté unique : `hf-modeles-gguf` signale DeepSeek-V4-Flash-0731 (4,3 M dl — LLM texte) et Agnes-3.0-Flash (VLM image-text-to-text, créé la veille) — pipelines vérifiées API : **aucun text-to-audio → instruction permanente musique non déclenchée, rien à tester** ; pure tendance LLM.

- **2026-09-12 • qwentts.cpp • CORRECTION : la maj en attente a été appliquée cet après-midi par l'outillage d'`ai-doc2video` — clone à `779c7cb`, binaire recompilé le 2026-09-12 16:39** — vérifié en fin de journée : `C:\IA\qwentts.cpp` est passé de `a69194f` (02/09) à **`779c7cb`** (master, à jour), `qwen-tts.exe --help` s'annonce « qwentts.cpp 779c7cb (2026-09-11) », mtime du binaire 2026-09-12 16:39 → les 2 commits « ggml: fork update » ont été tirés et le moteur recompilé via `factory.py qwentts-update` (le transfert de gestion a donc déjà produit son effet). Usage doc corrigé chez ai-doc2video (commit local `2f8c8ab`, non pushé — leur dépôt a du WIP) : chemin binaire `build\Release\` (leur AGENTS.md indiquait à tort `build\bin\Release\`, inexistant — leur code `find_executable()` cherchait déjà au bon endroit) + exemples des 3 modes (customvoice = mode du pipeline `generate_voices.py`, speaker par défaut `ryan` ; clonage `--ref-wav`+`--ref-text` obligatoire ; voicedesign `--instruct`).

- **2026-09-12 • qwentts.cpp • gestion TRANSFÉRÉE au projet `C:\GIT\ai-doc2video` (maj + modèles) — la veille de ce dépôt ne le suit plus** — décision utilisateur du jour : le moteur TTS dédié Qwen3-TTS (`C:\IA\qwentts.cpp`) relève désormais du projet de production vidéo `ai-doc2video`, qui possède **déjà l'outillage complet** : `manage_qwentts.py` exposé via `python factory.py qwentts-check|backup|update|rollback` (maj git sécurisée, sauvegardes `C:\IA\qwentts_backups` (5 conservées), build Vulkan, smoke test, rollback auto) et son AGENTS.md référence binaire + modèles. Ajustements côté generator-assets : source de veille `qwentts.cpp` retirée de `scripts/veille_versions.py` (créée plus tôt dans la journée — l'entrée 🆕 « 2 commits d'avance » de `maj_en_attente.json` purgée, ce n'est plus notre affaire), ligne qwentts.cpp retirée du tableau « Process de mise à jour », note outil réécrite (« géré par ai-doc2video »), MEMORY_BANK §1.20 conservé pour le savoir-faire (capacités, leaderboard TTFA, inventaire modèles) avec la mention du transfert. **⚠️ Bug doc à signaler à ai-doc2video : son AGENTS.md indique `C:\IA\qwentts.cpp\build\bin\Release\qwen-tts.exe` — le chemin réel est `build\Release\qwen-tts.exe` (pas de dossier `bin\`).** État du moteur inchangé : build `a69194f` du 2026-09-02, 2 commits amont en attente (ggml only), 4 GGUF Q8_0 à jour — la mise à jour relève désormais d'`ai-doc2video` (`factory.py qwentts-update`).

- **2026-09-12 • écosystème ServeurpersoCom • veille manuelle : Qwen3-TTS-GGUF 🥇 1er en TTFA sur l'Open TTS Leaderboard HF (09/09) — et un NOUVEAU repo `yue2.cpp` (moteur dédié musique Yue2) créé aujourd'hui** — vérification directe des sources officielles (recherche web générale indisponible, quota épuisé jusqu'au 20/09) : ① **[Open TTS Leaderboard](https://huggingface.co/spaces/hf-audio/open_tts_leaderboard)** : Qwen3-TTS-GGUF classé **1er en TTFA (time-to-first-audio)** du benchmark streaming (le plus rapide à produire le premier audio en mode streaming mono-requête) — renforce qwentts.cpp comme candidat voix off chaîne (MEMORY_BANK §1.20) ; commit README du 09/09 = ajout de ce classement, aucun fichier GGUF changé (tailles locales re-confirmées identiques). ② **[ServeurpersoCom/qwentts.cpp](https://github.com/ServeurpersoCom/qwentts.cpp) issues** : rien de bloquant pour notre usage CLI Vulkan (#33 = ROCm only, #31 = concurrence CPU, #28/#29/#32 = bugs tts-server que nous n'utilisons pas encore). ③ **Nouveau repo [ServeurpersoCom/yue2.cpp](https://github.com/ServeurpersoCom/yue2.cpp) créé le 2026-09-12** (description vide, 1 star, pas encore de GGUF HF associé) — port dédié de Yue2, modèle musique déjà vu via les demos `audio.cpp-gguf` (journalisé, instruction permanente musique couvre la famille) → **à surveiller : dès qu'un GGUF Yue2 testable y apparaît, la règle « tester sans attendre d'accord » s'applique**. ④ Écosystème de moteurs dédiés frères (même philosophie C++17/GGML, tous Vulkan) : **omnivoice.cpp** (⭐172, TTS 600+ langues — famille `omnivoice` déjà dans audio.cpp, §1.13 « non testé, licence à vérifier »), **acestep.cpp** (⭐414, ACE-Step 1.5 standalone — notre moteur musique par défaut §1.11 est ACE-Step via audio.cpp ; un moteur dédié = candidat perf/simplicité à évaluer en info), **qwenasr.cpp** (⭐16, ASR Qwen), **audio.cpp-webui**. ⑤ Org Qwen officielle : aucun nouveau modèle TTS (6 modèles, dernière maj 2026-01-29). **Action : rien à installer ; note leaderboard ajoutée à MEMORY_BANK §1.20 ; re-vérifier yue2.cpp à la prochaine session de veille.**
- **2026-09-12 • qwentts.cpp • convention de gestion actée + source de veille branchée : `C:\IA\qwentts.cpp` clone JAMAIS modifié à la main, maj via ses propres scripts** — accord utilisateur du jour : le port C++17/GGML de Qwen3-TTS 12 Hz (ServeurpersoCom) devient un outil géré comme les autres, avec la règle spéciale « aucun README custom dedans, doc dans le dépôt » (MEMORY_BANK §1.20). État au jour de l'accord : clone à jour (commit `a69194f` du 2026-09-02), build Release Vulkan (`qwen-tts.exe`, `quantize.exe`), 4 GGUF Q8_0 en place dans `models\` (talker 1.7B base/customvoice/voicedesign + tokenizer 12 Hz, ~6,1 Go — tailles re-confirmées identiques au repo HF amont). Nouvelle source `qwentts.cpp` dans `scripts/veille_versions.py` (commits amont comparés au HEAD local, le repo n'a pas de releases) — premier run : 🆕 **2 commits d'avance détectés** (`6cb8a29` + `779c7cb`, tous deux « ggml: fork update », aucun changement moteur) → maj légère en attente de l'accord utilisateur (procédure AGENTS.md : `update.cmd` + `buildvulkan.cmd`, smoke test `qwen-tts.exe`, rollback `git checkout a69194f`).

- **2026-09-12 • sd-cli • 3 nouvelles releases hier soir (master-857 `7f986a9`, master-858 `5ebce93`, master-859 `7f410a3`, publiées 19:33/19:45/20:38 UTC le 11/09) — 18 commits d'avance sur notre build, mais toujours pas le fix #1946** — publiées ~2 h après notre publication du bisect binaire et la réponse de leejet ; corps de release vide (pas de notes officielles), contenu établi par compare GitHub `6b3edaa...7f410a3` : ① **PR #1958 (guard mémoire #1946) toujours NON fusionnée** — absente des 18 commits, le critère « NE PAS INSTALLER tant que pas de vrai fix » du 2026-09-11 tient donc ; ② le culprit identifié par notre bisect (#1940, runner lifecycles/weight residency) est toujours en place, mais la zone mémoire a bougé autour : **#1942 « single-GPU auto-fit with tiered parameter placement »** (placement par paliers — domaine exact de notre panne), #1943 (reuse graph cut plans CFG), **#1959 (preserve BF16 embedding weights for get_rows)**, #1963 (reuse graph plans when scale parameters change) ; ③ le reste = gros refactors (#1956 model source, #1957 split generation pipeline), features sans rapport (#1935 SenseNova U1.5, #1905 prefetch déjà connu innocent, #1962 API model version, #1964 scale overrides). **État #1946 : RAS côté issue** (aucun commentaire depuis leejet le 10/09 « Could you give it a try? » sur #1958, déjà traité). **Action : aucune maj requise — option à proposer si l'utilisateur veut avancer : installer master-859 et retester le repro Flux schnell seed 42 (18 commits dont #1942 s'attaquant au placement mémoire ; smoke test + éventuel contournement `GGML_VK_DISABLE_HOST_VISIBLE_VIDMEM=1` validé le 11/09) — sinon attendre la fusion de #1958.**
- **2026-09-12 • org-audio-cpp • GGUF **Kokoro-82M** publiés dans `audio.cpp-gguf/Kokoro-82M-GGUF` (kokoro-82m-bf16.gguf 202 Mo, kokoro-82m-q8_0.gguf 180 Mo) — préparation d'un support TTS à venir, audio.cpp v0.7.3 ne le gère pas encore** — vérifié : aucun `kokoro.json` dans `C:\audio-cpp\model_specs\` (les familles installées vont de ace_step à voxtral_realtime, sans kokoro) → les GGUF sont posés en amont d'une release logicielle future. Kokoro-82M = TTS ultra-léger (82 M params, qualité remarquable pour la taille) — candidat potentiel pour la **voix off de la chaîne YouTube** en complément/sauvegarde du TTS actuel, génération quasi instantanée à cette taille. **Action : surveiller la prochaine release audio.cpp (familles nouvelles dans `model_specs` = signal) ; à ce moment-là, test avec la recette de référence + soumission à l'écoute utilisateur (instruction permanente musique ne s'applique pas — c'est du TTS, mais même logique test → validation → workflow).**
- **2026-09-12 • hf-modeles-gguf • 13 nouveaux LLM en tendance HF (NeoHorse-1-9B, halogen-qwen3.8-flash-next, Edge0-8B-A1B-preview, Ling-3.0-tiny, OxCoder-9B…) — aucun text-to-audio, rien à tester** — vérification API sur les 2 noms suspects (TokenRhythm/NeoHorse-1-9B, Edge0/Edge0-8B-A1B-preview) : `pipeline_tag: text-generation` pour les deux → pas de déclenchement de l'instruction permanente musique ; pure veille LLM en diff.
- **2026-09-11 • veille • purge de `maj_en_attente.json` : les 6 entrées retirées (toutes déjà journalisées et sans action) — le hook ne re-signale plus d'infos anciennes à chaque session** — signalement utilisateur (« ComfyUI v0.35.0 ?? encore vérifie j'ai déjà vu cette info ») : l'entrée comfyui-core v0.35.0 datait du 2026-09-10 (journalisée avec analyse complète le même jour), et le mécanisme ne la retire que si la version installée rattrape — impossible pour une source purement info (ComfyUI non installé, sa3.cpp non installé, diffs HF/org sans notion d'installé). Entrées purgées après re-vérification une à une : ① comfyui-core v0.35.0 (toujours la dernière release, vérifié API le 2026-09-11 soir) ; ② comfyui-nouveaux-repos H3-LongVideo (journalisé le jour même) ; ③ hf-modeles-gguf 9 GGUF (journalisé le jour même, rien à tester) ; ④ sa3.cpp PR #42 (journalisé le jour même, pas d'action) ; ⑤ org-audio-cpp demos Yue2-3B-GGUF (journalisé 2×, instruction permanente yue2 inchangée — elle ré-entrera à la prochaine vraie nouveauté) ; ⑥ sd-cli master-853 (décision « NE PAS INSTALLER » du jour, journalisée — la veille recréera l'entrée dès qu'une release postérieure sortira, c'est le vrai signal attendu pour le fix #1946). Les baselines `etat.json` étant à jour, aucun 🆕 ne repartira des runs en cours ; le fichier est vide (`items: []`) et chaque nouveauté future y recréera son entrée.
- **2026-09-11 • sd-cli • BREAKthrough #1946 : leejet demande 2 runs diag (`GGML_VK_MEMORY_LOGGER=1` ± `GGML_VK_DISABLE_HOST_VISIBLE_VIDMEM=1`) — le 2e RÉUSSIT : `--offload-to-cpu` fonctionne, image correcte, et plus vite ; cause = fenêtre host-visible VRAM de 256 Mo du pilote AMD** — demande de leejet (17:49 UTC, [commentaire 5638526154](https://github.com/leejet/stable-diffusion.cpp/issues/1946#issuecomment-5638526154)), après lecture de notre log debug : échec = première exécution du graphe T5 après staging OK ; il a trouvé un chemin de cleanup pouvant lever une 2e exception pendant la gestion de l'échec (d'où le `terminate` qui rapporte `Queue::submit` au lieu du vrai `createComputePipeline` — le nouveau handler #1958 n'a jamais sa chance) ; il soupçonne l'interaction entre les petites allocations du refactor et la préférence Vulkan pour la VRAM CPU-visible. **Runs exécutés (même build `58d3f6e`, même commande pomme seed 42, machine libre vérifiée)** : ① run 1 = crash habituel (exit 127) avec le logger mémoire — staging T5 9 083,77 Mo OK en 5,84 s, buffer compute de 68,25 Mo alloué, puis `createComputePipeline: ErrorOutOfDeviceMemory` sur `rms_norm_mul_f32` avec « Total device: 9.17 GiB » et ~6 Go libres ; ② **run 2 = exit 0, `out_memlogger_nohv.png` générée (pomme conforme, seed 42)** — même staging T5 en 1,25 s (vs 5,84 s), conditioning 4,71 s, sampling 11,65 s (4 steps), VAE 1,41 s : **non seulement ça marche, mais tout est plus rapide sans VRAM host-visible**. `vulkaninfo` (section heaps/types archivée `scratch/bisect_1946/vulkaninfo_memory_section.txt`) explique le pressage : **heap 2 = 256 Mo seulement** (`DEVICE_LOCAL|HOST_VISIBLE`, type 2) sur cette carte — heap 0 = 15,73 GiB DEVICE_LOCAL pur, heap 1 = 15,92 GiB partagé/GTT ; avec la préférence host-visible active, les allocations device finissent en OOM contre cette fenêtre minuscule malgré le heap 0 quasi vide ; désactivée, tout passe en device-local. **Cause racine du crash `--offload-to-cpu` identifiée et contournement validé localement** (variable d'env, pas encore de correctif code) ; la question du OOM « graphe unifié » sans offload (segment 30/59) reste ouverte mais leejet dispose désormais de tous les éléments. Brouillon de réponse EN prêt : `scratch/bisect_1946/commentaire_memlogger_BROUILLON.md` (logs des 2 runs : `leejet_memlogger.log` 531 l., `leejet_memlogger_nohostvisible.log` 1787 l. ; gists à créer à la publication). **Action : publication des 2 logs + vulkaninfo + résultat = publiée le jour même avec accord utilisateur : [commentaire #issuecomment-5638768931](https://github.com/leejet/stable-diffusion.cpp/issues/1946#issuecomment-5638768931) + [gist 3 fichiers (2 logs complets + vulkaninfo)](https://gist.github.com/laurentvv/6422321845c3466179f20f7ec5c0cf80) ; à suggérer à leejet : gate host-visible par taille du heap ou défaut off si petit heap device-local host-visible.**
- **2026-09-11 • sd-cli • test `--max-vram` exécuté sur notre repro #1946 (build `58d3f6e`) : le budget mémoire fonctionne mais l'éviction ne suit pas — aucun budget ne fait passer le graphe, échec décalé mais identique (données propres pour leejet)** — accord utilisateur du soir (« test oui avec --max-vram ») suite au contournement suggéré par leejet sur #1947 ; machine libre vérifiée (check_charge_systeme OK, VRAM 0,7/16 Go). Repro Flux schnell inchangé (Q6_K, 512×512, 4 steps, euler, seed 42, `--vae-tiling`, pomme) + `--max-vram {10,12,14}` (unité GiB, confirmée dans le source : budget par device pour poids gérés + buffers pendant l'exécution segmentée). **Résultat : 3 échecs, mais propre et systématique** — ① avec budget, la VRAM libre rapportée suit exactement le plafond (6,19/4,32/2,20 Go ⇒ le rapport de VRAM libre cassé du rapport original est bien contourné) ; ② le besoin par segment devient CONSTANT (793,13 Mo device / 537,55 Mo budget = poids streamés + pic runtime, identique à chaque mort) alors que la VRAM réelle libre est large (2,2-6,2 Go) — le seul bloquant est la **réserve de budget** : la passe d'éviction ne libère jamais les 537,55 Mo de staging une fois la résidence saturée ; ③ le segment de mort recule avec le budget (4/59 double_blocks.2 → 11/59 double_blocks.9 → 19/59 double_blocks.17 ; meilleur cas 12 GiB, manque de 36,42 Mo seulement) mais chaque valeur meurt pareil ; ④ sans budget (run du 10/09), le besoin budget est plus faible (457,22 Mo au segment 30, single blocks plus légers) mais c'est le rapport VRAM libre cassé qui tue (954,20/906,93). **Lecture : sur cette carte (18 858 Mo de poids > 16 Go ⇒ stream-through obligatoire), le graphe ne peut pas se terminer sous aucune valeur `--max-vram` testée — l'échec se déplace seulement ; le chemin critique pour leejet = interaction éviction/budget du stream-through.** Logs : `scratch/bisect_1946/maxvram{10,12,14}_test.log` ; brouillon de commentaire EN prêt : `scratch/bisect_1946/commentaire_maxvram_BROUILLON.md` (tableau comparatif 4 configs). **Action : PUBLIÉ le jour même avec accord utilisateur — 1re publication (issuecomment-5638771022) supprimée à la demande de l'utilisateur car l'en-tête « Brouillon » du fichier de travail avait été inclus par erreur dans le corps ; republié au propre (fichier `commentaire_maxvram_FINAL_v2.md`, sans marqueurs, contexte ajouté = chemin plain/non-offload) avec [gist des 3 logs](https://gist.github.com/laurentvv/8a4eaa0437b586ae0e2c3e288181d819) : [commentaire #issuecomment-5639599085](https://github.com/leejet/stable-diffusion.cpp/issues/1946#issuecomment-5639599085) ; toute nouvelle valeur `--max-vram` ou itération PR #1958 sur l'éviction = retest même repro.**
- **2026-09-11 • sd-cli • issue #1947 : leejet diagnostique les 2 défauts de farawayso (sortie noire = overflow numérique ROCm ; perf = dépassement VRAM → repli mémoire partagée) et donne un contournement générique `--max-vram` — directement transposable à notre #1946** — réponse intégrale ([commentaire 5637803064](https://github.com/leejet/stable-diffusion.cpp/issues/1947#issuecomment-5637803064), 16:54 UTC, **postérieure à notre log debug #1946 de 16:37 — leejet n'a pas encore réagi à nos données**) : ① la sortie noire viendrait d'un **overflow numérique du backend ROCm** (cohérent avec Q6_K×ROCm KO / Q6_K×Vulkan OK constaté le matin) ; ② pour la perf, hypothèse mainteneur : **usage VRAM dépassant la limite → repli sur la mémoire GPU partagée (RAM système)**, d'où l'effondrement ; ③ contournement documenté pour forcer le calcul segmenté : **`--max-vram` réglé légèrement sous la VRAM requise** → segmentation automatique du graphe. **Pertinence directe #1946** : c'est notre signature exacte — le calcul de budget mémoire décide d'un graphe unifié (assez de VRAM en théorie), puis OOM à mi-sampling (segment 30/59, need 954,20 MB / available 906,93 MB) sur nos builds récents ; le contournement `--max-vram` est **testable dès maintenant sur notre build local `58d3f6e`** (même repro Flux schnell seed 42 + `--max-vram` ~500 Mo sous le besoin) : s'il fait passer la génération sur le code nouveau, on tient à la fois **un contournement provisoire pour réutiliser des builds récents sur 16 Go** et **la confirmation expérimentale du nœud mémoire** à resservir à leejet sur #1946. État : test non lancé → non validé ; publication du résultat sur GitHub = accord utilisateur. Toujours aucune réponse de leejet sur #1946 lui-même (8 commentaires, le dernier = le nôtre) ni activité sur PR #1958 ; releases inchangées (master-853 en tête, toujours sans le fix).
- **2026-09-11 • sd-cli • log debug complet ajouté à notre commentaire #1946 (bloc dépliable) + log de farawayso (#1947) téléchargé et comparé — différence de comportement texte-encoder CPU vs staging GPU** — ① notre commentaire (issuecomment-5637339395) édité pour inclure le log intégral en `<details>` (192 lignes, 12 lignes de barres de progression épurées ; version brute inchangée dans le gist) — équivalent de la pièce jointe de farawayso sur #1947, tout reste sur l'issue ; ② `farawayso_leejet_debug_log.txt` (220 lignes, master-853, ROCm, 7900 XTX 24 Go) archivé dans `scratch/bisect_1946/` : son run H3 (vid_gen, 22 frames) passe intégralement — sampling 5,1 s/it vs ses 825 s/it d'origine (**#1905 bien corrigé sur master-853**) — mais sortie tout noir (bogue Q6_K×ROCm, déjà journalisé le 2026-09-11 matin) ; **fait notable pour notre #1946** : chez lui le text-encoder qwen3vl préparé « on CPU » **tourne sur CPU** (`compute buffer size: 1.41 MB(RAM) on CPU`, aucun staging GPU, 17723 MB de buffers restés en RAM), alors que chez nous (build `58d3f6e`/PR #1958, `--offload-to-cpu`) les poids texte sont **staged vers Vulkan0** (copie VRAM 9 Go → OOM pipeline) — le staging GPU des poids offloadés semble être le comportement divergent à creuser côté mainteneur ; à resservir dans la discussion si leejet confirme que le staging est le nœud du problème.
- **2026-09-11 • sd-cli • test demandé par leejet exécuté (issue #1946, commentaire 5636742869) : crash `--offload-to-cpu` reproduit avec logs debug — info nouvelle clé : les poids T5 sont « staged to Vulkan0 » (VRAM) malgré l'offload CPU, et le guard #1958 ne couvre pas ce chemin** — commande exacte de leejet (Flux schnell Q6_K, 512×512, seed 42, `--offload-to-cpu --log-level debug`) sur le build local `58d3f6e` (PR #1958 head, gcc 16.2, ggml pin `e20c3a14` ; RX 6950 XT 16 Go, VRAM initiale 0,7/16 Go, machine libre) : mort non catchée identique au 2026-09-10 — exit 127, `vk::Device::createComputePipeline: ErrorOutOfDeviceMemory` sur `rms_norm_mul_f32` au **segment T5 1/1**, aucune image produite. Ce que le debug ajoute : ① le moteur compte bien les 18 858 Mo de poids en RAM (VRAM 0.00 MB), mais `model_manager` **stage ensuite les params de compute « to Vulkan0 »** : CLIP-L 235 Mo → exécution OK, puis **T5 9 083,77 Mo copiés en VRAM en 6,05 s** → pipeline creation OOM ; ② **aucun WARN du guard capacité de #1958 sur ce chemin** (il fonctionne pourtant dans le chemin sans offload : « cannot make enough memory available » → échec propre à `flux.single_blocks.9`) — le staging vers VRAM semble contourner le guard ; ③ ~6 Go de VRAM devraient rester libres après staging → OOM à la création de pipeline avec autant de libre = indice inattendu pour leejet (pas d'interprétation poussée de notre côté, faits publiés tels quels). Log complet : `scratch/bisect_1946/pr1958_offload_debug.log` (192 lignes) ; brouillon de réponse EN prêt : `scratch/bisect_1946/commentaire_leujet_debug.md`. **Publié le jour même avec accord utilisateur : [commentaire #issuecomment-5637339395](https://github.com/leejet/stable-diffusion.cpp/issues/1946#issuecomment-5637339395) + [gist du log complet](https://gist.github.com/laurentvv/7f93d6c2e50feee1483dc49b3f5730ea) ; aucune maj sd-cli (toujours 6b3edaa installé) ; le hook SessionStart alertera automatiquement sur la réponse de leejet.**
- **2026-09-11 • sd-cli • issue #1946 : leejet répond (17:27 locale, 15:27 UTC) et demande un run de debug dédié — le mainteneur travaille activement notre cas** — après notre CR du test PR #1958 (crash dur éliminé, propagation propre, mais OOM pipeline persistant : manque ~47 Mo à mi-sampling au segment 30/59 `flux.single_blocks.9` — need 954.20 MB / available 906.93 MB — et variante `--offload-to-cpu` = mort non catchée `createComputePipeline: ErrorOutOfDeviceMemory` pendant le compute), leejet (qui ne reproduit pas chez lui) demande d'exécuter une commande de repro précise (Flux schnell Q6_K + clip_l + t5xxl_fp16 + ae.safetensors, 512×512, 4 steps, seed 42, `--offload-to-cpu --log-level debug`) et de lui envoyer les logs détaillés. PR #1958 toujours open/non fusionnée, sans commit depuis le 2026-09-10. **Action : lancer la commande demandée sur notre machine (check_charge_systeme avant), préparer le log debug — publication sur GitHub = accord utilisateur requis ; le fix final viendra probablement d'itérations sur #1958 alimentées par ces logs.**
- **2026-09-11 • sd-cli • nouvelle release `master-853-b68d586` — NE PAS INSTALLER : le correctif de #1946 n'y est pas (PR #1958 toujours ouverte), rester sur `6b3edaa` (master-841)** — vérification manuelle approfondie : ① la PR #1958 « guard GPU memory capacity and propagate encoding failures » (le candidat fix testé le 2026-09-10 : crash éliminé, propagation propre, mais OOM pipeline persistant sur 16 Go) est **encore OPEN, non fusionnée** ; ② compare GitHub `6b3edaa...b68d586` = 12 commits : les deux suspects du bisect (#1905 prefetch, #1940 runner lifecycles) y sont toujours, dernier commit = #1932 (VAE decode tiling fallback sans auto-fit — sans rapport avec l'OOM au submit) ; ③ issue #1946 toujours open (6 commentaires) : un 2e reporter (`mlsterpr0`) a confirmé le crash Minimax-H3 « pre-allocated tensor … cannot run the operation » après master-841, et leejet ne reproduit pas sur son matériel. **Action : aucune maj sd-cli tant qu'une release postérieure n'inclut pas un vrai fix #1946 (testable via smoke Flux + le statut de la PR).** Complément (cross-ref `farawayso` vu le 2026-09-11, issue [#1947](https://github.com/leejet/stable-diffusion.cpp/issues/1947), RX 7900 XTX 24 Go/ROCm) : sa lenteur streaming 825 s/it semble résolue sur master-853 (PR #1905 incluse), MAIS il isole un 2e défaut AMD dans la même fenêtre 842–853 : **sortie tout noir en Q6_K + ROCm** (YAVG 0,00) alors que Q6_K + **Vulkan** et Q4_K_M + ROCm sont corrects — bogue noyau quant×backend, non bisecté, fenêtre chevauchant #1940/#1956 ; à noter pour nous : **Vulkan + Q6_K fonctionne sur master-853 côté 24 Go** — confirme que le calcul Vulkan est sain et que notre #1946 est purement la gestion de capacité mémoire 16 Go. RAS côté #1946 lui-même : pas de nouveau commentaire, PR #1958 sans review ni commit depuis le 2026-09-10. Réponse publiée sur #1947 avec nos données de bisect (fenêtre ouvre exactement à 843 = #1940, #1905 innocent, échec différent du sien : OOM capacité vs noyau) : [commentaire](https://github.com/leejet/stable-diffusion.cpp/issues/1947#issuecomment-5631188347), accord utilisateur du jour.
- **2026-09-11 • org-audio-cpp • `Yue2-3B-GGUF` complété cette nuit (q4_0 + sidecars tiktoken + nouvelles demos) et YuE2 entre dans le top trending text-to-audio — sortie runtime imminente, test toujours bloqué en attente (instruction permanente armée)** — ① repo HF `audio-cpp/Yue2-3B-GGUF` modifié le 2026-09-11 00:08 UTC : ajout `yue2-3b-q4_0.gguf` + demos comparatives q4_0/q8 (demo1-4 + tonight-awake) + `yue2_demo.mp4` + sidecars complets (`yue2-generation-config.json`, `yue2-model-config.json`, `yue2-vae-config.json`, `yue2-qwen.tiktoken` — format model_specs audio.cpp) → le paquet est finalisé, la famille ne manque plus que le runtime ; ② vérifié via API GitHub : **aucun commit `yue2` dans audio.cpp master** (40 derniers commits, jusqu'au 2026-09-11 — le support vit toujours en build interne mainteneur) ; ③ le pipeline `text-to-audio` trending HF voit entrer **3 repos YuE2** (`m-a-p/YuE2-3B` officiel, miroir `mrfakename/YuE2-3B`, GGUF communautaire `ngquocvinh/YuE2-3B-GGUF` créé ce jour). Au passage, master audio.cpp très actif côté parole : VibeVoice ASR Streaming 7B câblé (#497), diarization Sortformer v2.1 streaming (#493), **perf mira-tts CPU/Vulkan (#491)**, fix upstream Vulkan fill dispatch (#508 — à guetter dans la prochaine release), Irodori GGUF natif — rien de musical hors yue2. **Action inchangée : dès que la veille voit `yue2` dans une release audio.cpp → maj binaire + test immédiat recette gothic rock** (commandes prêtes `C:\audio-cpp\README.md` §yue2 ; rappel licence poids CC BY-NC 4.0 — limite chaîne monétisée).
- **2026-09-11 • hf-modeles-gguf • 9 GGUF en tendance HF — les 3 entrants audio sont les repos YuE2 (couverts ci-dessus), les 6 autres sont des LLM/VLM : rien à tester** — `Edge0/Edge0-35B-A3B-preview`, `thesysdev/OUI-1`, `ornith-ai/Ornith-1.5-35B-A3B` (Ornith-1.5-9B déjà invité local), `nex-agi/Nex-N2.5-Max`, `agentionai/Signal-3.8-27B-GGUF` + rotation classique Qwen3.8/GLM-5.3 — aucun modèle musical supplémentaire dans le pipeline text-to-audio (vérifié) → instruction permanente non déclenchée au-delà de YuE2 (runtime absent).
- **2026-09-11 • comfyui-nouveaux-repos • `Zzz1phyrf/ComfyUI-H3-LongVideo` (66★) — plugin long-format audio-driven pour H3 Ref2VA : source d'idées directe pour le endless H3 et la chaîne** — analyse du README : import d'un audio complet (chanson ou locution) → **segmentation automatique** selon analyse audio (cibles/min/max secondes, réglables avec pré-écoute), **plan de caméra par segment** (mode chant : auto/moderate/dynamic selon l'énergie audio, limite de cadrage ; mode locution : composition de référence figée), génération **séquentielle avec pause/reprise/retry**, **régénération locale d'un segment** avec retour version précédente, remux final avec l'audio source. Recette interne : Ref2VA + turbo LoRA, euler/beta **10 steps**, 960×544. **Idées transposables à `scripts/proto_endless_h3.py`** (points faibles connus) : file de chunks persistée avec reprise après crash, redo chunk par chunk sans relancer la chaîne, et découpage piloté par l'énergie audio plutôt que par durée fixe ; coté chaîne YouTube : la brique « vidéo chantée/lip-sync segmentée » existe désormais clé en main dans l'écosystème si un format clip devient pertinent.
- **2026-09-11 • sa3.cpp • nouveaux commits (PR #42 « fix a2a schedule ») — info, pas d'action** — stabilité du planificateur audio-à-audio sur le port C++ Stable Audio 3 ; sa3.cpp reste non installé (famille stable_audio déjà couverte par audio.cpp, workflow `sfx`).
- **2026-09-11 • paquets-python • 2 paquets avec maj détectés à 09:31 — APPLIQUÉS le jour même à 10:14 (commit `7417e33`) : onnxruntime 1.29.0→1.30.0 + numpy 2.5.2→2.5.3** — `uv lock --upgrade && uv sync` ; vérification post-maj : imports `core`/`workflows` + `main.py --help` OK (re-validés en session à 18:20). Re-vérification versions en session : installé = uv.lock = latest PyPI (onnxruntime 1.30.0, numpy 2.5.3), `uv pip list --outdated` vide → plus rien à faire. Rien de cassant (mineure + patch). Entrée retirée de `maj_en_attente.json` (résolue).
- **2026-09-10 • musique/parole • AuK (Tencent Hunyuan) analysé à la demande utilisateur ([GitHub](https://github.com/Tencent-Hunyuan/AuK)) — modèle de PAROLE 1,5 Md (TTS/édition/séparation), MIT, sorti la veille — INCOMPATIBLE poste aujourd'hui (PyTorch/safetensors, zéro GGUF), aucune action** — fondation speech : zero-shot TTS (clonage par référence) + **instruct TTS** (voix décrite en texte), édition de contenu (réécriture/remplacement/insertion, **paroles chantées**), édition acoustique (pitch/vitesse/volume) et paralinguistique (émotion, timbre, soupirs/rires, murmure↔parole), amélioration (débruitage/déréverb) et **séparation (extraction vocale musicale, locuteur cible)** — interface unifiée en langage naturel, DiT + VAE + encodeur MLLM Qwen2.5-Omni-3B obligatoire. 2 variantes HF `tencent/AuK` et `tencent/AuK-Flash` (distillée, 4 steps, CFG 0) ; rapport arXiv 2609.08936. **Vérifié HF : aucun GGUF nulle part** (ni officiel, ni org audio-cpp — 5 repos, pas d'AuK ; seul `drbaph/AuK-comfyui` du jour = re-pack ComfyUI, et les nœuds officiels `ComfyUI-AuK` existent déjà) → instruction permanente NON déclenchée (parole, pas musique — la cible gothic rock reste YuE2). **Pertinence future si audio.cpp publie un jour un port GGUF** : candidat sérieux pour la voix off chaîne (concurrent qwen3-tts §1.13 — l'édition d'émotion/pitch d'une même voix sans regénérer serait un gain pour les retakes) et l'extraction vocale musicale compléterait htdemucs/bs_roformer ; la veille org-audio-cpp le détectera automatiquement. Rien à télécharger (6+ Go de safetensors inutilisables).
- **2026-09-10 • comfyui-core • release ComfyUI v0.35.0 (2026-09-09) — pas de maj à faire (ComfyUI non installé, source d'idées), mais 3 signaux forts pour nos workflows** — ① **famille MiniMax-H3 très active** : PDD LoRA (#15908), controlnet en model patch (#15975), fix « prefetch raciness » sur references Fun Union (#16020 — possiblement lié aux instabilités chunk→chunk du endless), **H3 Max** sur T2V/first-last-frame/Reference (#16025, #16041) et **H3 Max Turbo** (#16094), VAE optional / references text-encoder-only (#16065), LoRAs DiffSynth-Studio/ModelScope (#15662) → à surveiller côté sd-cli/GGUF pour `h3_ref2va` (le turbo LoRA `lightx2v/Minimax-h3-Turbo` du rapport du 2026-09-09 reste la piste ~2,5× à A/B) ; ② **WAN3-Prime** ajouté aux partner nodes WAN (#15894) — signe précurseur d'un Wan 3.0 Prime dans l'écosystème ; ③ **Trellis : VRAM/RAM des workflows réduits** (#16034, #16054) + nœud 3D File3DToMesh (GLB/GLTF/OBJ/STL → MESH, #15919) → pertinent pour `mesh_ia` si les optimisations atterrissent dans trellis.cpp. Divers : remux HEVC hvc1 (#15809 — couvert par ffmpeg local), SDPA/FP8 pour 2 cibles AMD gfx117x (#16014/16039 — côté PyTorch, hors stack Vulkan RDNA2), partner nodes cloud seulement (Veo 2/3 retirés, Google-Omni 1.1, Muse, gpt-6-astra).
- **2026-09-10 • hf-modeles-gguf • 10 GGUF en tendance HF — AUCUN modèle musical dans le lot, rien à tester (instruction permanente non déclenchée)** — vérification manuelle du seul nom ambigu : `TokenRhythm/NeoHorse-1-4B` (dl 5,3k, ♥684, créé 2026-09-05) = **LLM text-generation** finetune Qwen3.5-4B (agentic/tool-use/coding/reasoning, safetensors, Apache 2.0) — **pas de la musique** malgré le nom d'org, et pas de GGUF natif. Autres : `prism-ml/Ternary-Bonsai-27B-gguf` (LLM ternaire 27B, 645k dl — invité llama.cpp possible, pas de besoin), `deepseek-ai/DeepSeek-V4.1-Flash` (LLM, publié ce jour), `inclusionAI/Ling-3.0-flash-VL` (VLM léger — LFM2.5-VL-3B déjà présent en invité), `nvidia/GLM-5.3-Flash-NVFP4` (**NVFP4 = illisible llama.cpp/Vulkan**, même cas que Qwen3.8-NVFP4 du 2026-09-09). Verdict : rotation tendance LLM/VLM, aucune action pour le poste.
- **2026-09-10 • org-audio-cpp • nouveau repo `VibeVoice-ASR-Streaming-7B-GGUF` — pas de besoin identifié, aucune action** — variante **streaming** (7B : bf16 17,36 Go / q8_0 9,86 / q4_k 5,86, créé 2026-09-09 23:55 UTC) de l'ASR VibeVoice ; le spec local `vibeasr.json` de v0.7.3 ne couvre que la variante **BitNet** INT8/ternary en mode offline → la variante streaming nécessiterait une maj runtime (non vérifiée, aucun besoin). Côté chaîne YouTube : inutile — la voix off est générée par TTS (qwen3-tts §1.13) depuis un texte qu'on possède déjà (les sous-titres existent par construction, pas d'ASR à faire). À considérer seulement si un besoin de transcription temps réel apparaît.
- **2026-09-10 • org-audio-cpp • nouveau repo `Yue2-3B-GGUF` : YuE2 arrive en GGUF Vulkan, mais le runtime audio.cpp n'est PAS encore publié — test impossible aujourd'hui, à lancer dès la sortie (instruction permanente)** — enquête complète à la demande utilisateur (repo [m-a-p/YuE](https://github.com/multimodal-art-projection/YuE)). **YuE2-3B = lyrics-to-song** (paroles balisées `[Verse]/[Chorus]` + descripteur `style=` → chanson complète chant+instru, stéréo 48 kHz, modèle AR–NAR MoT + flow matching + VAE, backbone tokeniseur Qwen) — même checkpoint pour création, **reprises zéro-shot** et **édition agentique sur partition ABC** (`cot=off|full|melody`, `abc_file=`). Sortie upstream yue2-v0.1.6, benchmarks SongBench du 2026-09-05 (YuE2 6,7/7 vs YuE1 4,9). GGUF HF (publié le 2026-09-10 07:45 UTC) : `yue2-3b-q8_0.gguf` 4,26 Go / `yue2-3b-bf16.gguf` 7,26 Go + **VAE séparé** (f16 0,27 / f32 0,53 Go) + sidecars configs/tiktoken — **⚠️ le support runtime famille `yue2` n'est NI dans v0.7.3 (installée) NI dans une release : annoncé « bientôt » sur la branche `dev`** (constaté : dev s'arrête au 2026-09-04, pas de `yue2.json`, aucune PR ouverte — les wav d'exemple ont été générés avec un build interne mainteneur). **Candidat DIRECT pour la cible « réalisme instrumental rock » (plafond DiT §1.11)** : les démos générées par audio.cpp vont jusqu'à **3 min 27 de chanson complète** — 5 copies locales à écouter : `output/test_yue2/` (demo1-4 + tonight-awake 207 s). ⚠️ **Licence poids CC BY-NC 4.0** (code Apache 2.0) — limite pour la chaîne YouTube si monétisée (ACE-Step reste MIT). Action : dès que la veille voit `yue2` dans une release/dev d'audio.cpp → maj binaire puis test immédiat recette de référence gothic rock (instruction permanente ; commandes prêtes dans `C:\audio-cpp\README.md` §yue2, `--model` = répertoire du repo GGUF, q8_0+vae-f16 en premier, bf16 si artefacts). Rien téléchargé à ce stade (le paquet GGUF peut encore évoluer avant la sortie du runtime).
- **2026-09-09 • hf-modeles-gguf • 2 nouveaux en tendance (après-midi) : NVFP4 Qwen3.8 et Music3-8bit MLX — hors de portée du poste, rien à faire** — `nvidia/Qwen3.8-27B-NVFP4` : quantisation **NVFP4** (NVIDIA Model Optimizer, safetensors, pas GGUF) — format flottant 4 bits NVIDIA (Blackwell/TensorRT) illisible par llama.cpp/Vulkan ; Qwen3.8-27B est déjà installé localement en Q3_K_M (invité). `mlx-community/MiniMax-Music3-8bit` : ré-quantisation **MLX** (mlx-audio, Apple Silicon uniquement) du MiniMax-Music3 **déjà installé et validé** en GGUF (§1.10) — pas un nouveau modèle. Rotation normale du top trending (les 5 entrants du matin en sont déjà sortis). Entrées retirées de `maj_en_attente.json` (triage : aucune action ; trace ici et dans l'entrée du matin pour Irodori TTS).
- **2026-09-09 • veille • NOUVELLE SOURCE : écosystème ComfyUI (idées de workflows) + gros rapport de recherche GitHub** — demande utilisateur suite au workflow `h3_ref2va` (reproduction CLI du nœud ComfyUI « HR Endless Sampler ») : recherche `gh` complète de l'écosystème consignée dans **`docs/recherche_comfyui_2026-09-09.md`** (galeries, écosystème MiniMax-H3/Ref2VA — très actif depuis juillet —, Wan/LTX, audio, 3D, pipelines de production). Idées adaptables phares : ① **turbo LoRA Ref2VA 8 steps** (`lightx2v/Minimax-h3-Turbo` sur HF, 1,3 M dl — le flag `--lora-model-dir` existe déjà sur `sd-cli vid_gen`) → ~2,5× sur h3_ref2va, à A/B ; ② réf de continuation **downscalée alignée 32 px** + **fenêtre audio « qui remonte »** pour la boucle endless (leçons HR Endless Sampler + Motion-Context 928★) ; ③ prompts par chunk écrits par VLM local ou `lightx2v/MiniMax-H3-Prompt-Rewriter-LoRA` ; ④ galerie **634 prompts H3 publics** (awesome-MiniMax-H3-cases) ; ⑤ FaceRefine / H3-pour-stills / Photoshoot (série portrait cohérente) / production.json (short-drama-production). La veille `veille_versions.py` surveille désormais : releases du cœur ComfyUI (v0.34 = TRELLIS2, masques H3 par token, Wan 3.0 partenaires — bon prédicteur des évolutions sd-cli), commits de 4 repos clés (HR-Endless-Sampler, H3-Motion-Context, ComfyUI_examples, LTXVideo), et nouveaux repos `topic:comfyui` ≥20★ créés sur 45 j (diff, baseline 2026-09-09). Au passage : fix lecture `version.json` en `utf-8-sig` (BOM → section audio.cpp en ⚠️ depuis la maj v0.7.3). **Complément du soir : nouvelle source `sa3.cpp`** (port C++/GGML de Stable Audio 3, Vulkan, zéro PyTorch — commits, repo sans releases ; famille stable_audio déjà couverte par audio.cpp).
- **2026-09-09 • org-audio-cpp • nouveau fichier GGUF : Irodori-TTS-v4-Small (anime, q8_0)** dans `audio.cpp-gguf` — nouvelle famille TTS potentielle d'audio.cpp (« Irodori TTS v4.1 anime »). Info : pas de besoin actuel (voix off chaîne = qwen3-tts validé §1.13) ; à considérer seulement si un perso anime du jeu Godot demande une voix typée.
- **2026-09-09 • hf-modeles-gguf • 5 entrants en tendance HF** : Nex-N2.5-Pro, Ling-3.0-flash-VL, Qwen3.8-27B-Mythos-Class, DeepSeek-V4-Flash-Vision (LLM/VLM — info compatibilité llama.cpp à vérifier au besoin) et **`thepatch/stable-audio-3-small-sfx-GGUF`** — variante **SFX** de SA3 small en GGUF : candidate directe pour le workflow `sfx` (audio.cpp, famille stable_audio). **⚠️ Précision du même jour : ce repo GGUF multi-fichiers cible le moteur DISTINCT [`sa3.cpp`](https://github.com/betweentwomidnights/sa3.cpp)** (port C++/GGML de Stable Audio 3, CPU/CUDA/Vulkan/Metal, philosophie zéro PyTorch — compatible repo mais outil séparé, à surveiller par la veille) ; le modèle est en fait **déjà supporté par audio.cpp v0.7.3** (paquet officiel `Stable-Audio-3-Small-SFX-GGUF/stable-audio-3-small-sfx-f16.gguf`, 2,20 Gio, org `audio-cpp`). **Testé le jour même (instruction permanente)** : 6 échantillons graine 42 dans `output/test_sa3_small_sfx/` (recette gothic rock de référence + 5 SFX jeu/chaîne), **RTF 0,19** (le plus rapide du poste) — **VALIDÉ le jour même après 3 itérations sur le sample pluie** (4 SFX « ok » d'emblée ; pluie : volume → spectre du prompt → chaîne nappe gain+limiteur, cf. MEMORY_BANK §1.11) ; workflow `sfx` passé en moteur IA par défaut (`--sfx-engine procedural` en repli).

- **2026-09-08 • sd-cli • bisect #1946 TERMINÉ : coupable isolé = `462d675` (PR #1940 « unify runner lifecycles and weight residency »)** — 2e étape par builds locaux MSYS2/UCRT64 gcc 16.2 (`-DGGML_VULKAN=ON`, ggml pin `e20c3a14`, ~2 min/build, source dans `scratch/bisect_1946/sd-cpp-src/`, binaires `build-841`/`build-1905`/`build-1940`) : `6c57cc3` (#1905 prefetch seul) = **✅ image → #1905 innocent** ; `462d675` = **❌ crash** avec l'exception réelle enfin visible `what(): vk::Queue::submit: ErrorOutOfDeviceMemory` (le « crash silencieux » du build MSVC officiel = cette exception avalée) — OOM au premier submit de calcul après l'allocation des ~9,4 Go de buffers DiT. **Résultats publiés sur l'issue** : [#issuecomment-5592101471](https://github.com/leejet/stable-diffusion.cpp/issues/1946#issuecomment-5592101471). Toolchain MSYS2 complété au passage (cmake, ninja, spirv-headers installés — réutilisable pour tout build C++ Vulkan). 6b3edaa reste installé ; machine proposée pour tester un build correctif. ⚠️ Builds gcc locaux : PATH doit contenir `/ucrt64/bin` (libgomp-1.dll).
- **2026-09-08 • modèles • incident nettoyage C:\Modeles_LLM : SeedVC supprimé (voulu), 4 modèles supprimés PAR ERREUR puis restaurés intégralement** — consigne utilisateur ambiguë mal interprétée (« les autres, même fish audio, en internet ça peut me servir » = GARDER, lu comme « tout supprimer ») → Qwen3.8-27B, Ornith-1.5-9B, Fish-Audio-S2-Pro, LFM2.5-VL-3B + mmproj (~29,5 Gio) effacés puis **restaurés en ~5 min** depuis leurs sources vérifiées (mradermacher/Qwen3.8-27B-GGUF pour le Q3_K_M, ornith-ai, audio.cpp-gguf, LiquidAI). **Leçon : avant toute suppression effective, reformuler la liste validée à l'utilisateur quand l'accord n'est pas 100 % explicite** (règle ajoutée à AGENTS.md). Bilan final du nettoyage : seul SeedVC-MLX (3,0 Gio, test chant-clone rejeté) supprimé. Les 4 restaurés restent des INVITÉS sans workflow (usage potentiel futur décidé par l'utilisateur).
- **2026-09-08 • musique • SA3 Medium testé (instruction permanente) → NON VALIDÉ : « toujours pop »** — `stable_audio_3_medium_f16` (5,43 Gio) testé avec la recette gothic rock de référence (83 BPM/C# minor dans le prompt — SA3 n'a pas d'options planner, graine 42) : **RTF 0,48** (le plus rapide des moteurs musicaux du poste après SA3 small), mais verdict utilisateur « toujours pop » → consigné §1.11 MEMORY_BANK, pas de workflow. État de la quête registre sombre : ACE-Step (plafond réalisme), Music3 (lent, partiel), SA3 small (qualité), SA3 Medium (pop en texte) — **seule piste locale restante : mode essence `init_audio` sur Medium** (contourne le conditionnement texte) ; sinon attente de nouveaux moteurs via la veille `hf-modeles-gguf`.
- **2026-09-08 • outillage • `scripts/check_charge_systeme.py` : contrôle CPU/GPU/RAM/VRAM avant toute génération (règle AGENTS.md)** — motif : mesures RTF faussées par contention (voir entrée audio.cpp du même jour) ; zéro dépendance (compteurs WMI `GPUPerformanceCounters` + registre), exit 0/1/2, top process CPU, flags EN (`--cpu-threshold`…) — identifiants en anglais règle AGENTS.md. À lancer avant chaque génération lourde.
- **2026-09-08 • audio.cpp • v0.7.3 FINALEMENT INSTALLÉE ET GARDÉE (A/B idle post-reboot : RTF inchangé)** — épisode complet de la soirée : ① maj v0.7.3 → smoke ace_step **12 s** = RTF 5,0-5,5 vs baseline « ~1,5 » (mesuré sur 28 s) → conclusion hâtive « régression 3× » + rollback v0.7.2 ; ② rollback mesure RTF 4,55 → incohérence → vraie cause n°1 : **~4,6 Gio de VRAM dédiée retenue par des processus morts** (PID introuvables, partis au reboot) ; ③ post-reboot machine propre (0,5/16 Gio) : A/B rigoureux **à durée identique au baseline (28 s)** : v0.7.2 RTF **1,620** vs v0.7.3 RTF **1,617** → **aucune régression, v0.7.3 gardée** (PR #423 fallback attention sans MMA kernels : aucun effet mesuré sur RX 6950 XT). **Leçons** : (a) le RTF dépend fortement de la durée — le chargement fixe ~40 s du modèle écrase les courtes générations (12 s et 28 s prennent ~50 s de wall time) → **toujours comparer à durée identique** ; (b) `check_charge_systeme.py` avant toute mesure/génération (règle AGENTS.md du jour) ; (c) une VRAM occupée par des processus morts dégrade silencieusement les perfs sans OOM visible. Nouveautés v0.7.3 : familles TTS/ASR (sanotts, mira_tts, sopro_tts, vibeasr), PocketTTS streaming, ModelScope natif, `--version` — **rien de musical**. README `C:\audio-cpp\README.md` + CHANGELOG + MEMORY_BANK (pins v0.7.3) à jour.
- **2026-09-07 • musique • campagne anti-pop close : xl-sft+CFG = meilleur levier, plafond réalisme instrumental confirmé** — 9 candidats testés (xl-turbo/base/xl-sft ± CFG 7.0 ± lm_cfg_scale ± négatif, + MiniMax-Music3) : **seul xl-sft 4B + CFG 7.0 respecte tempo ET mode mineur** (« passe le mieux », verdict utilisateur), mais « très loin du résultat souhaité » : basse/batterie/guitares synthétiques — aucun réglage ne compense la capacité du modèle. Music3 crédible en tonalité mais 37 min/morceau. Tout est consigné §1.11 MEMORY_BANK (statut testé non validé) ; veille `text-to-audio` + instruction permanente en place pour tester les futurs moteurs. Au passage : ModelScope supporte HTTP Range → 166 Mo/s en parallèle (xl-sft en 3,5 min), script de téléchargement ACE-Step corrigé.
- **2026-09-07 • musique • INSTRUCTION PERMANENTE (AGENTS.md) : tester tout nouveau modèle musical signalé par la veille, sans attendre d'accord** — motif : plafond confirmé du réalisme instrumental rock d'ACE-Step (les leviers CFG/négatif/planner orientent le style, cf. `output/test_acestep_sft/` cand_A→F, mais basse/batterie/guitares restent synthétiques, « mou », −15 LUFS vs −10,5 pour le pop témoin ; rendu le plus crédible du poste = registre dark folk à ancre Wardruna, §1.11). Recette de référence + consignation MEMORY_BANK détaillées dans AGENTS.md. Tests en cours ce soir : candidat I sur MiniMax-Music3 (registre rock, jamais testé sur ce moteur), xl-sft 4B en téléchargement.
- **2026-09-07 • veille • NOUVELLE SOURCE : nouveaux modèles LLM/VLM GGUF tendance sur Hugging Face** : section `hf-modeles-gguf` de `veille_versions.py` — diff du top 60 trending HF (API `sort=trendingScore`, filtres `library=gguf` + `pipeline_tag=text-generation` / `image-text-to-text`), baseline enregistrée le 2026-09-07, seuls les ENTRANTS dans le top sont signalés (dl/likes/date de création en contexte). Rôle info : vérifier la compatibilité moteur (llama.cpp/audio.cpp, Vulkan, zéro PyTorch) avant tout téléchargement — jamais d'installation automatique. Détection validée en test réel (MiniCPM5-2B flaggé).
- **2026-09-07 • veille • surveillance automatique de l'issue sd-cli #1946 par le hook SessionStart** : l'issue (régression master-848, rollback) est vérifiée à chaque nouvelle session via un appel API GitHub non authentifié (timeout 5 s, échec silencieux) ; alerte injectée dans le contexte uniquement si l'issue a bougé (nouvelle réponse, fermeture = correction probable à retenter avec smoke test Flux). État déjà vu : `output/veille/issue_sdcli_1946.json` ; consigne agent : résumer les réponses et proposer la mise à jour selon process AGENTS.md (accord utilisateur requis). À supprimer après installation d'une release corrigée (`bloc_issue_sdcli` + état + mention AGENTS.md).
- **2026-09-07 • sd-cli • `master-848-9cdb6b6` testée → RÉGRESSION → ROLLBACK, rester sur `6b3edaa`** ; **`master-849-d04e895` (signalée par l'utilisateur) AUSSI CRASHÉE** = 848 + 1 commit sans rapport (fix embeddings SDXL #1939), crash identique vérifié → commentaire publié sur l'issue ; les deux versions sont à ÉVITER jusqu'à correctif. : 3 nouveaux commits par rapport à master-845 (réutilisation des plans de graphe entre passes CFG #1943, refactor extensions ggml #1945, **préservation des poids d'embedding K-quantifiés #1936**). Installée puis **crash silencieux (exit 127 sans aucun message) avec Flux schnell + encodeurs séparés sur Vulkan/AMD RX 6950 XT**, dès la préparation des poids du DiT (`model_manager.cpp:428`, buffers 9379 Mo) ; `--auto-fit off` idem, `--offload-to-cpu` = `ErrorOutOfDeviceMemory` (createComputePipeline), `--max-vram 6` = `t5 segment 17/26 failed during weight preparation` puis mort silencieuse. **SDXL monolithique non affecté** ; `6b3edaa` OK en A/B (image générée) → régression dans la zone model_manager/runner (#1940/#1942/#1905). **Issue détaillée (EN) : leejet/stable-diffusion.cpp#1946**. Rollback appliqué depuis `backups/backup_2026-09-07_6b3edaa/`, smoke test Flux 4 pas revalidé. Docs mises à jour : `C:\SD\README.md` (avertissement NE PAS INSTALLER master-848). **Action future : ne réinstaller que via une release postérieure corrigeant l'issue** (vérifier son statut avant toute tentative).
- **2026-09-07 • veille • plus d'automatisation cron : la veille se déclenche à l'ouverture de session, relayée par le hook SessionStart** : chaque nouvelle session ZCode du projet reçoit les 7 derniers jours de ce journal **et les majs en attente** via le hook `.zcode/config.json` → `scripts/hook_session_start.py` (validé en session réelle ; les hooks de scope projet exigent l'approbation « Review » — gate de confiance). **Déclenchement automatique sans cron** (PC allumé à heure variable) : si la dernière veille date de > 20 h (date de `output/veille/rapports.log`), le hook relance `veille_versions.py` en arrière-plan (process détaché, log `output/veille/veille_arriere_plan.log`). Le script maintient désormais `output/veille/maj_en_attente.json` : entrée ajoutée à la détection, **retirée automatiquement** quand la version installée rattrape la dernière vue ; l'agent journalise + commit les nouveautés absentes du journal (convention §🔄 AGENTS.md). Première entrée en attente : sd-cli `master-848-9cdb6b6` (installé : `6b3edaa`).
- **2026-09-07 • sd-cli • nouvelle release `master-845-80bac2d`** (installé : `6b3edaa` du 2026-09-06 — 4 commits d'écart, notes de release vides → changelog via compare GitHub) : ① **prefetch des couches streamées pendant le calcul** (#1905 — le streaming des poids chevauche le calcul, gain de perf) ; ② refactor du cycle de vie des runners et de la résidence des poids (#1940) ; ③ logging verbeux + sélection du niveau de log (#1941) ; ④ **auto-fit mono-GPU avec placement hiérarchique des paramètres** (#1942 — gros modèles répartis automatiquement VRAM/RAM). **Impact projet : image/vidéo côté sd-cli (Flux, Wan 14B, MiniMax-H3 27,8 Gio)** — gains perf/mémoire bienvenus, rien de cassant visible, aucun lien avec ace_step. Action : mise à jour selon process AGENTS.md (backup `C:\SD`, asset `sd-master-80bac2d-bin-win-vulkan-x64.zip`, smoke test Flux 4 pas).

- **2026-09-06 • 3D • NOUVELLE STACK VALIDÉE : image → 3D volumique par TRELLIS.2-4B (trellis.cpp, Vulkan) — workflow `mesh_ia`** (détails §1.14 MEMORY_BANK) : enquête image-to-3d (TRELLIS.2 / Hunyuan3D-2.1 / stable-fast-3d) → seuls les ports C++/GGML permettent de rester en Vulkan zéro-PyTorch. `pwilkin/trellis.cpp` v0.6.0 (MIT) installé dans `C:\trellis` + GGUF f16 `ilintar/trellis2-gguf` (16,4 Go). Perf RX 6950 XT : res 512 = **10 min 44 s**, res 1024 = **55 min 10 s** (pas de matrix cores Vulkan sur RDNA2 → ~4-6× plus lent que Strix Halo). Validé utilisateur sur le casque ; workflow `main.py -w mesh_ia "<prompt>|-i img> --res 512|1024` (GLB PBR + rendus contrôle + planche). Veille étendue : releases trellis.cpp + fichiers du repo GGUF. **Impact projet : le jeu Godot peut maintenant générer des objets 3D volumiques complets (casques, statues, créatures) depuis un simple prompt ou sprite** ; variantes q4/q8 des GGUF non testées (levier VRAM/temps).

- **2026-09-06 • TTS • clonage vocal français validé sur 3 moteurs GGUF (détails §1.13 MEMORY_BANK)** : qwen3-tts 1.7B / VoxCPM2 / Fish S2-Pro testés avec la voix de l'utilisateur (11,8 s) — Vulkan, RTF 1,5-1,7, mono (44,1/48/24 kHz). Écueils : qwen3 Base et fish exigent le transcript de la référence (contourné par transcription locale qwen3-asr 0.6B, bonus réutilisable sous-titres) ; voxcpm2 clone sans transcript. Expression : fish = balises inline `[whisper]/[excited]` (le plus riche, mais licence commerciale payante), qwen3 = `--instruct`. **Impact projet : voix off chaîne = qwen3-tts (Apache-2.0) en piste sérieuse, à valider à l'écoute** ; 12,5 Gio de modèles installés dans `C:\Modeles_LLM\`.
- **2026-09-06 • veille • angle mort sd-cli corrigé + watch org audio-cpp ajouté** : `veille_versions.py` surveille désormais **sd-cli/stable-diffusion.cpp** (commit local via `sd-cli.exe --version` vs tag de release `leejet/stable-diffusion.cpp` — état actuel : commit `6b3edaa` = release `master-841-6b3edaa` du 30/08, **à jour**) et **toute l'org audio-cpp sur HF** (nouveaux repos dédiés + nouveaux fichiers de `audio.cpp-gguf`, suite au rectificatif MiniMax-Music3). Création de `C:\SD\README.md` (modèle audio-cpp/FFmpeg : release, familles modèles, commandes validées img/vid_gen/upscale, maj+rollback) ; AGENTS.md mis à jour (veille, tableau maj, table doc).
- **2026-09-06 • audio-cpp • RECTIFICATIF : MiniMax-Music3-GGUF n'a PAS disparu — repo dédié** (l'alerte précédente était fausse). L'org `audio-cpp` héberge **3 repos** : `audio.cpp-gguf` (collection principale), **`MiniMax-Music3-GGUF`** (toutes variantes q4_0/**q4_k**/q8_0/bf16 — plus que la copie locale) et `VibeVoice-7B-GGUF` (7B absent du repo principal). **Leçon : pour juger de la retéléchargeabilité d'un modèle, lister TOUS les repos de l'org (`https://huggingface.co/api/models?author=audio-cpp`), pas seulement l'arborescence d'`audio.cpp-gguf`.** La règle AGENTS.md « vérifier avant suppression » reste pertinente ; liste des irremplaçables : **vide à ce jour**. Inventaire du même jour : musique = ace_step/minimax_music3/**heartmula**/**midashenglm_gen**/stable_audio (dont **Medium**, défaut de spec, jamais testé) ; sep = htdemucs/bs_roformer/mel_band_roformer ; veille **sd-cli/stable-diffusion.cpp absent de `veille_versions.py`** (angle mort à combler).
- **2026-09-06 • test HF • `facebook/musicgen-melody-large` testé → NON RETENU (détails §1.12 MEMORY_BANK)** : aucune route GGUF/audio.cpp (famille inexistante, aucun GGUF valide sur HF/ModelScope) → test via transformers 5.16.1 CPU en venv isolé. RTF 11,6, 32 kHz mono, licence **CC-BY-NC** ( incompatible chaîne monétisée), et **régression v5 : conditionnement mélodie inopérant** (A/B même graine → sorties bit-identiques ; kwargs v4 ignorés silencieusement + bug resample). **Impact projet : aucun changement de stack** — musique = ACE-Step 1.5 (MIT, RTF 1,5) ; essence de référence = SA3 init_audio. Modèle 9,8 Gio supprimé le jour même sur décision utilisateur après écoute (retest possible si audio.cpp ajoute musicgen-GGUF ou si transformers v5 corrige la régression melody).
- **2026-09-06 • stable_audio/SA3 • nouveau moteur « essence d'une référence » validé** : Stable Audio 3 small-music f16 (2,2 Gio, HF audio.cpp-gguf) — mode `init_audio` = conditionnement stylistique par audio, **fonctionne sur Vulkan à RTF 0,25** (pas de limite buffer contrairement aux routes audio d'ace_step). Fidélité mesurée : BPM 83,4 vs 83,3 sur la référence, énergie suivie. **Impact projet : « se caler sur un type de musique en audio » → SA3 init_audio est l'outil dédié** (ACE-Step reste pour chansons avec paroles). À écouter : `output/music_chanson/llb_sa3_essence.mp3`.
- **2026-09-06 • ace-step/audio.cpp • routes audio-conditionnées (cover/repaint/lego/extract) bloquées sur Vulkan** : le VAE encoder exige un buffer unique de ~4,5-4,8 Gio, au-dessus de la limite `maxBufferSize` (4 Gio) du pilote AMD Windows — limite **par buffer** (VK_KHR_maintenance4), pas de VRAM totale (d'où aucun pic VRAM visible). Buffer de taille fixe, indépendante de la durée de la source. **Workaround validé : `--backend cpu --threads 20`** (cover 30 s en 77 s, RTF 2,58, turbo 2B). Test réel : cover « Love Like Blood » (groupe allemand) réinterprété en dark folk Vent-Gris. **Impact projet : réinterprétations calées sur un audio existant = CPU obligatoire pour l'instant** ; si une future release audio.cpp/ggml splitte ce buffer ou si le pilote AMD relève la limite → retester en Vulkan.
- **2026-09-06 • veille • mise en place du système** : script `scripts/veille_versions.py`
  (audio.cpp, FFmpeg, Python, paquets uv, paquets GGUF ACE-Step HF, repo ACE-Step-1.5,
  llama.cpp ; notes de release archivées dans `output/veille/notes/`), automatisation
  quotidienne 9 h, process de mise à jour documenté dans `AGENTS.md`. Baseline : audio.cpp
  v0.7.2, FFmpeg 9.0.1, Python 3.12.9, ACE-Step repo v0.1.8, llama.cpp v0.4.0.
- **2026-09-06 • paquets-python • protobuf 7.36.0 → 7.36.1** (release v36.1 du 31/08) :
  optimisation du chemin d'analyse Python pur pour les noms d'enum JSON personnalisés +
  tests ; C# (alias d'enum) et Rust (refactor crates) touchés. **Impact projet : aucun**
  (dépendance transitive, rien de cassant) — appliqué et vérifié (`fcfaeb0`).
- **2026-09-06 • à surveiller • paquets GGUF ACE-Step sur HF** : les variantes XL
  (xl-turbo, xl-sft) n'y sont toujours PAS (uniquement sur le miroir ModelScope
  `HereIsMark/audio.cpp-gguf`). Si un jour elles apparaissent sur HF, préférer HF
  (miroir officiel audio-cpp). Le script de veille liste les fichiers ACE-Step du repo HF.
